<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>ARM Cortex-M3 M4</title>
    <url>/2025/01/20/ARM-Cortex-M3-M4/</url>
    <content><![CDATA[<h1 id="ARM-Cortex-M3-M4"><a href="#ARM-Cortex-M3-M4" class="headerlink" title="ARM Cortex-M3 M4"></a>ARM Cortex-M3 M4</h1><p><strong>引言</strong> </p>
<p>ARM Cortex-M3 M4是ARM公司推出的32RISC（精简指令集计算机）处理器内核，用于各种嵌入式系统中。</p>
<span id="more"></span>

<h4 id="第一章-ARM-Cortex-M-处理器简介"><a href="#第一章-ARM-Cortex-M-处理器简介" class="headerlink" title="第一章 ARM Cortex-M 处理器简介"></a>第一章 ARM Cortex-M 处理器简介</h4><h5 id="1-1-什么是ARM-Cortex-M处理器"><a href="#1-1-什么是ARM-Cortex-M处理器" class="headerlink" title="1.1 什么是ARM Cortex-M处理器"></a>1.1 什么是ARM Cortex-M处理器</h5><p>Cortex-M3和M4为cortex处理器中的第一个，M3和M4处理器使用32位架构，寄存器组中的内部寄存器、数据通路以及总线接口都是32位的，Cortex-M处理器使用指令集架构（ISA）为Thumb ISA，其基于Thumb-2技术并同时支持16位和32位指令。</p>
<p><strong>指令集架构</strong> </p>
<p>指令集架构定义了以下的内容：</p>
<ul>
<li>处理器可以之心那个的指令集合：例如加法、减法、乘法、除法、数据加载、数据存储、跳转等。每条指令都有特定的二进制编码。</li>
<li>指令格式：指令由哪些部分组成，例如操作码、操作数等</li>
<li>寄存器组织：处理器内部有哪些寄存器，每个寄存器的用途和大小。</li>
<li>寻址方式：如何访问存储器中数据，例如立即数寻址、寄存器寻址、直接寻址、间接寻址等。（这是汇编的知识）</li>
<li>铸锻和异常处理：如何响应外部中断和内部异常。</li>
</ul>
<p>ISA是软件和硬件之间的接口，程序员通过使用ISA定义的指令来编写程序，而硬件则根据ISA的定义来执行这些指令。不同的处理器有不同的ISA，例如X86、ARM、MIPS、RISC-V等</p>
<p><strong>Thumb-2技术：</strong> 后来的ARM架构引入了Thumb-2技术，它扩展了Thumb指令集，增加了32位指令，使得Thumb指令集更加强大和灵活，几乎可以替代传统的ARM指令集，同时保持了较高的代码密度。Cortex-M系列处理器主要使用Thumb-2技术。</p>
<p>共同点：</p>
<ul>
<li><p>哈佛总线架构，且具有统一的存储器空间：指令和地址总线使用相同的地址空间。<br>  地址空间是CPU可以访问的存储器地址的范围。例如，一个32位处理器的地址空间为2^32个字节（4GB）。</p>
</li>
<li><p>架构基础：cortex-M3和M4基于相同的架构（ARMv7-M）。</p>
</li>
<li><p>Thumb-2指令集：支持16位和32位的混合指令，兼顾代码密度和性能。</p>
</li>
<li><p>3级流水线：提高指令执行效率。</p>
</li>
<li><p>NVIC：高效的中断管理。</p>
</li>
<li><p>存储器保护单元（MPU）：提供内存保护，增强系统可靠性。</p>
</li>
<li><p>调试接口：支持JTAG和SWD调式。</p>
</li>
<li><p>指令兼容性：Cortex-M3编写的二进制指令无需修改就可用在Cortex-M4上执行。</p>
</li>
</ul>
<p>主要区别：</p>
<ul>
<li>Cortex-M在Cortex-M3的基础上增加了DSP功能，是器更加适合处理信号处理的应用。</li>
<li>DSP拓展指令集：Cortex-M4包含一系列优化的DSP指令。</li>
<li>浮点单元（FPU）：Cortex-M4可以选择支撑单精度浮点运算单元，童工硬件浮点运算支持。</li>
</ul>
<h6 id="Cortex-M处理器家族"><a href="#Cortex-M处理器家族" class="headerlink" title="Cortex-M处理器家族"></a>Cortex-M处理器家族</h6><p><img src="/2025/01/20/ARM-Cortex-M3-M4/1150.png" alt="1150"></p>
<p>Cortex-M3和M4处理器基于ARMv7-M架构，既有高性能且为微控制器应用设计的。</p>
<h6 id="处理器和为控制器的区别"><a href="#处理器和为控制器的区别" class="headerlink" title="处理器和为控制器的区别"></a>处理器和为控制器的区别</h6><p>ARM公司不生产微控制器，ARM设计的处理器及多种部件都是芯片制造商所需要的，ARM会对包括微控制器供应商在内的各家芯片设备公司授权，也被称为IP授权。</p>
<p>在一个典型的微控制器设计中，处理器只会占用芯片中的一小块区域。在其他部分则成为存储器、时钟生成和分配逻辑、系统总线、外设等。</p>
<p><img src="/2025/01/20/ARM-Cortex-M3-M4/151510.png" alt="image-20250120151523458"></p>
<p>许多微控制器供应商选择ARM Cortex-M处理器作为它们的CPU，但是存储器映射系统、存储器映射、外设及操作特性（时钟频率和电压）各不相同，就构成了微控制器产品的多样性。</p>
<h5 id="1-2-C程序中的数据类型"><a href="#1-2-C程序中的数据类型" class="headerlink" title="1.2 C程序中的数据类型"></a>1.2 C程序中的数据类型</h5><p><img src="/2025/01/20/ARM-Cortex-M3-M4/154947.png" alt="image-20250120154950338"></p>
]]></content>
      <categories>
        <category>stm32学习</category>
      </categories>
      <tags>
        <tag>stm32学习</tag>
        <tag>ARM Cortex-M3</tag>
        <tag>ARM Cortex-M4</tag>
      </tags>
  </entry>
  <entry>
    <title>FreeRTOS</title>
    <url>/2025/02/19/FreeRTOS/</url>
    <content><![CDATA[<h1 id="FreeRTOS"><a href="#FreeRTOS" class="headerlink" title="FreeRTOS"></a>FreeRTOS</h1><p>以下内容是根据野火的<a href="https://weread.qq.com/web/reader/38f32720717e5b0b38f24a4ka87322c014a87ff679a21ea">FreeRTOS内核实现与应用开发实战指南</a> 以及AI搜索进行集合的记录。</p>
<h3 id="为什么学习RTOS"><a href="#为什么学习RTOS" class="headerlink" title="为什么学习RTOS"></a>为什么学习RTOS</h3><p>我们进入嵌入式这个领域开始，首先在大学时开始接触电路、汇编以及51单片机，后来是stm32，在到操作系统RTOS、linux等。stm32和51都是裸机编程。常见的RTOS有国外的FreeRTOS、μC&#x2F;OS、RTX和国内的FreeRTOS、Huawei LiteOS和AliOS-Things等，其中，开源且免费的FreeRTOS的市场占有率最高。</p>
<p>在裸机系统中，所有的程序都是自己来完成的，并且所有的操作或者实现的工程都是在一个无线的循环中的，但是现在的生活中的电子产品不在满足裸机开发，更加多的功能，更加复杂的需求，这些都需要由操作系统来实现。</p>
<span id="more"></span>

<h3 id="第一章初识FreeRTOS"><a href="#第一章初识FreeRTOS" class="headerlink" title="第一章	初识FreeRTOS"></a>第一章	初识FreeRTOS</h3><p>FreeRTOS于2018年被亚马逊收购，改名为AWS FreeRTOS，版本号升级为V10，且开源协议也由原来的GPLv2+修改为MIT，与GPLv2+相比，MIT更加开放，你完全可以理解为完全免费。V9以前的版本还是维持原样，V10版本相比于V9就是加入了一些物联网相关的组件，内核基本不变。亚马逊收购FreeRTOS也是为了进军物联网和人工智能领域。本书还是以V9版本来讲解。</p>
<h3 id="第二章新建FreeRTOS工程"><a href="#第二章新建FreeRTOS工程" class="headerlink" title="第二章	新建FreeRTOS工程"></a>第二章	新建FreeRTOS工程</h3><h4 id="1、新建本地工程文件"><a href="#1、新建本地工程文件" class="headerlink" title="1、新建本地工程文件"></a>1、新建本地工程文件</h4><p>在开始创建工程之前，我们现在本地中新建一个文件夹用于存放工程，在该文件夹中新建各个文件夹和文件，下面是文件夹的包含关系和具体的作用。</p>
<p><img src="/2025/02/19/FreeRTOS/151023.png" alt="工程文件夹根目录下的文件夹及其作用"> </p>
<h4 id="2、使用keil创建工程"><a href="#2、使用keil创建工程" class="headerlink" title="2、使用keil创建工程"></a>2、使用keil创建工程</h4><h4 id="3、在keil工程中新建文件组"><a href="#3、在keil工程中新建文件组" class="headerlink" title="3、在keil工程中新建文件组"></a>3、在keil工程中新建文件组</h4><p>在工程中添加user、FreeRTOS&#x2F;Posrt、FreeRTOS&#x2F;source、doc这个几个文件组。</p>
<h4 id="4、在keil工程中添加文件"><a href="#4、在keil工程中添加文件" class="headerlink" title="4、在keil工程中添加文件"></a>4、在keil工程中添加文件</h4><p>在工程中添加组后，需要将本地工程中新建的文件添加到工程中。</p>
<h3 id="第三章裸机系统与多任务系统"><a href="#第三章裸机系统与多任务系统" class="headerlink" title="第三章	裸机系统与多任务系统"></a>第三章	裸机系统与多任务系统</h3><h4 id="1、裸机系统"><a href="#1、裸机系统" class="headerlink" title="1、裸机系统"></a>1、裸机系统</h4><p>裸机系统通常分为轮询系统和前后台系统。</p>
<h5 id="轮询系统"><a href="#轮询系统" class="headerlink" title="轮询系统"></a>轮询系统</h5><p>轮询系统就是在裸机编程中，先编写硬件的相关初始化，然后在mian主程序中的while死循环中不断的循环，依次的实现功能。例如LED灯反转、串口输出等，只是按照顺序执行的功能；但是要是实现按键等操作，就需要检测外部信号，这时在用轮询系统，就会实时性大大降低。</p>
<h5 id="前后台系统"><a href="#前后台系统" class="headerlink" title="前后台系统"></a>前后台系统</h5><p>相对于轮询系统，前后台系统就是在其基础上加入中断，前台依然按照顺序执行，但是当后台由中断产生时，前台就要停止手上的任务，执行后台的任务，只有当中断任务执行完成后，再返回执行刚才放下的前台任务。</p>
<h4 id="2、多任务系统"><a href="#2、多任务系统" class="headerlink" title="2、多任务系统"></a>2、多任务系统</h4><p>相对于轮询系统、前后台系统，多任务系统优化的是前台。再前台按顺序执行的基础上，我们增加一个优先级功能。在多任务系统中，任务和中断都具有优先级，但是普遍中断的优先级都比任务的优先级高。所以当中断信号产生时，还是优先执行中断。而前台执行的任务那，就也有紧急的和不紧急的区别，高优先级的比低优先的任务优先执行。</p>
<p>除了优先级还有优化了循环的问题。在前后台系统中，依然是在一个大循环中执行，而多任务系统中，我们可以根据程序的功能，把程序主体分为一个k一个独立的、互不干扰的的小任务。每个任务都具备优先级，由操作系统调度管理。</p>
<p><img src="/2025/02/19/FreeRTOS/153950.png" alt="轮询、前后台、多任务系统的区别"></p>
<h3 id="第四章数据结构–列表和列表项"><a href="#第四章数据结构–列表和列表项" class="headerlink" title="第四章	数据结构–列表和列表项"></a>第四章	数据结构–列表和列表项</h3>]]></content>
      <categories>
        <category>FreeRTOS</category>
      </categories>
      <tags>
        <tag>stm32学习</tag>
        <tag>操作系统</tag>
        <tag>FreeRTOS</tag>
      </tags>
  </entry>
  <entry>
    <title>FSMC—扩展外部SRAM</title>
    <url>/2025/03/03/FSMC%E2%80%94%E6%89%A9%E5%B1%95%E5%A4%96%E9%83%A8SRAM/</url>
    <content><![CDATA[<h1 id="FSMC—扩展外部SRAM"><a href="#FSMC—扩展外部SRAM" class="headerlink" title="FSMC—扩展外部SRAM"></a>FSMC—扩展外部SRAM</h1><p>STM32F407ZGT6 自带了 192K 字节的 SRAM，对一般应用来说，已经足够了，不过在一 些对内存要求高的场合，STM32F4 自带的这些内存就不够用了。比如跑算法或者跑 GUI 等， 就可能不太够用，所以探索者 STM32F4 开发板板载了一颗 1M 字节容量的 SRAM 芯片： XM8A51216。</p>
<span id="more"></span>

<h3 id="1、SRAM控制原理"><a href="#1、SRAM控制原理" class="headerlink" title="1、SRAM控制原理"></a>1、SRAM控制原理</h3><p>拓展内存时一般选择SRAM和SDRAM两种，但是stm32F407不支持拓展SDRAM（stm32F429系列支持），芯片使用FSMC外设拓展SRAM。</p>
<p>SRAM（静态随机存储器）和SDRAM（同步动态随机存储器）。</p>
<ul>
<li>SRAM：静态存储器，使用触发器来存储数据。只要保持通电，数据就不会丢失，无需刷新。</li>
<li>SDRAM：动态随机存储器。使用电容器存储数据，与系统时钟同步操作，需要定期的刷新以保持数据。</li>
<li>SRAM：使用六个晶体管称为一个存储单元，通过触发器保持状态。</li>
<li>SDRAM：每个存储由一个电容器和一个晶体管组成，依靠电容器存储的电荷来存储数据，需要定期充电保持数据。</li>
</ul>
<h4 id="XM8A51216简介"><a href="#XM8A51216简介" class="headerlink" title="XM8A51216简介"></a>XM8A51216简介</h4><p>在我们使用的是XM8A51216的SRAM芯片内部结构框图。以他为模型学习。</p>
<ul>
<li>国产SRAM芯片具有最高10&#x2F;12ns的访问速度。</li>
<li>具备低功耗</li>
<li>TTL电容兼容<ul>
<li>TTL：晶体管-晶体管逻辑。一种逻辑电平标准。<br>  TTL定义逻辑高和逻辑低电平的范围。高电平是2.4-5V，低电平是0-0.8V。</li>
</ul>
</li>
<li>三态输出。<ul>
<li>三态表示：高电平、低电平、高阻抗（既不输出高、也不输出低）。</li>
<li>作用：当多个设备连接一天总线时，通过控制三态输出端使能端，当一个设备输出时，其他设备设置高阻抗，不干扰总线数据传输。</li>
<li>这允许多个存储器芯片或其他设备共享同一组数据线，从而减少了电路板上的布线数量。</li>
</ul>
</li>
<li>全静态操作，不需要刷新</li>
<li>字节控制功能，可以读取16位、8位数据。</li>
</ul>
<p><img src="/2025/03/03/FSMC%E2%80%94%E6%89%A9%E5%B1%95%E5%A4%96%E9%83%A8SRAM/092613.png"></p>
<p><img src="/2025/03/03/FSMC%E2%80%94%E6%89%A9%E5%B1%95%E5%A4%96%E9%83%A8SRAM/092731.png"></p>
<h5 id="1、SRAM信号线"><a href="#1、SRAM信号线" class="headerlink" title="1、SRAM信号线"></a>1、SRAM信号线</h5><p>第一张图是芯片内部结构框图，其说明表如下图。</p>
<p><img src="/2025/03/03/FSMC%E2%80%94%E6%89%A9%E5%B1%95%E5%A4%96%E9%83%A8SRAM/092950.png"></p>
<p>SRAM的控制比较简单，只要使能控制信号，从地址从线输入要访问的地址，就可以读写数据。</p>
<h5 id="2、存储矩阵"><a href="#2、存储矩阵" class="headerlink" title="2、存储矩阵"></a>2、存储矩阵</h5><p>框图中标记号表示存储矩阵，这个SRAM芯片的内部空间大小为512*16（bits），即1M字节容量16位宽的静态内存芯片。</p>
<p><img src="/2025/03/03/FSMC%E2%80%94%E6%89%A9%E5%B1%95%E5%A4%96%E9%83%A8SRAM/093304.png"></p>
<p>SRAM内部包含的存储阵列，可以把它理解成一张表格，数据就填在这张表格上。和表格查找一样，指定一个行地址和列地址，就可以精确地找到目标单元格， 这是SRAM芯片寻址的基本原理。这样的每个单元格被称为存储单元，而这样的表则被称为存储矩阵。</p>
<h5 id="3、地址编译器、列I-o及I-O数据电路"><a href="#3、地址编译器、列I-o及I-O数据电路" class="headerlink" title="3、地址编译器、列I&#x2F;o及I&#x2F;O数据电路"></a>3、地址编译器、列I&#x2F;o及I&#x2F;O数据电路</h5><p>地址编译器把N条地址线转换为2N根信号线，每根地址线对应着一列或着一行存储单元，通过地址线找到具体的存储单元。如果存储矩阵比较大，地址会分为列地址线、行地址线，或者行、列地址线会复用一条地址线，访问数据寻址时会先用地址线传输行地址再传输列地址。</p>
<p>我们这次使用的SRAM比较小，没有列地址线，只有行地址线。再图中我们看到数据线为0-15，传输16位数据，则就是2字节。地址线是0-18，经过转换就是2^18&#x3D;2^8x1024&#x3D;512K行存储单元。所以总共是512*16bits空间大小。</p>
<p>例如，当要访问宽度为16位的数据时，使用行地址线指出地址，然后把UB#和LB#线都设置为低电平， 那么I&#x2F;O0-I&#x2F;O15线都有效，它们一起输出该地址的16位数据(或者接收16位数据到该地址)；当要访问宽度为8位的数据时，使用行地址线指出地址， 然后把UB#或LB#其中一个设置为低电平，I&#x2F;O会对应输出该地址的高8位和低8位数据，因此它们被称为数据掩码信号。</p>
<h5 id="4、控制电路"><a href="#4、控制电路" class="headerlink" title="4、控制电路"></a>4、控制电路</h5><p>控制电路包括了片选、读写使能、上面提到的数据宽度控制信号UB、LB。</p>
<p>利用片选信号可以把多个SRAM组成一个大容量的内存条。</p>
<p>OE、WE可以控制读写使能，防止误操作。</p>
<h5 id="5、SRAM的读写流程"><a href="#5、SRAM的读写流程" class="headerlink" title="5、SRAM的读写流程"></a>5、SRAM的读写流程</h5><p>对SRAM进行读写数据时，它各个信号线的时序流程图如图。</p>
<p><img src="/2025/03/03/FSMC%E2%80%94%E6%89%A9%E5%B1%95%E5%A4%96%E9%83%A8SRAM/094859.png"></p>
<p>读写时序的流程：</p>
<ul>
<li>主机使能地址信号线，发送要访问的存储器地址。</li>
<li>控制片选信号CS1、CS2，使能存储芯片</li>
<li>若进行读操作，则控制读使能信号OE；若进行写操作，则控制写使能信号WE。</li>
<li>使用掩码LB与UB指示访问目标的高、低字节。</li>
<li>若读取过程中，存储去会通过数据线像主机输出目标数据；若写入过程，主要使用数据像存储器传输木目标。</li>
</ul>
<h3 id="2、FSMC简介"><a href="#2、FSMC简介" class="headerlink" title="2、FSMC简介"></a>2、FSMC简介</h3><p>STM32F407系列芯片使用FSMC外设来管理扩展的存储器， FSMC是Flexible Static Memory Controller的缩写，译为灵活的静态存储控制器。 它可以用于驱动包括SRAM、NOR FLASH以及NANDFLSAH类型的存储器， 不能驱动如SDRAM这种动态的存储器而在STM32F429系列的控制器中， 它具有FMC外设，支持控制SDRAM存储器。</p>
<p> FSMC 具有四个芯片选择输出（NE1-NE4），允许 STM32F407 与最多四个外部存储设备接口。FSMC 为外部存储器提供了一个专用的地址空间，通常从 0x60000000 开始，</p>
<p>FSMC 的内存映射特性简化了对外部存储器的访问，因为可以像访问内部存储器位置一样，使用微控制器的标准加载和存储指令来处理它</p>
<h4 id="FSMC框图解析"><a href="#FSMC框图解析" class="headerlink" title="FSMC框图解析"></a>FSMC框图解析</h4><p><img src="/2025/03/03/FSMC%E2%80%94%E6%89%A9%E5%B1%95%E5%A4%96%E9%83%A8SRAM/095536.png"></p>
<h5 id="1、通讯引脚"><a href="#1、通讯引脚" class="headerlink" title="1、通讯引脚"></a>1、通讯引脚</h5><p>在框图的右侧是FSMC外设相关的控制引脚，由于控制不同类型存储器的时候会有一些不同的引脚，看起来有非常多，其中地址线FSMC_A和数据线FSMC_D是所有控制器都共用的。 这些FSMC引脚具体对应的GPIO端口及引脚号可在《STM32F4xx规格书》中搜索查找到，不在此列出。针对本示例中的SRAM控制 器，我们整理出以下的FSMC与SRAM引脚对照表 <a href="https://doc.embedfire.com/mcu/stm32/f407batianhu/std/zh/latest/book/FSMC.html#id14">FSMC中的SRAM控制信号线</a>。</p>
<p><img src="/2025/03/03/FSMC%E2%80%94%E6%89%A9%E5%B1%95%E5%A4%96%E9%83%A8SRAM/095737.png"></p>
<p>其中比较特殊的FSMC_NE是用于控制SRAM芯片的片选控制信号线，STM32具有FSMC_NE1&#x2F;2&#x2F;3&#x2F;4号引脚，不同的引脚对应STM32内部不同的地址区域。 例如，当STM32访问0x6C000000-0x6FFFFFFF地址空间时，FSMC_NE3引脚会自动设置为低电平，由于它连接到SRAM的CE#引脚， 所以SRAM的片选被使能，而访问0x60000000-0x63FFFFFF地址时，FSMC_NE1会输出低电平。当使用不同的FSMC_NE引脚连接外部存储器时， STM32访问SRAM的地址不一样，从而达到控制多块SRAM芯片的目的。各引脚对应的地址会在后面“FSMC的地址映射”小节讲解。</p>
<h5 id="2、存储器控制器"><a href="#2、存储器控制器" class="headerlink" title="2、存储器控制器"></a>2、存储器控制器</h5><p>上面不同类型的引脚是连接到FSMC内部对应的存储控制器中的。NOR&#x2F;PSRAM&#x2F;SRAM设备使用相同的控制器，NAND&#x2F;PC卡设备使用相同的控制器，不同的控制器有专用的寄存器用于配置其工作模式。</p>
<p>控制SRAM的有FSMC_BCR1&#x2F;2&#x2F;3&#x2F;4控制寄存器、FSMC_BTR1&#x2F;2&#x2F;3&#x2F;4片选时序寄存器以及FSMC_BWTR1&#x2F;2&#x2F;3&#x2F;4写时序寄存器。每种寄存器都有4个，分别对应于4个不同的存储区域，各种寄存器介绍如下：</p>
<ul>
<li><p>FSMC_BCR控制寄存器可配置要控制的存储器类型、数据线宽度以及信号有效极性能参数。</p>
</li>
<li><p>FMC_BTR时序寄存器用于配置SRAM访问时的各种时间延迟，如数据保持时间、地址保持时间等。</p>
</li>
<li><p>FMC_BWTR写时序寄存器与FMC_BTR寄存器控制的参数类似，它专门用于控制写时序的时间参数。</p>
</li>
</ul>
<h5 id="3、时钟控制逻辑"><a href="#3、时钟控制逻辑" class="headerlink" title="3、时钟控制逻辑"></a>3、时钟控制逻辑</h5><p>FSMC外设挂载在AHB总线上，时钟信号来自于HCLK(默认168MHz)，控制器的同步时钟输出就是由它分频得到。例如，NOR控制器的FSMC_CLK引脚输出的时钟，它可用于与同步类型的SRAM芯片进行同步通讯，它的时钟频率可通过FSMC_BTR寄存器的CLKDIV位配置，可以配置为HCLK的1&#x2F;2或 1&#x2F;3，也就是说，若它与同步类型的SRAM通讯时，同步时钟最高频率为84MHz。本示例中的SRAM为异步类型的存储器，不使用同步时钟信号，所以时钟分频配置不起作用。</p>
<h5 id="4、FSMC地址映射"><a href="#4、FSMC地址映射" class="headerlink" title="4、FSMC地址映射"></a>4、FSMC地址映射</h5><p>FSMC连接好外部的存储器并初始化后，就可以直接通过访问地址来读写数据，这种地址访问与I2C EEPROM、SPI FLASH的不一样， 后两种方式都需要控制I2C或SPI总线给存储器发送地址，然后获取数据；在程序里，这个地址和数据都需要分开使用不同的变量存储， 并且访问时还需要使用代码控制发送读写命令。而使用FSMC外接存储器时，其存储单元是映射到STM32的内部寻址空间的；在程序里， 定义一个指向这些地址的指针，然后就可以通过指针直接修改该存储单元的内容，FSMC外设会自动完成数据访问过程， 读写命令之类的操作不需要程序控制。FSMC的地址映射见图 <a href="https://doc.embedfire.com/mcu/stm32/f407batianhu/std/zh/latest/book/FSMC.html#id18">FSMC的地址映射</a>。</p>
<p><img src="/2025/03/03/FSMC%E2%80%94%E6%89%A9%E5%B1%95%E5%A4%96%E9%83%A8SRAM/100215.png"></p>
<p>图中左侧的是Cortex-M4内核的存储空间分配，右侧是STM32 FSMC外设的地址映射。 可以看到FSMC的NOR&#x2F;PSRAM&#x2F;SRAM&#x2F;NAND FLASH以及PC卡的地址都在ExternalRAM地址空间内。 正是因为存在这样的地址映射，使得访问FSMC控制的存储器时，就跟访问STM32的片上外设寄存器一样(片上外设的地址映射即图中左侧的“Peripheral”区域)。</p>
<p>FSMC把整个External RAM存储区域分成了4个Bank区域，并分配了地址范围及适用的存储器类型， 如NOR及SRAM存储器只能使用Bank1的地址。在每个Bank的内部又分成了4个小块， 每个小块有相应的控制引脚用于连接片选信号，如FSMC_NE[4:1]信号线可用于选择BANK1内部的4小块地址区域， 见图 <a href="https://doc.embedfire.com/mcu/stm32/f407batianhu/std/zh/latest/book/FSMC.html#bank1">Bank1内部的小块地址分配</a> ， 当STM32访问0x6C000000-0x6FFFFFFF地址空间时，会访问到Bank1的第4小块区域， 相应的FSMC_NE3信号线会输出控制信号。</p>
<p><img src="/2025/03/03/FSMC%E2%80%94%E6%89%A9%E5%B1%95%E5%A4%96%E9%83%A8SRAM/105408.png"></p>
<h5 id="5、FSMC控制SRAM的时序"><a href="#5、FSMC控制SRAM的时序" class="headerlink" title="5、FSMC控制SRAM的时序"></a>5、FSMC控制SRAM的时序</h5><p>FSMC外设支持输出多种不同的时序以便于控制不同的存储器，它具有ABCD四种模式， 下面我们仅针对控制SRAM使用的模式A进行讲解，见图 <a href="https://doc.embedfire.com/mcu/stm32/f407batianhu/std/zh/latest/book/FSMC.html#fsmca">FSMC模式A的读时序</a> 及图 <a href="https://doc.embedfire.com/mcu/stm32/f407batianhu/std/zh/latest/book/FSMC.html#id20">FSMC模式A的写时序</a>。</p>
<p><img src="/2025/03/03/FSMC%E2%80%94%E6%89%A9%E5%B1%95%E5%A4%96%E9%83%A8SRAM/111455.png"></p>
<p><img src="/2025/03/03/FSMC%E2%80%94%E6%89%A9%E5%B1%95%E5%A4%96%E9%83%A8SRAM/111509.png"></p>
<p>当内核发出访问某个指向外部存储器地址时，FSMC外设会根据配置控制信号线产生时序访问存储器，上图中的是访问外部SRAM时FSMC外设的读写时序。</p>
<p>以读时序为例，该图表示一个存储器操作周期由地址建立周期(ADDSET)、数据建立周期(DATAST)以及2个HCLK周期组成。</p>
<ul>
<li>在地址建立周期中，地址线发出要访问的地址， </li>
<li>数据掩码信号线指示出要读取地址的高、低字节部分，</li>
<li>片选信号使能存储器芯片；</li>
<li>地址建立周期结束后读使能信号线发出读使能信号， </li>
<li>接着存储器通过数据信号线把目标数据传输给FSMC，FSMC把它交给内核。</li>
</ul>
<p>写时序类似，区别是它的一个存储器操作周期仅由地址建立周期(ADDSET)和数据建立周期(DATAST)组成， 且在数据建立周期期间写使能信号线发出写信号，接着FSMC把数据通过数据线传输到存储器中。</p>
<h5 id="6、代码"><a href="#6、代码" class="headerlink" title="6、代码"></a>6、代码</h5><ul>
<li>FSMC初始化</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void FSMC_SRAM_Init(void)</span><br><span class="line">&#123;</span><br><span class="line">	GPIO_InitTypeDef  				GPIO_InitStructure;</span><br><span class="line">	FSMC_NORSRAMInitTypeDef  		FSMC_NORSRAMInitStructure;</span><br><span class="line">	FSMC_NORSRAMTimingInitTypeDef  	readWriteTiming; </span><br><span class="line">	</span><br><span class="line">	XmRamInit();                //初始化XMRAM 20191024</span><br><span class="line">	delay_us(100);</span><br><span class="line"></span><br><span class="line">	RCC_AHB1PeriphClockCmd(RCC_AHB1Periph_GPIOB|RCC_AHB1Periph_GPIOD|RCC_AHB1Periph_GPIOE|RCC_AHB1Periph_GPIOF|RCC_AHB1Periph_GPIOG, ENABLE);//使能PD,PE,PF,PG时钟  </span><br><span class="line">	RCC_AHB3PeriphClockCmd(RCC_AHB3Periph_FSMC,ENABLE);//使能FSMC时钟  </span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	GPIO_InitStructure.GPIO_Pin = GPIO_Pin_15;//PB15 推挽输出,控制背光</span><br><span class="line">	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_OUT;//普通输出模式</span><br><span class="line">	GPIO_InitStructure.GPIO_OType = GPIO_OType_PP;//推挽输出</span><br><span class="line">	GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;//100MHz</span><br><span class="line">	GPIO_InitStructure.GPIO_PuPd = GPIO_PuPd_UP;//上拉</span><br><span class="line">	GPIO_Init(GPIOB, &amp;GPIO_InitStructure);//初始化 //PB15 推挽输出,控制背光</span><br><span class="line"></span><br><span class="line">	GPIO_InitStructure.GPIO_Pin = (3&lt;&lt;0)|(3&lt;&lt;4)|(0XFF&lt;&lt;8);//PD0,1,4,5,8~15 AF OUT</span><br><span class="line">	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_AF;//复用输出</span><br><span class="line">	GPIO_InitStructure.GPIO_OType = GPIO_OType_PP;//推挽输出</span><br><span class="line">	GPIO_InitStructure.GPIO_Speed = GPIO_Speed_100MHz;//100MHz</span><br><span class="line">	GPIO_InitStructure.GPIO_PuPd = GPIO_PuPd_UP;//上拉</span><br><span class="line">	GPIO_Init(GPIOD, &amp;GPIO_InitStructure);//初始化  </span><br><span class="line"></span><br><span class="line">	GPIO_InitStructure.GPIO_Pin = (3&lt;&lt;0)|(0X1FF&lt;&lt;7);//PE0,1,7~15,AF OUT</span><br><span class="line">	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_AF;//复用输出</span><br><span class="line">	GPIO_InitStructure.GPIO_OType = GPIO_OType_PP;//推挽输出</span><br><span class="line">	GPIO_InitStructure.GPIO_Speed = GPIO_Speed_100MHz;//100MHz</span><br><span class="line">	GPIO_InitStructure.GPIO_PuPd = GPIO_PuPd_UP;//上拉</span><br><span class="line">	GPIO_Init(GPIOE, &amp;GPIO_InitStructure);//初始化  </span><br><span class="line"></span><br><span class="line">	GPIO_InitStructure.GPIO_Pin = (0X3F&lt;&lt;0)|(0XF&lt;&lt;12); 	//PF0~5,12~15</span><br><span class="line">	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_AF;//复用输出</span><br><span class="line">	GPIO_InitStructure.GPIO_OType = GPIO_OType_PP;//推挽输出</span><br><span class="line">	GPIO_InitStructure.GPIO_Speed = GPIO_Speed_100MHz;//100MHz</span><br><span class="line">	GPIO_InitStructure.GPIO_PuPd = GPIO_PuPd_UP;//上拉</span><br><span class="line">	GPIO_Init(GPIOF, &amp;GPIO_InitStructure);//初始化  </span><br><span class="line"></span><br><span class="line">	GPIO_InitStructure.GPIO_Pin =(0X3F&lt;&lt;0)| GPIO_Pin_10;//PG0~5,10</span><br><span class="line">	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_AF;//复用输出</span><br><span class="line">	GPIO_InitStructure.GPIO_OType = GPIO_OType_PP;//推挽输出</span><br><span class="line">	GPIO_InitStructure.GPIO_Speed = GPIO_Speed_100MHz;//100MHz</span><br><span class="line">	GPIO_InitStructure.GPIO_PuPd = GPIO_PuPd_UP;//上拉</span><br><span class="line">	GPIO_Init(GPIOG, &amp;GPIO_InitStructure);//初始化 </span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	GPIO_PinAFConfig(GPIOD,GPIO_PinSource0,GPIO_AF_FSMC);//PD0,AF12</span><br><span class="line">	GPIO_PinAFConfig(GPIOD,GPIO_PinSource1,GPIO_AF_FSMC);//PD1,AF12</span><br><span class="line">	GPIO_PinAFConfig(GPIOD,GPIO_PinSource4,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOD,GPIO_PinSource5,GPIO_AF_FSMC); </span><br><span class="line">	GPIO_PinAFConfig(GPIOD,GPIO_PinSource8,GPIO_AF_FSMC); </span><br><span class="line">	GPIO_PinAFConfig(GPIOD,GPIO_PinSource9,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOD,GPIO_PinSource10,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOD,GPIO_PinSource11,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOD,GPIO_PinSource12,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOD,GPIO_PinSource13,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOD,GPIO_PinSource14,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOD,GPIO_PinSource15,GPIO_AF_FSMC);//PD15,AF12</span><br><span class="line"></span><br><span class="line">	GPIO_PinAFConfig(GPIOE,GPIO_PinSource0,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOE,GPIO_PinSource1,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOE,GPIO_PinSource7,GPIO_AF_FSMC);//PE7,AF12</span><br><span class="line">	GPIO_PinAFConfig(GPIOE,GPIO_PinSource8,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOE,GPIO_PinSource9,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOE,GPIO_PinSource10,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOE,GPIO_PinSource11,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOE,GPIO_PinSource12,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOE,GPIO_PinSource13,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOE,GPIO_PinSource14,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOE,GPIO_PinSource15,GPIO_AF_FSMC);//PE15,AF12</span><br><span class="line"></span><br><span class="line">	GPIO_PinAFConfig(GPIOF,GPIO_PinSource0,GPIO_AF_FSMC);//PF0,AF12</span><br><span class="line">	GPIO_PinAFConfig(GPIOF,GPIO_PinSource1,GPIO_AF_FSMC);//PF1,AF12</span><br><span class="line">	GPIO_PinAFConfig(GPIOF,GPIO_PinSource2,GPIO_AF_FSMC);//PF2,AF12</span><br><span class="line">	GPIO_PinAFConfig(GPIOF,GPIO_PinSource3,GPIO_AF_FSMC);//PF3,AF12</span><br><span class="line">	GPIO_PinAFConfig(GPIOF,GPIO_PinSource4,GPIO_AF_FSMC);//PF4,AF12</span><br><span class="line">	GPIO_PinAFConfig(GPIOF,GPIO_PinSource5,GPIO_AF_FSMC);//PF5,AF12</span><br><span class="line">	GPIO_PinAFConfig(GPIOF,GPIO_PinSource12,GPIO_AF_FSMC);//PF12,AF12</span><br><span class="line">	GPIO_PinAFConfig(GPIOF,GPIO_PinSource13,GPIO_AF_FSMC);//PF13,AF12</span><br><span class="line">	GPIO_PinAFConfig(GPIOF,GPIO_PinSource14,GPIO_AF_FSMC);//PF14,AF12</span><br><span class="line">	GPIO_PinAFConfig(GPIOF,GPIO_PinSource15,GPIO_AF_FSMC);//PF15,AF12</span><br><span class="line"></span><br><span class="line">	GPIO_PinAFConfig(GPIOG,GPIO_PinSource0,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOG,GPIO_PinSource1,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOG,GPIO_PinSource2,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOG,GPIO_PinSource3,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOG,GPIO_PinSource4,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOG,GPIO_PinSource5,GPIO_AF_FSMC);</span><br><span class="line">	GPIO_PinAFConfig(GPIOG,GPIO_PinSource10,GPIO_AF_FSMC);</span><br><span class="line">	</span><br><span class="line"> 	  </span><br><span class="line"> 	readWriteTiming.FSMC_AddressSetupTime = 0;	 			//地址建立时间（ADDSET）为1个HCLK 1/36M=27ns </span><br><span class="line">	readWriteTiming.FSMC_AddressHoldTime = 0;				//地址保持时间（ADDHLD）模式A未用到	</span><br><span class="line">	readWriteTiming.FSMC_DataSetupTime = 12;		 		//数据保持时间（DATAST）为13个HCLK 6*13=117ns		 	 </span><br><span class="line">	readWriteTiming.FSMC_BusTurnAroundDuration = 0x00;</span><br><span class="line">	readWriteTiming.FSMC_CLKDivision = 0x00;</span><br><span class="line">	readWriteTiming.FSMC_DataLatency = 0x00;</span><br><span class="line">	readWriteTiming.FSMC_AccessMode = FSMC_AccessMode_A;	 //模式A </span><br><span class="line">	</span><br><span class="line">	</span><br><span class="line"></span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_Bank = FSMC_Bank1_NORSRAM3;//  这里我们使用NE3 ，也就对应BTCR[4],[5]。</span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_DataAddressMux = FSMC_DataAddressMux_Disable; </span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_MemoryType =FSMC_MemoryType_SRAM;// FSMC_MemoryType_SRAM;  //SRAM   </span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_MemoryDataWidth = FSMC_MemoryDataWidth_16b;//存储器数据宽度为16bit  </span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_BurstAccessMode =FSMC_BurstAccessMode_Disable;// FSMC_BurstAccessMode_Disable; </span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_WaitSignalPolarity = FSMC_WaitSignalPolarity_Low;</span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_AsynchronousWait=FSMC_AsynchronousWait_Disable;</span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_WrapMode = FSMC_WrapMode_Disable;   </span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_WaitSignalActive = FSMC_WaitSignalActive_BeforeWaitState;  </span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_WriteOperation = FSMC_WriteOperation_Enable;	//存储器写使能 </span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_WaitSignal = FSMC_WaitSignal_Disable;  </span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_ExtendedMode = FSMC_ExtendedMode_Disable; // 读写使用相同的时序</span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_WriteBurst = FSMC_WriteBurst_Disable;  </span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_ReadWriteTimingStruct = &amp;readWriteTiming;</span><br><span class="line">	FSMC_NORSRAMInitStructure.FSMC_WriteTimingStruct = &amp;readWriteTiming; //读写同样时序</span><br><span class="line"></span><br><span class="line">	FSMC_NORSRAMInit(&amp;FSMC_NORSRAMInitStructure);  //初始化FSMC配置</span><br><span class="line"></span><br><span class="line"> 	FSMC_NORSRAMCmd(FSMC_Bank1_NORSRAM3, ENABLE);  // 使能BANK1区域3	</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>GPIO初始化：配置了多个 GPIO 引脚的工作模式、输出类型、速度和上下拉状态。这些引脚将用作 SRAM 的地址线、数据线和控制线。</p>
</li>
<li><p>GPIO复用功能配置：将配置好的 GPIO 引脚连接到 FSMC 外设的相应功能。</p>
</li>
<li><pre><code>  readWriteTiming.FSMC_AddressSetupTime = 0;                 //地址建立时间（ADDSET）为1个HCLK 1/36M=27ns
      readWriteTiming.FSMC_AddressHoldTime = 0;                //地址保持时间（ADDHLD）模式A未用到
      readWriteTiming.FSMC_DataSetupTime = 12;                 //数据保持时间（DATAST）为13个HCLK 6*13=117ns
      readWriteTiming.FSMC_BusTurnAroundDuration = 0x00;
      readWriteTiming.FSMC_CLKDivision = 0x00;
      readWriteTiming.FSMC_DataLatency = 0x00;
      readWriteTiming.FSMC_AccessMode = FSMC_AccessMode_A;     //模式A
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">    * 配置FSMC的读写时序。定义了在惊醒SRAM读写时，地址线和数据线和控制信号之间的时序。</span><br><span class="line">    * FSMC_AddressSetupTime = 0;</span><br><span class="line">        * 设置地址建立时间为0个HCLK周期。</span><br><span class="line">        * 地址建立时间是指在使能控制信号之前，地址信号必须保持稳定。</span><br><span class="line">    * `FSMC_AddressHoldTime = 0;`:</span><br><span class="line">        * 设置地址保持时间为0个HCLK周期。</span><br><span class="line">        * 地址保持时间是指在禁用控制信号之后，地址信号必须保持稳定的时间。</span><br><span class="line">    * FSMC_DataSetupTime = 12;</span><br><span class="line">        * 设置数据建立时间 (DATAST) 为 12 个 HCLK 周期。</span><br><span class="line">        * 数据建立时间是指在读取操作期间，数据信号在被微控制器采样之前必须保持稳定的时间。</span><br><span class="line">    * `FSMC_BusTurnAroundDuration = 0x00;`: 设置总线周转时间，在非复用总线模式下通常设置为 0。</span><br><span class="line">    * `FSMC_CLKDivision = 0x00;`: 设置时钟分频因子，在异步模式下通常设置为 0。</span><br><span class="line">    * `FSMC_DataLatency = 0x00;`: 设置数据延迟，在异步模式下通常设置为 0。</span><br><span class="line">    * `FSMC_AccessMode = FSMC_AccessMode_A;`: 选择 FSMC 的访问模式为模式 A。不同的访问模式定义了不同的时序行为。</span><br><span class="line"></span><br><span class="line">* ```</span><br><span class="line">    FSMC_NORSRAMInitStructure.FSMC_Bank = FSMC_Bank1_NORSRAM3;//  这里我们使用NE3 ，也就对应BTCR[4],[5]。</span><br><span class="line">    	FSMC_NORSRAMInitStructure.FSMC_DataAddressMux = FSMC_DataAddressMux_Disable;</span><br><span class="line">    	FSMC_NORSRAMInitStructure.FSMC_MemoryType =FSMC_MemoryType_SRAM;// FSMC_MemoryType_SRAM;  //SRAM</span><br><span class="line">    	FSMC_NORSRAMInitStructure.FSMC_MemoryDataWidth = FSMC_MemoryDataWidth_16b;//存储器数据宽度为16bit</span><br><span class="line">    	FSMC_NORSRAMInitStructure.FSMC_BurstAccessMode =FSMC_BurstAccessMode_Disable;// FSMC_BurstAccessMode_Disable;</span><br><span class="line">    	FSMC_NORSRAMInitStructure.FSMC_WaitSignalPolarity = FSMC_WaitSignalPolarity_Low;</span><br><span class="line">    	FSMC_NORSRAMInitStructure.FSMC_AsynchronousWait=FSMC_AsynchronousWait_Disable;</span><br><span class="line">    	FSMC_NORSRAMInitStructure.FSMC_WrapMode = FSMC_WrapMode_Disable;</span><br><span class="line">    	FSMC_NORSRAMInitStructure.FSMC_WaitSignalActive = FSMC_WaitSignalActive_BeforeWaitState;</span><br><span class="line">    	FSMC_NORSRAMInitStructure.FSMC_WriteOperation = FSMC_WriteOperation_Enable;	//存储器写使能</span><br><span class="line">    	FSMC_NORSRAMInitStructure.FSMC_WaitSignal = FSMC_WaitSignal_Disable;</span><br><span class="line">    	FSMC_NORSRAMInitStructure.FSMC_ExtendedMode = FSMC_ExtendedMode_Disable; // 读写使用相同的时序</span><br><span class="line">    	FSMC_NORSRAMInitStructure.FSMC_WriteBurst = FSMC_WriteBurst_Disable;</span><br><span class="line">    	FSMC_NORSRAMInitStructure.FSMC_ReadWriteTimingStruct = &amp;readWriteTiming;</span><br><span class="line">    	FSMC_NORSRAMInitStructure.FSMC_WriteTimingStruct = &amp;readWriteTiming; //读写同样时序</span><br><span class="line">    </span><br><span class="line">    	FSMC_NORSRAMInit(&amp;FSMC_NORSRAMInitStructure);  //初始化FSMC配置</span><br><span class="line">    </span><br><span class="line">     	FSMC_NORSRAMCmd(FSMC_Bank1_NORSRAM3, ENABLE);  // 使能BANK1区域3</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

  * 选择 FSMC Bank 1 的第三个 NOR/SRAM 区域 (NE3)。

  * `FSMC_DataAddressMux = FSMC_DataAddressMux_Disable;`: 禁用数据线和地址线的复用模式，因为 SRAM 通常使用独立的地址线和数据线。

  * `FSMC_MemoryType = FSMC_MemoryType_SRAM;`: 设置存储器类型为 SRAM。

  * `FSMC_MemoryDataWidth = FSMC_MemoryDataWidth_16b;`: 设置存储器的数据宽度为 16 位

  * `FSMC_BurstAccessMode = FSMC_BurstAccessMode_Disable;`: 禁用突发访问模式。

      `FSMC_WaitSignalPolarity = FSMC_WaitSignalPolarity_Low;`: 设置等待信号的极性（如果使用）。

      `FSMC_AsynchronousWait = FSMC_AsynchronousWait_Disable;`: 禁用异步等待模式。

      `FSMC_WrapMode = FSMC_WrapMode_Disable;`: 禁用回绕模式。

      `FSMC_WaitSignalActive = FSMC_WaitSignalActive_BeforeWaitState;`: 设置等待信号激活的时间（如果使用）。

      `FSMC_WriteOperation = FSMC_WriteOperation_Enable;`: 使能对 SRAM 的写操作。

      `FSMC_WaitSignal = FSMC_WaitSignal_Disable;`: 禁用等待信号。

      `FSMC_ExtendedMode = FSMC_ExtendedMode_Disable;`: 禁用扩展模式，这意味着读写操作使用相同的时序配置。

      `FSMC_WriteBurst = FSMC_WriteBurst_Disable;`: 禁用写突发模式。

      `FSMC_ReadWriteTimingStruct = &amp;readWriteTiming;`: 将之前配置的读写时序参数结构体赋值给 FSMC 初始化结构体。

      `FSMC_WriteTimingStruct = &amp;readWriteTiming;`: 由于禁用了扩展模式，写操作也使用相同的时序配置。

      `FSMC_NORSRAMInit(&amp;FSMC_NORSRAMInitStructure);`: 使用配置好的参数初始化 FSMC 外设。

      `FSMC_NORSRAMCmd(FSMC_Bank1_NORSRAM3, ENABLE);`: 使能 FSMC Bank 1 的第三个区域，激活与该区域连接的外部 SRAM 芯片，使其可以被微控制器访问。
</code></pre>
</li>
</ul>
<h3 id="流程"><a href="#流程" class="headerlink" title="流程"></a>流程</h3><p><strong>一、硬件连接准备：</strong></p>
<ol>
<li><strong>连接地址线：</strong> 将 STM32F407 的 FSMC 地址线引脚（例如 A0-A18）与 IS62WV51216 SRAM 芯片的对应地址引脚连接。IS62WV51216 是 512K x 16 位，需要 19 根地址线。</li>
<li><strong>连接数据线：</strong> 将 STM32F407 的 FSMC 数据线引脚（D0-D15）与 IS62WV51216 SRAM 芯片的对应数据引脚连接，实现 16 位的数据传输。</li>
<li><strong>连接片选信号：</strong> 将 STM32F407 的一个 FSMC Bank 选择引脚（例如 NE3）与 IS62WV51216 SRAM 芯片的片选信号引脚（CS# 或 CE#）连接。</li>
<li><strong>连接读使能信号：</strong> 将 STM32F407 的 FSMC 读使能引脚（NOE）与 IS62WV51216 SRAM 芯片的读使能引脚（OE# 或 RD#）连接。</li>
<li><strong>连接写使能信号：</strong> 将 STM32F407 的 FSMC 写使能引脚（NWE）与 IS62WV51216 SRAM 芯片的写使能引脚（WE# 或 WR#）连接。</li>
<li><strong>连接字节控制信号：</strong> 将 STM32F407 的 FSMC 字节控制引脚（NBL0 和 NBL1）与 IS62WV51216 SRAM 芯片的字节控制引脚（LB# 和 UB#）连接，以支持按字节读写。</li>
<li><strong>电源和地：</strong> 确保 STM32F407 和 IS62WV51216 SRAM 芯片的电源和地线连接正确。</li>
</ol>
<p><strong>二、软件初始化流程 (概念性描述)：</strong></p>
<ol>
<li><p><strong>使能时钟：</strong> 在 STM32F407 的初始化代码中，需要使能 FSMC 外设的时钟以及所有与 SRAM 连接的 GPIO 端口的时钟。</p>
</li>
<li><p><strong>配置 GPIO：</strong> 对于所有连接到 SRAM 的 STM32F407 引脚，将其配置为“复用功能 (Alternate Function)”模式。同时设置合适的输出类型（通常为推挽）、输出速度（建议设置为高速或极高速以满足 SRAM 的时序要求）和上拉&#x2F;下拉电阻（通常使用上拉）。选择与 FSMC 外设对应的复用功能（一般是 AF12）。</p>
</li>
<li><p><strong>配置 FSMC Bank</strong>：</p>
<p>  选择一个 FSMC Bank（例如 Bank 1 的第三个子 Bank，对应 NE3 引脚）来连接 IS62WV51216。对该 Bank 进行如下配置：</p>
<ul>
<li><strong>存储器类型：</strong> 设置为 SRAM。</li>
<li><strong>数据&#x2F;地址复用：</strong> 禁用，因为 SRAM 使用独立的地址线和数据线。</li>
<li><strong>存储器数据宽度：</strong> 设置为 16 位，与 IS62WV51216 的数据宽度匹配。</li>
<li><strong>突发访问模式：</strong> 通常禁用，因为 IS62WV51216 是异步 SRAM。</li>
<li><strong>等待信号：</strong> 禁用，因为这是一个异步 SRAM。</li>
<li><strong>写使能：</strong> 启用，允许向 SRAM 写入数据。</li>
<li><strong>扩展模式：</strong> 通常禁用，以便读写操作使用相同的时序配置。</li>
</ul>
</li>
<li><p>配置 FSMC 时序：</p>
<p>  这是驱动 SRAM 的关键步骤，需要根据 IS62WV51216 的数据手册进行配置。主要配置以下参数：</p>
<ul>
<li><strong>地址建立时间 (Address Setup Time)：</strong> 设置地址信号在读&#x2F;写使能信号有效之前需要保持稳定的时间，以 HCLK 时钟周期为单位。</li>
<li><strong>地址保持时间 (Address Hold Time)：</strong> 设置地址信号在读&#x2F;写使能信号无效之后需要保持稳定的时间，以 HCLK 时钟周期为单位。</li>
<li>数据建立时间 (Data Setup Time)：<ul>
<li><strong>读取时：</strong> 设置在读使能信号有效后，SRAM 输出的数据需要保持稳定的时间，以便 FSMC 正确采样，以 HCLK 时钟周期为单位。</li>
<li><strong>写入时：</strong> 设置在写使能信号有效后，STM32F407 输出的数据需要保持稳定的时间，以确保 SRAM 正确写入，以 HCLK 时钟周期为单位。</li>
</ul>
</li>
<li>根据 IS62WV51216 的访问速度（最高 10&#x2F;12ns），结合 STM32F407 的时钟频率，合理设置这些时序参数。</li>
</ul>
</li>
<li><p><strong>初始化 FSMC：</strong> 使用配置好的参数初始化 FSMC 外设。</p>
</li>
<li><p><strong>使能 FSMC Bank：</strong> 启用所选的 FSMC Bank，使得外部 SRAM 可以通过 STM32F407 的内存映射地址空间进行访问。</p>
</li>
</ol>
<p><strong>三、读写数据流程：</strong></p>
<ol>
<li><strong>内存映射：</strong> 完成 FSMC 初始化后，外部 SRAM 会被映射到 STM32F407 的一块特定的内存地址空间。例如，如果使用 Bank 1 的第三个子 Bank，起始地址通常是 <code>0x68000000</code>。</li>
<li><strong>写入数据：</strong> 要向 SRAM 写入数据，只需要像操作 STM32F407 内部内存一样，向 SRAM 映射的地址执行写操作。例如，要向地址 <code>0x68000000 + 0x1000</code> 写入一个 16 位的数据 <code>0xABCD</code>，您只需要在代码中执行类似于 <code>*(volatile uint16_t*)(0x68000000 + 0x1000) = 0xABCD;</code> 的操作。FSMC 硬件会自动产生相应的控制信号（片选、写使能）和时序来完成写入过程。</li>
<li><strong>读取数据：</strong> 要从 SRAM 读取数据，同样只需要像操作内部内存一样，从 SRAM 映射的地址执行读操作。例如，要从地址 <code>0x68000000 + 0x1000</code> 读取一个 16 位的数据，您可以执行类似于 <code>uint16_t data = *(volatile uint16_t*)(0x68000000 + 0x1000);</code> 的操作。FSMC 硬件会自动产生相应的控制信号（片选、读使能）和时序来完成读取过程，并将读取到的数据返回给您的程序。</li>
<li><strong>字节控制：</strong> 如果需要进行字节级别的读写，您可以使用 8 位的数据类型（<code>uint8_t</code>）并配合 FSMC 的字节控制信号（NBL0 和 NBL1）。当您访问奇数地址或偶数地址时，FSMC 会自动激活相应的字节使能信号，从而实现对 SRAM 特定字节的操作。</li>
</ol>
]]></content>
      <categories>
        <category>stm32F407</category>
      </categories>
      <tags>
        <tag>stm32F407</tag>
        <tag>多媒体项目</tag>
      </tags>
  </entry>
  <entry>
    <title>OTA远程更新</title>
    <url>/2025/01/23/OTA%E8%BF%9C%E7%A8%8B%E6%9B%B4%E6%96%B0/</url>
    <content><![CDATA[<h1 id="OTA远程更新"><a href="#OTA远程更新" class="headerlink" title="OTA远程更新"></a>OTA远程更新</h1><p>OTA全称 Over-The-Air Technology，中文译为“空中下载技术”。简单的来说，它是一种通过无线网络对设备进行远程更新和管理的技术。就像我们手机更新App或者更新系统一样。</p>
<span id="more"></span>

<h4 id="1、基本原理"><a href="#1、基本原理" class="headerlink" title="1、基本原理"></a>1、基本原理</h4><p>stm32的OTA升级，简单的来说就是通过某种痛惜方式（例如串口，WIFI，4G等）将新的固件程序下载到STM32的Flash存储器中，然后通过引导程序（Bootloader）将新的固件程序启动起来，从而实现程序的更新。</p>
<p>要是实现OTA，至少需要两个程序：</p>
<ul>
<li>Bootloader程序：负责接收新的固件程序，将其写入到Flash，并在必要时启动新的固件程序。Bootloader程序通常是固定的，不会被OTA更新。</li>
<li>应用程序（APP）：这是用户实际运行的程序，会被OTA更新。</li>
</ul>
<h4 id="2、工程准备"><a href="#2、工程准备" class="headerlink" title="2、工程准备"></a>2、工程准备</h4><ul>
<li><p>使用stm32F103C8T6开发板。</p>
</li>
<li><p>使用软件STM32CubelIDE软件。</p>
</li>
<li><p>创建工程</p>
<ul>
<li>在STM32CubeIDE中，选择“File” -&gt; “New” -&gt; “STM32 Project”。</li>
<li>在“Target Selection”窗口中，搜索并选择“STM32F103C8Tx”，然后点击“Next”</li>
<li>在“Project Name”中输入“Bootloader”，然后点击“Next”。</li>
<li>在“Initialise all peripherals with their default Mode?”界面<strong>务必选择“No”</strong>。这将允许我们手动配置所需的外设，避免不必要的代码和潜在的冲突。然后点击“Finish”。</li>
</ul>
</li>
<li><p>配置时钟</p>
<ul>
<li>双击项目根目录下的 <code>.ioc</code> 文件（例如 <code>Bootloader.ioc</code>），打开STM32CubeMX配置界面。</li>
<li>在左侧的“System Core”中选择“RCC”。</li>
<li>在“HSE”下拉菜单中选择“Crystal&#x2F;Ceramic Resonator”（假设你使用外部晶振）。<ul>
<li>菜单中选择项的解释：</li>
<li><strong>Disable</strong> ：禁用</li>
<li><strong>Bypass Clock Source（旁路时钟源）</strong> ：选择此项表示使用外部时钟源直接输入到 MCU，绕过内部的振荡器电路。这意味着外部提供一个稳定的时钟信号到 OSC_IN 引脚，而 OSC_OUT 引脚则悬空。使用此选项时，外部时钟源必须已经产生稳定的时钟信号。</li>
<li><strong>Crystal&#x2F;Ceramic Resonator（晶体&#x2F;陶瓷谐振器）</strong>：选择此项表示使用外部晶体或陶瓷谐振器作为时钟源。这是最常见的配置方式。在这种模式下，晶振连接到 OSC_IN 和 OSC_OUT 引脚，并且通常需要配合合适的负载电容使用，以确保晶振正常起振</li>
</ul>
</li>
<li>在“PLLCLK”选项卡中配置PLL倍频器，将系统时钟配置为72MHz。通常的配置是：<ul>
<li>PLL Source Mux: HSE</li>
<li>PLL Multiplier: x9</li>
</ul>
</li>
<li>确认“SYSCLK”显示为72MHz。</li>
<li>保存 <code>.ioc</code> 文件（Ctrl+S 或 File -&gt; Save），STM32CubeIDE会自动生成或更新代码。</li>
</ul>
</li>
<li><p>配置串口</p>
<ul>
<li>在左侧的“Connectivity”中选择“USART1”。</li>
<li>在中间的配置界面中：<ul>
<li>“Mode”选择“Asynchronous”。（异步，还有其他几个选择，）<ul>
<li>Mode选择项：</li>
<li><strong>Asynchronous（异步模式）</strong>：这是 USART 最常用的模式。在这种模式下，数据传输不需要单独的时钟线进行同步，而是通过起始位、数据位、校验位和停止位来定义数据帧的结构。发送方和接收方需要预先约定好波特率（即每秒传输的比特数），以确保数据能够正确传输。</li>
<li><strong>Synchronous（同步模式）</strong>：在这种模式下，数据传输需要一根额外的时钟线（通常称为 SCLK 或 CK）进行同步。发送方提供时钟信号，接收方根据时钟信号进行数据的采样和接收。</li>
<li><strong>Single Wire (Half-Duplex)（单线（半双工）模式）</strong>：在这种模式下，USART 使用同一根线进行发送和接收，但不能同时进行。</li>
<li><strong>Multiprocessor Communication（多处理器通信模式）</strong>：这种模式允许多个处理器通过同一条 USART 总线进行通信。在这种模式下，每个处理器都有一个唯一的地址。</li>
<li><strong>IrDA（红外数据协会）</strong>：这种模式支持红外线通信协议。</li>
<li><strong>LIN（本地互联网络）</strong>：LIN 是一种低成本的串行通信协议，常用于汽车电子等领域。</li>
<li><strong>SmartCard（智能卡模式）</strong>：这种模式支持智能卡（例如 SIM 卡、银行卡）的通信协议。</li>
</ul>
</li>
<li>“Baud Rate”设置为115200（或其他你需要的波特率）。</li>
<li>“Word Length”选择“8 Bits”。</li>
<li>“Parity”选择“None”。（奇偶校验）</li>
<li>“Stop Bits”选择“1 Stop Bit”。</li>
<li>使能“NVIC Settings”中的USART1全局中断。这将在之后允许我们使用中断方式接收串口数据（虽然在之前的简化版本中未使用中断，但这是一个良好的习惯，后续会用到）。</li>
</ul>
</li>
<li>保存 <code>.ioc</code> 文件。</li>
</ul>
</li>
<li><p>配置Flash分区</p>
<ul>
<li><p>在项目资源管理器中，展开“Bootloader”项目，打开 <code>STM32F103C8Tx_FLASH.ld</code> 文件。</p>
</li>
<li><p>找到以下代码段：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">FLASH (rx)      : ORIGIN = 0x08000000, LENGTH = 64K</span><br></pre></td></tr></table></figure>
</li>
<li><p>将其修改为：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">FLASH (rx)      : ORIGIN = 0x08000000, LENGTH = 16K</span><br></pre></td></tr></table></figure>

<p>  这会将Flash的前16KB分配给Bootloader。应用程序将从<code>0x08004000</code>开始。</p>
</li>
</ul>
</li>
</ul>
<h4 id="3、代码编辑"><a href="#3、代码编辑" class="headerlink" title="3、代码编辑"></a>3、代码编辑</h4><ul>
<li>串口接收数据</li>
</ul>
]]></content>
      <categories>
        <category>stm32学习</category>
      </categories>
      <tags>
        <tag>stm32学习</tag>
        <tag>OTA</tag>
      </tags>
  </entry>
  <entry>
    <title>Pinctrl和GPIO子系统</title>
    <url>/2025/01/16/Pinctrl%E5%AD%90%E7%B3%BB%E7%BB%9F%E5%92%8CGPIO%E5%AD%90%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<h1 id="Pinctrl和GPIO子系统"><a href="#Pinctrl和GPIO子系统" class="headerlink" title="Pinctrl和GPIO子系统"></a>Pinctrl和GPIO子系统</h1><p><strong>引言</strong> </p>
<p>在Linux系统中Pinctrl子系统和GPIO子系统是非常重要的概念，他们共同负责和管理SoC（system on a Chip）上的引脚。这两个子系统相互协作，为驱动开发提供一个灵活且高效的接口，用于配置和控制硬件引脚。</p>
<span id="more"></span>

<h2 id="Pinctrl子系统"><a href="#Pinctrl子系统" class="headerlink" title="Pinctrl子系统"></a>Pinctrl子系统</h2><p>Pinctrl，即Pin Conctroller，中文翻译为引脚控制器。它是一个虚拟的概念，主要用与设置IOMUX（input&#x2F;output Multiplexer），将某个引脚链接到指定的模块，从而实现特定的功能。</p>
<p><strong>Pinctrl的作用</strong></p>
<ul>
<li>引脚复用：一个引脚可以被配置为多种功能（如GPIO、UART、IIC等），Pinctrl子系统负责管理这些服用配置。</li>
<li>引脚配置：设置引脚的电气特性，如输入输出模式、驱动能力、上拉下拉等。、</li>
<li>设备树集成：通过设备树描述硬件，pingctrl子系统更具设备树配置自动完成引脚的初始化。</li>
</ul>
<p><strong>Pinctrl的工作原理</strong></p>
<p>1、<strong>设备树描述</strong>：在设备树中，我们为每个引脚配置定义一个pinctrl-name属性，为这个属性	指定该引脚可以具有的各种状态。</p>
<p>2、<strong>Pinctrl驱动</strong>：pinctrl驱动程序会根据设备树中的配置，初始化pinctrl子系统。</p>
<p>3、<strong>驱动程序的使用</strong>：当一个设备的驱动程序需要使用某个引脚时，它会通过pinctrl子系统申请并配置引脚。</p>
<p>4、<strong>pinctrl子系统的配置</strong>：pinctrl子系统会根据驱动程序的请求，设置相应的寄存器，完成引脚的复用和配置。</p>
<h5 id="1、pinctrl节点介绍"><a href="#1、pinctrl节点介绍" class="headerlink" title="1、pinctrl节点介绍"></a>1、pinctrl节点介绍</h5><p>首先我们可以在linux-5.4.31&#x2F;arch&#x2F;arm&#x2F;boot&#x2F;dts&#x2F;stm32mp151-pinctrl.dtsi文件中找到pinctrl节点。或者也可以使用<code> grep &quot;pinctrl:&quot; stm32mp15*</code> 在dts目录下搜索。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">soc &#123;</span><br><span class="line">     pinctrl: pin-controller@50002000 &#123;</span><br><span class="line">         #address-cells = &lt;1&gt;;</span><br><span class="line">         #size-cells = &lt;1&gt;;</span><br><span class="line">         compatible = &quot;st,stm32mp157-pinctrl&quot;;</span><br><span class="line">         ranges = &lt;0 0x50002000 0xa400&gt;;</span><br><span class="line">         interrupt-parent = &lt;&amp;exti&gt;;</span><br><span class="line">         st,syscfg = &lt;&amp;exti 0x60 0xff&gt;;</span><br><span class="line">         hwlocks = &lt;&amp;hsem 0&gt;;</span><br><span class="line">         pins-are-numbered;</span><br><span class="line">     &#125;</span><br><span class="line"> /* 剩下内容省略 */</span><br><span class="line"> &#125;;</span><br></pre></td></tr></table></figure>

<ul>
<li>soc{…}：这表示pinctrl节点是soc根节点下的一个子节点，在设备树中，soc节点是描述整个片上系统。</li>
<li>pinctrl：节点标签，方便其他地方引用节点。</li>
<li>#address-cells、#address-cells：定义了地址单元、大小单元的数量。（也就是reg属性里面的数量）。</li>
<li>compatible：兼容性字符串，用于驱动的匹配。（很重要）（书写规范：从最具体到最通用的顺序排列，首先是制造商 的名称，然后是设备的型号或者设备类型的描述）</li>
<li>ranges：该属性定义了父地址空间和子地址空间的映射关系。0：父地址空间的起始地址偏移量为0；0x50002000：子地址空间的起始地址；0xa400：子地址空间的大小。<ul>
<li>rangs与reg两个的区别：reg属性时用来描述父节点的地址空间的地址和大小，是物理地址直接链接在内存总线上的。而rangs属性是用与父节点和子节点之间建立隐射关系的。</li>
</ul>
</li>
<li>interrupt-parent：中断的父节点。</li>
<li>st,syscfg：stm特定的属性，用于配置系统配置寄存器的。</li>
<li>hwlocks：该节点的硬件锁，&amp;hsem，0：表示使用hsem节点的第一个硬件信号量。</li>
<li>pins-are-numbered：这个是一个标记属性，表示该引脚控制器使用数字编号来表示引脚。</li>
</ul>
<p>该文件是由芯片厂商官方将芯片的通用部分单独提出来的一些设备树的配置信息，在soc节点中汇总了所需的配置信息。</p>
<p>在设备树的只要配置文件的stm32mp15-pinctrl.dtsi文件中，我们可以搜索到很多的配置信息。大概如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&amp;pinctrl &#123;</span><br><span class="line">        adc1_in6_pins_a: adc1-in6 &#123;</span><br><span class="line">                pins &#123;</span><br><span class="line">                        pinmux = &lt;STM32_PINMUX(&#x27;F&#x27;, 12, ANALOG)&gt;;</span><br><span class="line">                &#125;;</span><br><span class="line">        &#125;;</span><br><span class="line"></span><br><span class="line">        adc12_ain_pins_a: adc12-ain-0 &#123;</span><br><span class="line">                pins &#123;</span><br><span class="line">                        pinmux = &lt;STM32_PINMUX(&#x27;C&#x27;, 3, ANALOG)&gt;, /* ADC1 in13 */</span><br><span class="line">                                 &lt;STM32_PINMUX(&#x27;F&#x27;, 12, ANALOG)&gt;, /* ADC1 in6 */</span><br><span class="line">                                 &lt;STM32_PINMUX(&#x27;F&#x27;, 13, ANALOG)&gt;, /* ADC2 in2 */</span><br><span class="line">                                 &lt;STM32_PINMUX(&#x27;F&#x27;, 14, ANALOG)&gt;; /* ADC2 in6 */</span><br><span class="line">                &#125;;</span><br><span class="line">        &#125;;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<ul>
<li>&amp;pinctrl：这是一个引用。相当一在上面结果的pinctrl节点中添加的子节点。</li>
<li>adc1_in6_pins_a: adc1-in6 { … }：标签和名称。</li>
<li>pins{…}：pins节点包含具体的引脚配置。</li>
<li>pinmux：用于配置引脚的复用功能。</li>
<li>&lt;STM32_PINMUX(‘F’, 12, ANALOG)&gt;：这行代码的含义是将 PF12 引脚配置为 ADC1 的输入通道 6。</li>
</ul>
<h2 id="GPIO子系统"><a href="#GPIO子系统" class="headerlink" title="GPIO子系统"></a>GPIO子系统</h2><h4 id="1、GPIO子系统的作用"><a href="#1、GPIO子系统的作用" class="headerlink" title="1、GPIO子系统的作用"></a>1、GPIO子系统的作用</h4><p>GPIO（通用输入&#x2F;输出）是微控制器或者片上系统（soc）上最基本的外设之一。GPIO引脚乐意配置为输入或者输出，用于与外部设备进行数字信号的交互。</p>
<h4 id="2、GPIO子系统的组成部分"><a href="#2、GPIO子系统的组成部分" class="headerlink" title="2、GPIO子系统的组成部分"></a>2、GPIO子系统的组成部分</h4><ul>
<li>gpiolib核心：GPIO子系统的核心框架，提供了一组同的API函数，共驱动使用。</li>
<li>GPIO控制器驱动：每一个soc都有自己的GPIO控制器，需要相应的驱动程序来管理这些控制器。</li>
<li>设备树绑定：设备树用于描述硬件信息。</li>
</ul>
<h4 id="3、GPIO子系统的API"><a href="#3、GPIO子系统的API" class="headerlink" title="3、GPIO子系统的API"></a>3、GPIO子系统的API</h4><p>GPIO有两套API：</p>
<ul>
<li>基于描述符的API：新的API，函数以gpiod—为开头，它使用gpio-desc结构体表示一个gpio引脚。</li>
<li>传统的API：函数以gpio-为开头，使用一个整数来表示一个gpio引脚。</li>
</ul>
<h1 id="实验代码"><a href="#实验代码" class="headerlink" title="实验代码"></a>实验代码</h1><p><strong>功能实现：</strong> </p>
<ul>
<li>使用pinctrl子系统和GPIO子系统。</li>
<li>使用设备树。</li>
<li>点亮led灯。</li>
</ul>
<p><strong>子系统的讲解：</strong> </p>
<ul>
<li><p>在内核代码的&#x2F;home&#x2F;h&#x2F;h&#x2F;linux-5.4.31&#x2F;arch&#x2F;arm&#x2F;boot&#x2F;dts&#x2F;stm32mp157a-fsmp1a.dts目录下，添加设备树的节点。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">led_test2&#123;</span><br><span class="line">      compatible = &quot;h,led2&quot;;</span><br><span class="line">      led_name = &quot;led02&quot;;</span><br><span class="line">      led_minor = &lt;10&gt;;</span><br><span class="line">      gpios = &lt;&amp;gpioz 5 GPIO_ACTIVE_HIGH&gt;,</span><br><span class="line">              &lt;&amp;gpioz 6 GPIO_ACTIVE_HIGH&gt;,</span><br><span class="line">              &lt;&amp;gpioz 7 GPIO_ACTIVE_HIGH&gt;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>s其中gpios是在dts&#x2F;stm32mp151.dtsi文件中的gpioz节点中追加。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">gpioz: gpio@54004000 &#123;</span><br><span class="line">    gpio-controller;</span><br><span class="line">    #gpio-cells = &lt;2&gt;;</span><br><span class="line">    interrupt-controller;</span><br><span class="line">    #interrupt-cells = &lt;2&gt;;</span><br><span class="line">    reg = &lt;0 0x400&gt;;</span><br><span class="line">    clocks = &lt;&amp;scmi0_clk CK_SCMI0_GPIOZ&gt;;</span><br><span class="line">    st,bank-name = &quot;GPIOZ&quot;;</span><br><span class="line">    st,bank-ioport = &lt;11&gt;;</span><br><span class="line">    status = &quot;disabled&quot;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>重新编译设备树。运行下面命令。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">h@h-virtual-machine:~/h/linux-5.4.31$ make -j4 ARCH=arm dtbs LOADADDR=0xC2000040</span><br><span class="line">  DTC     arch/arm/boot/dts/stm32mp157a-fsmp1a.dtb</span><br><span class="line">h@h-virtual-machine:~/h/linux-5.4.31$ cp -raf arch/arm/boot/dts/stm32mp157a-fsmp1a.dtb /tftpboot/</span><br></pre></td></tr></table></figure>
</li>
<li><p>这样我们编写的设备树文件会重新编译并在重新启动板子的时候加载到芯片中。</p>
</li>
</ul>
<p><strong>平台驱动程序讲解：</strong> </p>
<ul>
<li><p>编写驱动框架代码。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static int __init led_pdrv_init(void)</span><br><span class="line">&#123;</span><br><span class="line">    printk(&quot;-------%s----------\n&quot;,__FUNCTION__);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">static void __exit led_pdrv_exit(void)</span><br><span class="line">&#123;</span><br><span class="line">    printk(&quot;-------%s----------\n&quot;,__FUNCTION__);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">module_init(led_pdrv_init);</span><br><span class="line">module_exit(led_pdrv_exit);</span><br><span class="line">MODULE_LICENSE(&quot;GPL&quot;);</span><br></pre></td></tr></table></figure>
</li>
<li><p>框架搭好的了之后，开始写注册平台驱动。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &lt;linux/init.h&gt;</span><br><span class="line">#include &lt;linux/module.h&gt;</span><br><span class="line">#include &lt;linux/miscdevice.h&gt;</span><br><span class="line">#include &lt;linux/platform_device.h&gt;</span><br><span class="line">#include &lt;linux/slab.h&gt;</span><br><span class="line">#include &lt;linux/fs.h&gt;</span><br><span class="line">#include &lt;linux/io.h&gt;</span><br><span class="line">#include &lt;linux/uaccess.h&gt;</span><br><span class="line">#include &lt;linux/mod_devicetable.h&gt;</span><br><span class="line">#include &lt;linux/of.h&gt;</span><br><span class="line">#include &lt;linux/of_gpio.h&gt;</span><br><span class="line">#include &lt;linux/gpio.h&gt;</span><br><span class="line"></span><br><span class="line">int led_probe(struct platform_device *pdev)</span><br><span class="line">&#123;</span><br><span class="line">    printk(&quot;-------%s----------\n&quot;,__FUNCTION__);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line">int led_remove(struct platform_device *pdev)</span><br><span class="line">&#123;</span><br><span class="line">    printk(&quot;-------%s----------\n&quot;,__FUNCTION__);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">struct of_device_id led_match_table[] = &#123;</span><br><span class="line">    &#123;</span><br><span class="line">        .compatible = &quot;h,led2&quot;,</span><br><span class="line">    &#125;,</span><br><span class="line">    &#123;</span><br><span class="line">    &#125;,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">struct platform_driver led_pdrv = &#123;</span><br><span class="line">    .probe = led_probe,</span><br><span class="line">    .remove = led_remove,</span><br><span class="line">    .driver = &#123;</span><br><span class="line">        .name = &quot;led_pdrv&quot;,</span><br><span class="line">        .of_match_table = led_match_table,</span><br><span class="line">    &#125;,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">static int __init led_pdrv_init(void)</span><br><span class="line">&#123;</span><br><span class="line">    printk(&quot;-------%s----------\n&quot;,__FUNCTION__);</span><br><span class="line">    return platform_driver_register(&amp;led_pdrv);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">static void __exit led_pdrv_exit(void)</span><br><span class="line">&#123;</span><br><span class="line">    printk(&quot;-------%s----------\n&quot;,__FUNCTION__);</span><br><span class="line">    platform_driver_unregister(&amp;led_pdrv);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">module_init(led_pdrv_init);</span><br><span class="line">module_exit(led_pdrv_exit);</span><br><span class="line">MODULE_LICENSE(&quot;GPL&quot;);</span><br></pre></td></tr></table></figure>

<ul>
<li>其中<code>struct platform_driver led_pdrv</code> 结构体是很重要的，probe函数是在驱动和设备树上的节点匹配之后，内核自动调用的。remove是在内核卸载是调用的。driver中的name是平台驱动的名字。of_match_table的作用是与设备树进行匹配用的，通过compatible进行匹配。</li>
</ul>
</li>
<li><p>平台驱动,probe函数的实现</p>
<ul>
<li><p>为自己创建的结构体申请空间</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct stm32mp157_led &#123;</span><br><span class="line">    struct miscdevice misc; // 直接定义miscdevice结构体</span><br><span class="line">    struct device_node *np;</span><br><span class="line">    int gpio_5;</span><br><span class="line">    int gpio_6;</span><br><span class="line">    int gpio_7;</span><br><span class="line">&#125;;</span><br><span class="line">struct stm32mp157_led *led_t;</span><br><span class="line"></span><br><span class="line">    led_t = kzalloc(sizeof(*led_t), GFP_KERNEL);</span><br><span class="line">    if (!led_t) &#123;</span><br><span class="line">        printk(KERN_ERR &quot;led kzalloc failed\n&quot;);</span><br><span class="line">        return -ENOMEM;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>获取设备树节点属性，为创建杂项设备做准备</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">led_t-&gt;np = pdev-&gt;dev.of_node;</span><br><span class="line">of_property_read_u32(led_t-&gt;np, &quot;led_minor&quot;, &amp;minor);</span><br><span class="line">of_property_read_string(led_t-&gt;np, &quot;led_name&quot;, (const char 	**)&amp;name);</span><br></pre></td></tr></table></figure>
</li>
<li><p>初始化杂项设备和注册。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//初始化杂项设备</span><br><span class="line">led_t-&gt;misc.minor = minor;</span><br><span class="line">led_t-&gt;misc.name = name;</span><br><span class="line">led_t-&gt;misc.fops = &amp;led_fops;</span><br><span class="line">        </span><br><span class="line">// 注册杂项设备</span><br><span class="line">ret = misc_register(led_t-&gt;misc);</span><br><span class="line">if(ret)</span><br><span class="line">&#123;</span><br><span class="line">    printk(&quot;misc register faile\n&quot;);</span><br><span class="line">    goto err_misc_register;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>申请gpio资源</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 申请gpio资源</span><br><span class="line">led_t-&gt;gpio_5 = of_get_gpio(led_t-&gt;np, 0);</span><br><span class="line">led_t-&gt;gpio_6 = of_get_gpio(led_t-&gt;np, 1);</span><br><span class="line">led_t-&gt;gpio_7 = of_get_gpio(led_t-&gt;np, 2);</span><br></pre></td></tr></table></figure>
</li>
<li><p>错误跳转</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">err_misc_register:</span><br><span class="line">    kfree(led_t);</span><br><span class="line">    return ret;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>平台驱动的remove函数实现</p>
<ul>
<li><p>注销和卸载</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">misc_deregister(led_t-&gt;misc);</span><br><span class="line">kfree(led_t);</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>文件操作函数write函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ssize_t led_write (struct file *file, const char __user *buf, size_t size, loff_t *loff)</span><br><span class="line">&#123;</span><br><span class="line">    int ret = 0;</span><br><span class="line">    int value;</span><br><span class="line">    printk(&quot;-------%s----------\n&quot;,__FUNCTION__);</span><br><span class="line">    // 将应用数据转换为内核数据</span><br><span class="line">    ret = copy_from_user(&amp;value, buf, size);</span><br><span class="line">    if (ret) &#123;</span><br><span class="line">        printk(&quot;copy from user error\n&quot;);</span><br><span class="line">        return -EAGAIN;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    gpio_request(led_t-&gt;gpio_5, &quot;led_5&quot;);</span><br><span class="line">    gpio_request(led_t-&gt;gpio_6, &quot;led_6&quot;);</span><br><span class="line">    gpio_request(led_t-&gt;gpio_7, &quot;led_7&quot;);</span><br><span class="line"></span><br><span class="line">    if (value) &#123;   // 开灯</span><br><span class="line">        gpio_direction_output(led_t-&gt;gpio_5, 1);</span><br><span class="line">        gpio_direction_output(led_t-&gt;gpio_6, 1);</span><br><span class="line">        gpio_direction_output(led_t-&gt;gpio_7, 1);</span><br><span class="line">    &#125; else &#123;  // 关灯</span><br><span class="line">        gpio_direction_output(led_t-&gt;gpio_5, 0);</span><br><span class="line">        gpio_direction_output(led_t-&gt;gpio_6, 0);</span><br><span class="line">        gpio_direction_output(led_t-&gt;gpio_7, 0);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    gpio_free(led_t-&gt;gpio_5);</span><br><span class="line">    gpio_free(led_t-&gt;gpio_6);</span><br><span class="line">    gpio_free(led_t-&gt;gpio_7);</span><br><span class="line"></span><br><span class="line">    return size;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<p><strong>应用程序led_app.c</strong> </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &lt;stdio.h&gt;</span><br><span class="line">#include &lt;stdlib.h&gt;</span><br><span class="line">#include &lt;sys/types.h&gt;</span><br><span class="line">#include &lt;sys/stat.h&gt;</span><br><span class="line">#include &lt;fcntl.h&gt;</span><br><span class="line">#include &lt;unistd.h&gt;</span><br><span class="line"></span><br><span class="line">int main(int argc,char **argv)</span><br><span class="line">&#123;</span><br><span class="line">    int fd;</span><br><span class="line">    int on;</span><br><span class="line"></span><br><span class="line">    fd = open(argv[1], O_RDWR);</span><br><span class="line">    if(fd &lt; 0)</span><br><span class="line">    &#123;</span><br><span class="line">        perror(&quot;open faile\n&quot;);</span><br><span class="line">        exit(1);</span><br><span class="line">    &#125;</span><br><span class="line">    while(1)</span><br><span class="line">    &#123;</span><br><span class="line">        on = 1;</span><br><span class="line">        write(fd, &amp;on, sizeof(on));</span><br><span class="line">        sleep(1);</span><br><span class="line">        on = 0;</span><br><span class="line">        write(fd, &amp;on, sizeof(on));</span><br><span class="line">        sleep(1);</span><br><span class="line">    &#125;</span><br><span class="line">    close(fd);</span><br><span class="line">    </span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Makefile文件</strong> </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">KERN_DIR = /home/h/h/linux-5.4.31</span><br><span class="line">DRV_DIR := $(shell pwd)  # 获取当前工作目录</span><br><span class="line">MODULE_NAME = led_pdrv</span><br><span class="line">APP_NAME = led_app</span><br><span class="line"></span><br><span class="line">all:</span><br><span class="line">	make -C $(KERN_DIR) M=$(DRV_DIR) modules</span><br><span class="line">	$(CC) -o $(APP_NAME) $(APP_NAME).c</span><br><span class="line"></span><br><span class="line">clean:</span><br><span class="line">	make -C $(KERN_DIR) M=$(DRV_DIR) clean</span><br><span class="line">	rm -rf $(APP_NAME)</span><br><span class="line"></span><br><span class="line">install:</span><br><span class="line">	cp -raf *.ko /opt/myrootfs/drv_module</span><br><span class="line">	cp -raf $(APP_NAME) /opt/myrootfs/drv_module</span><br><span class="line"></span><br><span class="line">obj-m += $(MODULE_NAME).o</span><br></pre></td></tr></table></figure>

<p><strong>下载验证</strong> </p>
<ul>
<li><p>在文件目录下，输入<code>make</code> 命令，进行编译，成功后，再输入<code>make install</code> 将编译的文件加载到内核中。</p>
</li>
<li><p>重新启动下载板子。</p>
</li>
<li><p>在串口助手的端口中找到存放代码的文件，可以发现有两个文件。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">led_app led_pdrv.ko</span><br></pre></td></tr></table></figure>
</li>
<li><p>输入<code>insmod led_pdrv.ko</code> 命令加载驱动。</p>
</li>
<li><p>成功后，可以在&#x2F;dev目录下找平台设备文件。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"># ls /dev/led02 -n</span><br><span class="line">crw-rw----    1 0        0          10,  10 Jan  5 14:04 /dev/led02</span><br><span class="line"></span><br></pre></td></tr></table></figure>
</li>
<li><p>然后加载应用程序就可以了。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">./led_app /dev/led02</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>Linux驱动</category>
      </categories>
      <tags>
        <tag>Pinctrl子系统</tag>
        <tag>GPIO子系统</tag>
        <tag>Linux学习</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2025/01/21/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<span id="more"></span>

<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>OV2460摄像头</title>
    <url>/2025/03/17/OV2460%E6%91%84%E5%83%8F%E5%A4%B4/</url>
    <content><![CDATA[<h1 id="OV2460摄像头"><a href="#OV2460摄像头" class="headerlink" title="OV2460摄像头"></a>OV2460摄像头</h1><h3 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h3><p>OV2460 是 OmniVision 推出的一款 200 万像素的 CMOS 图像传感器，像头中的图像传感器是一款型号为OV2640的CMOS类型数字图像传感器。该传感器支持输出最大为200万像素的图像 (1600x1200分辨率)， 支持使用VGA时序输出图像数据，输出图像的数据格式支持YUV(422&#x2F;420)、YCbCr422、RGB565以及JPEG格式，若直接输出JPEG格式的图像时可大大减少数据量， 方便网络传输。它还可以对采集得的图像进行补偿，支持伽玛曲线、白平衡、饱和度、色度等基础处理。根据不同的分辨率配置， 传感器输出图像数据的帧率从15-60帧可调，工作时功率在125mW-140mW之间。</p>
<span id="more"></span>

<h4 id="OV2460功能框图"><a href="#OV2460功能框图" class="headerlink" title="OV2460功能框图"></a>OV2460功能框图</h4><p><img src="/2025/03/17/OV2460%E6%91%84%E5%83%8F%E5%A4%B4/DCMIOV004.jpeg" alt="OV2640功能框图"></p>
<ul>
<li><p>控制寄存器</p>
<ul>
<li>标号1处是OV2460的控制寄存器，它由外部控制器通过SIO_C和SIO_D引脚写入。而SIO_C和SIO_D使用的协议是SCCB协议，而<a href="###SCCB%EF%BC%88%E4%B8%B2%E8%A1%8C%E6%91%84%E5%83%8F%E5%A4%B4%E6%8E%A7%E5%88%B6%E6%80%BB%E7%BA%BF%EF%BC%89">SCCB</a>和IIC协议大致类似，在软件中完全可以用IIC驱动来控制。</li>
</ul>
</li>
<li><p>通信、时钟、控制信号</p>
<ul>
<li>通信、控制信号<ul>
<li>PCLK：像素同步时钟，适用于输出数据的同步信号，是由OV2460产生的。</li>
<li>HREF：行同步信号</li>
<li>VSYNC：帧同步信号</li>
<li>RESETB：引脚为低电平时，复位传感器</li>
<li>PWDN：控制芯片进入低功耗模式</li>
</ul>
</li>
<li>时钟<ul>
<li>XCLK：于PCLK不同，XCLK是驱动整个传感器的时钟信号。由外部晶振输入的。</li>
</ul>
</li>
</ul>
</li>
<li><p>感光矩阵</p>
<ul>
<li>光信号转化为电信号，经过处理后，这些信号存储呈给一个个的像素点表示的图像。</li>
</ul>
</li>
<li><p>数据输出信号</p>
<ul>
<li><p>其中的DSP，会根据控制寄存器中设置进行图像处理。最终处理数据通过Y0-9引脚输出，一般来说使用8根数据线进行传输。</p>
<p>  <img src="/2025/03/17/OV2460%E6%91%84%E5%83%8F%E5%A4%B4/DCMIOV005.jpeg" alt="8位数据线接法"></p>
</li>
</ul>
</li>
</ul>
<h3 id="SCCB（串行摄像头控制总线）"><a href="#SCCB（串行摄像头控制总线）" class="headerlink" title="SCCB（串行摄像头控制总线）"></a>SCCB（串行摄像头控制总线）</h3><h5 id="SCCB概述："><a href="#SCCB概述：" class="headerlink" title="SCCB概述："></a>SCCB概述：</h5><ul>
<li>SCCB是由OV公司定义和发展的一种串行总线协议。</li>
<li>它主要用于控制和配置图像传感器。</li>
<li>通过SCCB，可以设置摄像头的各种数据。</li>
</ul>
<h5 id="SCCB特点："><a href="#SCCB特点：" class="headerlink" title="SCCB特点："></a>SCCB特点：</h5><ul>
<li>SCCB的结构和IIC相似。</li>
<li>SCCB有两种模式：一主多从，一主一从。</li>
<li>引脚：两根线：SIO_C、SIO_D.</li>
</ul>
<h5 id="SCCB的工作原理："><a href="#SCCB的工作原理：" class="headerlink" title="SCCB的工作原理："></a>SCCB的工作原理：</h5><ul>
<li>SCCB通信可以主机发送命令和数据给从机，还可以从从机中读取数据。</li>
<li>由主机通过SIO_C发送时钟信号，通过SIO_D线传输数据。</li>
<li>数据传输包括起始位、从机地址、读&#x2F;写位、寄存器地址、数据、停止位。</li>
</ul>
<h5 id="SCCB和IIC的不同"><a href="#SCCB和IIC的不同" class="headerlink" title="SCCB和IIC的不同"></a>SCCB和IIC的不同</h5><ul>
<li>应答位：SCCB的应答位是 Don’t care，而iIC是ACK。<ul>
<li>这里的Don’t care，表示当从机发送应答位，主机可以不进行判断，不管从机发送的啥，都会进行后续的操作。</li>
<li>IIC的ACK应答位是主机明确说从机只有发送应答为逻辑0，才会进行后续的操作。</li>
</ul>
</li>
<li>读写位操作：SCCB只能进行单次读取，而IIC可以进行单次和连续读取。<ul>
<li>SCCB协议主要适用于配置寄存器，所以SCCB单次读取，每次只读取一个寄存器的值。</li>
</ul>
</li>
<li>读写中间的stop信号：SCCB读操作中间由stop信号。</li>
</ul>
<h5 id="SCCB时序："><a href="#SCCB时序：" class="headerlink" title="SCCB时序："></a>SCCB时序：</h5><ul>
<li><p>起始信号、停止信号</p>
<p>  <img src="/2025/03/17/OV2460%E6%91%84%E5%83%8F%E5%A4%B4/DCMIOV006.jpeg" alt="SCCB停止信号"></p>
<ul>
<li>起始信号：在SIO_C为高电平时，SIO_D由低变为高电平。</li>
<li>停止信号：在SIO_C为高电平时，SIO_D由高变为低电平。</li>
<li>数据有效位：除去起始信号和停止信号，在数据传输过程中，当SIO_C为高电平期间，SIO_D为有效数据。</li>
</ul>
</li>
<li><p>SCCB数据读写过程</p>
<ul>
<li><p>SCCB有两种写操作：</p>
<ul>
<li><p>三步写操作：</p>
<ul>
<li><p>第一步：发送从设备的ID地址+W标志</p>
</li>
<li><p>第二部：发送从设备的目标寄存器的8位地址</p>
</li>
<li><p>第三步：发送写入寄存器的8位数据。</p>
<p>  <img src="/2025/03/17/OV2460%E6%91%84%E5%83%8F%E5%A4%B4/DCMIOV008.png" alt="SCCB的三步写操作"></p>
</li>
</ul>
</li>
<li><p>两步操作：</p>
<ul>
<li>第一步：发送从设备的ID地址和W标志</li>
<li>第二步：发送目标寄存器的地址</li>
</ul>
</li>
<li><p>两步写操作是用来配合后面的读寄存器的操作。</p>
</li>
</ul>
</li>
<li><p>SCCB读操作：</p>
<ul>
<li>在两部写操作的基础上，在加上发送从机地址和R读标志。</li>
<li>开始读取数据。</li>
</ul>
</li>
<li><p>SCCB传输一个字节数据会有一个无效位（don‘t care），无论从机发送的什么都不管，继续执行代码。</p>
</li>
</ul>
</li>
</ul>
<h4 id="OV2640的寄存器"><a href="#OV2640的寄存器" class="headerlink" title="OV2640的寄存器"></a>OV2640的寄存器</h4><p>在控制OV2640中涉及到了很多的寄存器，详细的可以字节去找资料。但是注意的两组寄存器。这两组寄存器的地址有些重合，通过配置为0xFF的RA_DLMT寄存器可以切换寄存器组，当RA_DLMT为0时，使用式DSP相关的寄存器组；当RA_DLMT为1时，使用是Sensor相关的寄存器组。</p>
<h4 id="像素输出时序"><a href="#像素输出时序" class="headerlink" title="像素输出时序"></a>像素输出时序</h4><p>主控制控制OV2640时，采用的是SCCB协议读写其寄存器，而它输出图像时则使用VGA时序（还有SVGA、UXGA，都差不多）。OV2640输出图像时，一帧一帧的输出，从左到右，从上到下。</p>
<p><img src="/2025/03/17/OV2460%E6%91%84%E5%83%8F%E5%A4%B4/DCMIOV014.jpeg" alt="摄像头数据输出"></p>
<p>如果我们使用Y2-9的8根数据线输出图像格式为RGB565，进行数据输出时，Y2-9数据线会在一个像素同步时钟PCLK的驱动下发送一个字节的数据信号。两个PCLK时钟信号可以发送一个RGB565格式的像素数据。依次传输，每传输一行的像素数据，行同步信号就会跳变一次。当一帧图像传输完成后，VSYNC会输出一个一个电平跳变的信号。</p>
<p><img src="/2025/03/17/OV2460%E6%91%84%E5%83%8F%E5%A4%B4/DCMIOV015.jpeg" alt="像素同步时序"></p>
<p><img src="/2025/03/17/OV2460%E6%91%84%E5%83%8F%E5%A4%B4/DCMIOV016.jpeg" alt="帧图像同步时序"></p>
<h3 id="DCMI接口"><a href="#DCMI接口" class="headerlink" title="DCMI接口"></a>DCMI接口</h3><p>STM32F4系列的控制器包含了DCMI数字摄像头接口(Digital cameraInterface)， 它支持使用上述类似VGA的时序获取图像数据流，支持原始的按行、帧格式来组织的图像数据，如YUV、RGB， 也支持接收JPEG格式压缩的数据流。接收数据时，主要使用HSYNC及VSYNC信号来同步。</p>
<h5 id="DCMI框图"><a href="#DCMI框图" class="headerlink" title="DCMI框图"></a>DCMI框图</h5><p><img src="/2025/03/17/OV2460%E6%91%84%E5%83%8F%E5%A4%B4/DCMIOV017.jpeg" alt="DCMI接口整体框图"></p>
<ul>
<li><p>标号1出的是DCMI向外部引出的信号线。DCMI提供的外部接口的方向都是输入的。</p>
<p>  <img src="/2025/03/17/OV2460%E6%91%84%E5%83%8F%E5%A4%B4/DCMIOV02.png" alt="DCMI的信号线说明"></p>
<ul>
<li>其中DCMI_D数据线的数量可选为8、10、12、14位。</li>
</ul>
</li>
<li><p>标号2处，HCLK是由控制器所提供的时钟源，提供给DCMI外设。从DCMI外设引出DCMI_IT中断信号线。并且可以通过DMA_REQ信号发送DMA请求。</p>
</li>
</ul>
<h5 id="DCMI内部结构"><a href="#DCMI内部结构" class="headerlink" title="DCMI内部结构"></a>DCMI内部结构</h5><p><img src="/2025/03/17/OV2460%E6%91%84%E5%83%8F%E5%A4%B4/DCMIOV019.jpeg" alt="DCMI接口内部结构"></p>
<ul>
<li>1、同步器<ul>
<li>同步器主要用于管理DCMI接收数据的时序，它根据外部信号提取输入的数据，也就是OV2640处来的数据。</li>
</ul>
</li>
<li>2、FIFO&#x2F;数据格式化器<ul>
<li>对传输的数据进行管理，DCMI接口提供四个字（4 * 32bit）深度的FIFO，用于缓冲接收的数据。</li>
</ul>
</li>
<li>3、AHB接口<ul>
<li>DCMI挂载在AHB总线上，在AHB总线上有一个DCMI接口的数据寄存器，当我们读取该寄存器是，它会从FIFO中获取数据。</li>
</ul>
</li>
<li>4、控制寄存器<ul>
<li>可以检测状态寄存器来获取DCMI的当前运行状态。</li>
</ul>
</li>
<li>5、DMA接口<ul>
<li>由于DCMI采集的数据量很大，我们一般使用DMA来将采集的数据搬运到内存中。</li>
</ul>
</li>
</ul>
<h5 id="DCMI同步方式"><a href="#DCMI同步方式" class="headerlink" title="DCMI同步方式"></a>DCMI同步方式</h5><p>DCMI支持两种同步方式：</p>
<ul>
<li>硬件同步<ul>
<li>使用HSYNC和VSYNC作为同步信号的的方式。（OV2640就是使用这个方式）</li>
<li><a href="#####DCMI%E7%9A%84%E5%90%8C%E6%AD%A5%E9%97%AE%E9%A2%98"><strong>在DCMI实现中，摄像头与为控制的同步问题需要额外的注意。</strong></a></li>
</ul>
</li>
<li>内嵌码同步方式<ul>
<li>使用数据信号线传输中的特定编码来表示同步信息。</li>
</ul>
</li>
</ul>
<h5 id="DCMI的捕获模式和捕获率"><a href="#DCMI的捕获模式和捕获率" class="headerlink" title="DCMI的捕获模式和捕获率"></a>DCMI的捕获模式和捕获率</h5><p>DCMI还支持两种数据捕获模式，分别为快照模式和连续采集模式。快照模式时只采集一帧的图像数据，连续采集模式会一直采集多个帧的数据， 并且可以通过配置捕获率来控制采集多少数据，如可配置为采集所有数据或隔1帧采集一次数据或隔3帧采集一次数据。</p>
<h5 id="DCMI的同步问题"><a href="#DCMI的同步问题" class="headerlink" title="DCMI的同步问题"></a>DCMI的同步问题</h5><p>HSYNC和VSYNC是两个同步信号，分别代表着新行和新帧的开始。他们的极性必须和OV2640的配置相匹配。</p>
<p>DCMI—PIXCLK是像素时钟，代表着传输数据的速率。</p>
<h5 id="DCMI传输数据"><a href="#DCMI传输数据" class="headerlink" title="DCMI传输数据"></a>DCMI传输数据</h5><ul>
<li>DCMI从OV2640接收到数据，通过并行接口将其暂存在内部FIFO（先进先出）的缓冲区中。</li>
<li>当启用 DMA 并配置好触发条件（如 FIFO 满或半满）时，DCMI 会发起一次 DMA 请求。</li>
<li>DMA会将缓存区中的内容搬运到内存中。</li>
<li>FIFO缓冲区大小为4个字，16个字节，148位。在DCMI外设内部。</li>
</ul>
<h4 id="操作流程"><a href="#操作流程" class="headerlink" title="操作流程"></a>操作流程</h4><ul>
<li>微控制器初始化其I&#x2F;O引脚和通信外设。</li>
<li>然后，微控制器使用SCCB协议配置OV2640，设置分辨率、输出格式（例如，YUV422、RGB565、JPEG）、帧率和图像处理设置等参数。</li>
<li>接下来，微控制器配置DCMI接口以匹配OV2640的输出格式以及时钟&#x2F;同步信号极性。</li>
<li>通常会配置DMA将传入的像素数据从DCMI FIFO传输到指定的内存区域（帧缓冲区）。</li>
<li>摄像头开始流式传输数据，DCMI捕获数据并通过DMA将其传输到内存。</li>
<li>最后，微控制器可以处理帧缓冲区中的图像数据（例如，用于显示、存储或进一步分析）。</li>
</ul>
<h4 id="代码分析"><a href="#代码分析" class="headerlink" title="代码分析"></a>代码分析</h4><h5 id="SCCB代码"><a href="#SCCB代码" class="headerlink" title="SCCB代码"></a>SCCB代码</h5><ul>
<li><p>初始化SCCB</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SCCB_Init(void)</span><br><span class="line">&#123;				</span><br><span class="line">  GPIO_InitTypeDef  GPIO_InitStructure;</span><br><span class="line"></span><br><span class="line">  RCC_AHB1PeriphClockCmd(RCC_AHB1Periph_GPIOD, ENABLE);//使能GPIOD时钟</span><br><span class="line">  //GPIOF9,F10初始化设置</span><br><span class="line">  GPIO_InitStructure.GPIO_Pin = GPIO_Pin_6|GPIO_Pin_7;//PD6,7 推挽输出</span><br><span class="line">  GPIO_InitStructure.GPIO_Mode = GPIO_Mode_OUT;  //PD6,7 推挽输出</span><br><span class="line">  GPIO_InitStructure.GPIO_OType = GPIO_OType_PP;//推挽输出</span><br><span class="line">  GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;//100MHz</span><br><span class="line">  GPIO_InitStructure.GPIO_PuPd = GPIO_PuPd_UP;//上拉</span><br><span class="line">  GPIO_Init(GPIOD, &amp;GPIO_InitStructure);//初始化</span><br><span class="line"> </span><br><span class="line">	GPIO_SetBits(GPIOD,GPIO_Pin_6|GPIO_Pin_7);</span><br><span class="line">	SCCB_SDA_OUT();	   </span><br><span class="line">&#125;	</span><br></pre></td></tr></table></figure>

<ul>
<li>初始化PD6、7为推挽输出。</li>
</ul>
</li>
<li><p>起始信号</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SCCB_Start(void)</span><br><span class="line">&#123;</span><br><span class="line">    SCCB_SDA=1;     //数据线高电平	   </span><br><span class="line">    SCCB_SCL=1;	    //在时钟线高的时候数据线由高至低</span><br><span class="line">    delay_us(50);  </span><br><span class="line">    SCCB_SDA=0;</span><br><span class="line">    delay_us(50);	 </span><br><span class="line">    SCCB_SCL=0;	    //数据线恢复低电平，单操作函数必要	  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>在SIO_C高电平期间，SIO_D由高变为低电平。</li>
</ul>
</li>
<li><p>停止信号</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SCCB_Stop(void)</span><br><span class="line">&#123;</span><br><span class="line">    SCCB_SDA=0;</span><br><span class="line">    delay_us(50);	 </span><br><span class="line">    SCCB_SCL=1;	</span><br><span class="line">    delay_us(50); </span><br><span class="line">    SCCB_SDA=1;	</span><br><span class="line">    delay_us(50);</span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>

<ul>
<li>在SIO_C高电平期间，SIO_D由低变为高电平。</li>
</ul>
</li>
<li><p>don’t care信号</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SCCB_No_Ack(void)</span><br><span class="line">&#123;</span><br><span class="line">	delay_us(50);</span><br><span class="line">	SCCB_SDA=1;	</span><br><span class="line">	SCCB_SCL=1;	</span><br><span class="line">	delay_us(50);</span><br><span class="line">	SCCB_SCL=0;	</span><br><span class="line">	delay_us(50);</span><br><span class="line">	SCCB_SDA=0;	</span><br><span class="line">	delay_us(50);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>写操作</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">u8 SCCB_WR_Byte(u8 dat)</span><br><span class="line">&#123;</span><br><span class="line">	u8 j,res;	 </span><br><span class="line">	for(j=0;j&lt;8;j++) //循环8次发送数据</span><br><span class="line">	&#123;</span><br><span class="line">		if(dat&amp;0x80)SCCB_SDA=1;	</span><br><span class="line">		else SCCB_SDA=0;</span><br><span class="line">		dat&lt;&lt;=1;</span><br><span class="line">		delay_us(50);</span><br><span class="line">		SCCB_SCL=1;	</span><br><span class="line">		delay_us(50);</span><br><span class="line">		SCCB_SCL=0;		   </span><br><span class="line">	&#125;			 </span><br><span class="line">	SCCB_SDA_IN();		//设置SDA为输入 </span><br><span class="line">	delay_us(50);</span><br><span class="line">	SCCB_SCL=1;			//接收第九位,以判断是否发送成功</span><br><span class="line">	delay_us(50);</span><br><span class="line">	if(SCCB_READ_SDA)res=1;  //SDA=1发送失败，返回1</span><br><span class="line">	else res=0;         //SDA=0发送成功，返回0</span><br><span class="line">	SCCB_SCL=0;		 </span><br><span class="line">	SCCB_SDA_OUT();		//设置SDA为输出    </span><br><span class="line">	return res;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>循环发送8位数据。</li>
<li>设置SDA位输入，读取SDA，但是不影响接下来的操作。</li>
</ul>
</li>
<li><p>读操作</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">u8 SCCB_RD_Byte(void)</span><br><span class="line">&#123;</span><br><span class="line">	u8 temp=0,j;    </span><br><span class="line">	SCCB_SDA_IN();		//设置SDA为输入  </span><br><span class="line">	for(j=8;j&gt;0;j--) 	//循环8次接收数据</span><br><span class="line">	&#123;		     	  </span><br><span class="line">		delay_us(50);</span><br><span class="line">		SCCB_SCL=1;</span><br><span class="line">		temp=temp&lt;&lt;1;</span><br><span class="line">		if(SCCB_READ_SDA)temp++;   </span><br><span class="line">		delay_us(50);</span><br><span class="line">		SCCB_SCL=0;</span><br><span class="line">	&#125;	</span><br><span class="line">	SCCB_SDA_OUT();		//设置SDA为输出    </span><br><span class="line">	return temp;</span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>

<ul>
<li>设置SDA为输入。</li>
<li>循环接收数据。</li>
</ul>
</li>
<li><p>写寄存器</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">u8 SCCB_WR_Reg(u8 reg,u8 data)</span><br><span class="line">&#123;</span><br><span class="line">	u8 res=0;</span><br><span class="line">	SCCB_Start(); 					//启动SCCB传输</span><br><span class="line">	if(SCCB_WR_Byte(SCCB_ID))res=1;	//写器件ID	  </span><br><span class="line">	delay_us(100);</span><br><span class="line">  	if(SCCB_WR_Byte(reg))res=1;		//写寄存器地址	  </span><br><span class="line">	delay_us(100);</span><br><span class="line">  	if(SCCB_WR_Byte(data))res=1; 	//写数据	 </span><br><span class="line">  	SCCB_Stop();	  </span><br><span class="line">  	return	res;</span><br><span class="line">&#125;	</span><br></pre></td></tr></table></figure>

<ul>
<li>起始信号。</li>
<li>发送从机地址。</li>
<li>发送数据。</li>
</ul>
</li>
<li><p>读寄存器</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">u8 SCCB_RD_Reg(u8 reg)</span><br><span class="line">&#123;</span><br><span class="line">	u8 val=0;</span><br><span class="line">	SCCB_Start(); 				//启动SCCB传输</span><br><span class="line">	SCCB_WR_Byte(SCCB_ID);		//写器件ID	  </span><br><span class="line">	delay_us(100);	 </span><br><span class="line">  	SCCB_WR_Byte(reg);			//写寄存器地址	  </span><br><span class="line">	delay_us(100);	  </span><br><span class="line">	SCCB_Stop();   </span><br><span class="line">	delay_us(100);	   </span><br><span class="line">	//设置寄存器地址后，才是读</span><br><span class="line">	SCCB_Start();</span><br><span class="line">	SCCB_WR_Byte(SCCB_ID|0X01);	//发送读命令	  </span><br><span class="line">	delay_us(100);</span><br><span class="line">  	val=SCCB_RD_Byte();		 	//读取数据</span><br><span class="line">  	SCCB_No_Ack();</span><br><span class="line">  	SCCB_Stop();</span><br><span class="line">  	return val;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>起始信号</li>
<li>发送从机地址、寄存器地址、停止信号。</li>
<li>发送从机地址和读命令</li>
<li>读取数据。</li>
</ul>
</li>
</ul>
<h5 id="OV2640代码"><a href="#OV2640代码" class="headerlink" title="OV2640代码"></a>OV2640代码</h5><ul>
<li><p>初始化OV2640</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">u8 OV2640_Init(void)</span><br><span class="line">&#123; </span><br><span class="line">	u16 i=0;</span><br><span class="line">	u16 reg;</span><br><span class="line">	//设置IO     	   </span><br><span class="line">  GPIO_InitTypeDef  GPIO_InitStructure;</span><br><span class="line"></span><br><span class="line">  RCC_AHB1PeriphClockCmd(RCC_AHB1Periph_GPIOG, ENABLE);</span><br><span class="line">  //GPIOG9,15初始化设置</span><br><span class="line">  GPIO_InitStructure.GPIO_Pin = GPIO_Pin_9|GPIO_Pin_15;//PG9,15推挽输出</span><br><span class="line">  GPIO_InitStructure.GPIO_Mode = GPIO_Mode_OUT; //推挽输出</span><br><span class="line">  GPIO_InitStructure.GPIO_OType = GPIO_OType_PP;//推挽输出</span><br><span class="line">  GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;//100MHz</span><br><span class="line">  GPIO_InitStructure.GPIO_PuPd = GPIO_PuPd_UP;//上拉</span><br><span class="line">  GPIO_Init(GPIOG, &amp;GPIO_InitStructure);//初始化</span><br><span class="line"> </span><br><span class="line"> 	OV2640_PWDN=0;	//POWER ON</span><br><span class="line">	delay_ms(10);</span><br><span class="line">	OV2640_RST=0;	//复位OV2640</span><br><span class="line">	delay_ms(10);</span><br><span class="line">	OV2640_RST=1;	//结束复位 </span><br><span class="line">  SCCB_Init();        		//初始化SCCB 的IO口	 </span><br><span class="line">	SCCB_WR_Reg(OV2640_DSP_RA_DLMT, 0x01);	//操作sensor寄存器</span><br><span class="line"> 	SCCB_WR_Reg(OV2640_SENSOR_COM7, 0x80);	//软复位OV2640</span><br><span class="line">	delay_ms(50); </span><br><span class="line">	reg=SCCB_RD_Reg(OV2640_SENSOR_MIDH);	//读取厂家ID 高八位</span><br><span class="line">	reg&lt;&lt;=8;</span><br><span class="line">	reg|=SCCB_RD_Reg(OV2640_SENSOR_MIDL);	//读取厂家ID 低八位</span><br><span class="line">	if(reg!=OV2640_MID)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;MID:%d\r\n&quot;,reg);</span><br><span class="line">		return 1;</span><br><span class="line">	&#125;</span><br><span class="line">	reg=SCCB_RD_Reg(OV2640_SENSOR_PIDH);	//读取厂家ID 高八位</span><br><span class="line">	reg&lt;&lt;=8;</span><br><span class="line">	reg|=SCCB_RD_Reg(OV2640_SENSOR_PIDL);	//读取厂家ID 低八位</span><br><span class="line">	if(reg!=OV2640_PID)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;HID:%d\r\n&quot;,reg);</span><br><span class="line">		//return 2;</span><br><span class="line">	&#125;   </span><br><span class="line"> 	//初始化 OV2640,采用SXGA分辨率(1600*1200)  </span><br><span class="line">	for(i=0;i&lt;sizeof(ov2640_sxga_init_reg_tbl)/2;i++)</span><br><span class="line">	&#123;</span><br><span class="line">	   	SCCB_WR_Reg(ov2640_sxga_init_reg_tbl[i][0],ov2640_sxga_init_reg_tbl[i][1]);</span><br><span class="line"> 	&#125; </span><br><span class="line">  	return 0x00; 	//ok</span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>

<ul>
<li>设置GPIO引脚，打开电源和复位摄像头。</li>
<li>初始化SCCB接口。</li>
<li>设置DSP_RA_DLMT 寄存器为0x01，操作传感器寄存器。</li>
<li>读取厂商和产品ID</li>
<li>设置SXGA，设置分辨率输出。<ul>
<li>SXGA是一种分辨率标准，通常定义为1280* 1024.</li>
<li>还有VGA、SVGA、UXGA等。</li>
<li>加载ov2640_sxga_init_reg_tbl表，是配置OV2640的初始捕获率，也就是读取和处理的图像大小。</li>
<li><strong>加载这个表相当于对摄像头进行一次完整的初始化，设定为 1600x1200 的捕获分辨率。</strong></li>
</ul>
</li>
</ul>
</li>
<li><p>设置为jpeg模式</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">oid OV2640_JPEG_Mode(void) </span><br><span class="line">&#123;</span><br><span class="line">	u16 i=0;</span><br><span class="line">	//设置:YUV422格式</span><br><span class="line">	for(i=0;i&lt;(sizeof(ov2640_yuv422_reg_tbl)/2);i++)</span><br><span class="line">	&#123;</span><br><span class="line">		SCCB_WR_Reg(ov2640_yuv422_reg_tbl[i][0],ov2640_yuv422_reg_tbl[i][1]); </span><br><span class="line">	&#125; </span><br><span class="line">	</span><br><span class="line">	//设置:输出JPEG数据</span><br><span class="line">	for(i=0;i&lt;(sizeof(ov2640_jpeg_reg_tbl)/2);i++)</span><br><span class="line">	&#123;</span><br><span class="line">		SCCB_WR_Reg(ov2640_jpeg_reg_tbl[i][0],ov2640_jpeg_reg_tbl[i][1]);  </span><br><span class="line">	&#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>设置为RGB565模式</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void OV2640_RGB565_Mode(void) </span><br><span class="line">&#123;</span><br><span class="line">	u16 i=0;</span><br><span class="line">	//设置:RGB565输出</span><br><span class="line">	for(i=0;i&lt;(sizeof(ov2640_rgb565_reg_tbl)/2);i++)</span><br><span class="line">	&#123;</span><br><span class="line">		SCCB_WR_Reg(ov2640_rgb565_reg_tbl[i][0],ov2640_rgb565_reg_tbl[i][1]); </span><br><span class="line">	&#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>设置图像输出窗口</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void OV2640_Window_Set(u16 sx,u16 sy,u16 width,u16 height)</span><br><span class="line">&#123;</span><br><span class="line">	u16 endx;</span><br><span class="line">	u16 endy;</span><br><span class="line">	u8 temp; </span><br><span class="line">	endx=sx+width/2;	//V*2</span><br><span class="line"> 	endy=sy+height/2;</span><br><span class="line">	</span><br><span class="line">	SCCB_WR_Reg(0XFF,0X01);			</span><br><span class="line">	temp=SCCB_RD_Reg(0X03);				//读取Vref之前的值</span><br><span class="line">	temp&amp;=0XF0;</span><br><span class="line">	temp|=((endy&amp;0X03)&lt;&lt;2)|(sy&amp;0X03);</span><br><span class="line">	SCCB_WR_Reg(0X03,temp);				//设置Vref的start和end的最低2位</span><br><span class="line">	SCCB_WR_Reg(0X19,sy&gt;&gt;2);			//设置Vref的start高8位</span><br><span class="line">	SCCB_WR_Reg(0X1A,endy&gt;&gt;2);			//设置Vref的end的高8位</span><br><span class="line">	</span><br><span class="line">	temp=SCCB_RD_Reg(0X32);				//读取Href之前的值</span><br><span class="line">	temp&amp;=0XC0;</span><br><span class="line">	temp|=((endx&amp;0X07)&lt;&lt;3)|(sx&amp;0X07);</span><br><span class="line">	SCCB_WR_Reg(0X32,temp);				//设置Href的start和end的最低3位</span><br><span class="line">	SCCB_WR_Reg(0X17,sx&gt;&gt;3);			//设置Href的start高8位</span><br><span class="line">	SCCB_WR_Reg(0X18,endx&gt;&gt;3);			//设置Href的end的高8位</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>参数：</p>
<ul>
<li>sx：水平起始坐标</li>
<li>sy：垂直起始坐标</li>
<li>width：窗口宽度</li>
<li>height：窗口高度</li>
</ul>
</li>
<li><p>endx、y是结束坐标。</p>
</li>
<li><p>为什么除去2？</p>
<ul>
<li>OV2640的串口设置寄存器使用一种特殊的表示方法，将其转换为一半的值存储。</li>
</ul>
</li>
<li><p>配置垂直窗口</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SCCB_WR_Reg(0xFF, 0x01);            // 选择传感器寄存器组</span><br><span class="line">temp = SCCB_RD_Reg(0x03);           // 读取 Vref 寄存器的当前值</span><br><span class="line">temp &amp;= 0xF0;                       // 保留高 4 位，清低 4 位</span><br><span class="line">temp |= ((endy &amp; 0x03) &lt;&lt; 2) | (sy &amp; 0x03);  // 设置 Vref 的 start 和 end 低 2 位</span><br><span class="line">SCCB_WR_Reg(0x03, temp);            // 写入更新后的 Vref 值</span><br><span class="line">SCCB_WR_Reg(0x19, sy &gt;&gt; 2);         // 设置 Vref start 的高 8 位</span><br><span class="line">SCCB_WR_Reg(0x1A, endy &gt;&gt; 2);       // 设置 Vref end 的高 8 位</span><br></pre></td></tr></table></figure>

<ul>
<li>清除第四位的值，在向其写入新的垂直的sy、endy值。</li>
<li>Vref（垂直参考）：<ul>
<li>定义读取的起始行（sy）和结束行（endy），控制垂直范围。</li>
<li>10 位分辨率（2 位在 0x03，8 位在 0x19&#x2F;0x1A）。</li>
</ul>
</li>
</ul>
</li>
<li><p>配置水平窗口</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">temp = SCCB_RD_Reg(0x32);           // 读取 Href 寄存器的当前值</span><br><span class="line">temp &amp;= 0xC0;                       // 保留高 2 位，清低 6 位</span><br><span class="line">temp |= ((endx &amp; 0x07) &lt;&lt; 3) | (sx &amp; 0x07);  // 设置 Href 的 start 和 end 低 3 位</span><br><span class="line">SCCB_WR_Reg(0x32, temp);            // 写入更新后的 Href 值</span><br><span class="line">SCCB_WR_Reg(0x17, sx &gt;&gt; 3);         // 设置 Href start 的高 8 位</span><br><span class="line">SCCB_WR_Reg(0x18, endx &gt;&gt; 3);       // 设置 Href end 的高 8 位</span><br></pre></td></tr></table></figure>

<ul>
<li>清除第四位的值，在向其写入新的垂直的sx、endx值。</li>
<li>Href（水平参考）：<ul>
<li>定义读取的起始列（sx）和结束列（endx），控制水平范围。</li>
<li>11 位分辨率（3 位在 0x32，8 位在 0x17&#x2F;0x18）。</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li><p>输出图像大小</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">u8 OV2640_OutSize_Set(u16 width,u16 height)</span><br><span class="line">&#123;</span><br><span class="line">	u16 outh;</span><br><span class="line">	u16 outw;</span><br><span class="line">	u8 temp; </span><br><span class="line">	if(width%4)return 1;</span><br><span class="line">	if(height%4)return 2;</span><br><span class="line">	outw=width/4;</span><br><span class="line">	outh=height/4; </span><br><span class="line">	SCCB_WR_Reg(0XFF,0X00);	</span><br><span class="line">	SCCB_WR_Reg(0XE0,0X04);			</span><br><span class="line">	SCCB_WR_Reg(0X5A,outw&amp;0XFF);		//设置OUTW的低八位</span><br><span class="line">	SCCB_WR_Reg(0X5B,outh&amp;0XFF);		//设置OUTH的低八位</span><br><span class="line">	temp=(outw&gt;&gt;8)&amp;0X03;</span><br><span class="line">	temp|=(outh&gt;&gt;6)&amp;0X04;</span><br><span class="line">	SCCB_WR_Reg(0X5C,temp);				//设置OUTH/OUTW的高位 </span><br><span class="line">	SCCB_WR_Reg(0XE0,0X00);	</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>为何要求 4 的倍数？<ul>
<li>OV2640 的输出数据通常以 4 字节对齐（可能是由于 JPEG 编码或 DMA 传输的需求）。</li>
<li>硬件或 DSP 处理时，要求宽度和高度能被 4 整除，以确保数据块对齐，避免零填充或传输错误。</li>
</ul>
</li>
</ul>
</li>
<li><p>捕获 vs 输出：</p>
<ul>
<li>捕获分辨率：由 OV2640_Window_Set() 设置，定义传感器读取的区域（如 800x600）。</li>
<li>输出分辨率：由 OV2640_OutSize_Set() 设置，定义最终输出的大小（如 320x240）。</li>
</ul>
</li>
</ul>
<h5 id="DCMI外设代码"><a href="#DCMI外设代码" class="headerlink" title="DCMI外设代码"></a>DCMI外设代码</h5><ul>
<li><p>DCMI初始化</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//DCMI初始化</span><br><span class="line">void My_DCMI_Init(void)</span><br><span class="line">&#123;</span><br><span class="line">  GPIO_InitTypeDef  GPIO_InitStructure;</span><br><span class="line">	NVIC_InitTypeDef NVIC_InitStructure;</span><br><span class="line"></span><br><span class="line">	</span><br><span class="line">  RCC_AHB1PeriphClockCmd(RCC_AHB1Periph_GPIOA|RCC_AHB1Periph_GPIOB|RCC_AHB1Periph_GPIOC|RCC_AHB1Periph_GPIOE, ENABLE);//使能GPIOA B C E 时钟</span><br><span class="line">	RCC_AHB2PeriphClockCmd(RCC_AHB2Periph_DCMI,ENABLE);</span><br><span class="line">  //GPIOF9,F10初始化设置</span><br><span class="line">  GPIO_InitStructure.GPIO_Pin = GPIO_Pin_4|GPIO_Pin_6;//PA4/6   复用功能输出</span><br><span class="line">  GPIO_InitStructure.GPIO_Mode = GPIO_Mode_AF; //复用功能输出</span><br><span class="line">  GPIO_InitStructure.GPIO_OType = GPIO_OType_PP;//推挽输出</span><br><span class="line">  GPIO_InitStructure.GPIO_Speed = GPIO_Speed_100MHz;//100MHz</span><br><span class="line">  GPIO_InitStructure.GPIO_PuPd = GPIO_PuPd_UP;//上拉</span><br><span class="line">  GPIO_Init(GPIOA, &amp;GPIO_InitStructure);//初始化</span><br><span class="line">	</span><br><span class="line">  GPIO_InitStructure.GPIO_Pin = GPIO_Pin_7|GPIO_Pin_6;//PA4/6   //PB6/7   复用功能输出</span><br><span class="line">  GPIO_Init(GPIOB, &amp;GPIO_InitStructure);//初始化</span><br><span class="line">	</span><br><span class="line">  GPIO_InitStructure.GPIO_Pin = GPIO_Pin_6|GPIO_Pin_7|GPIO_Pin_8|GPIO_Pin_9|GPIO_Pin_11;//PC6/7/8/9/11 复用功能输出</span><br><span class="line">  GPIO_Init(GPIOC, &amp;GPIO_InitStructure);//初始化	</span><br><span class="line"></span><br><span class="line">  GPIO_InitStructure.GPIO_Pin = GPIO_Pin_5|GPIO_Pin_6;//PE5/6  复用功能输出 </span><br><span class="line">  GPIO_Init(GPIOE, &amp;GPIO_InitStructure);//初始化	</span><br><span class="line"></span><br><span class="line">	GPIO_PinAFConfig(GPIOA,GPIO_PinSource4,GPIO_AF_DCMI); //PA4,AF13  DCMI_HSYNC</span><br><span class="line">	GPIO_PinAFConfig(GPIOA,GPIO_PinSource6,GPIO_AF_DCMI); //PA6,AF13  DCMI_PCLK  </span><br><span class="line"> 	GPIO_PinAFConfig(GPIOB,GPIO_PinSource7,GPIO_AF_DCMI); //PB7,AF13  DCMI_VSYNC </span><br><span class="line"> 	GPIO_PinAFConfig(GPIOC,GPIO_PinSource6,GPIO_AF_DCMI); //PC6,AF13  DCMI_D0  </span><br><span class="line"> 	GPIO_PinAFConfig(GPIOC,GPIO_PinSource7,GPIO_AF_DCMI); //PC7,AF13  DCMI_D1 </span><br><span class="line">	GPIO_PinAFConfig(GPIOC,GPIO_PinSource8,GPIO_AF_DCMI); //PC8,AF13  DCMI_D2</span><br><span class="line">	GPIO_PinAFConfig(GPIOC,GPIO_PinSource9,GPIO_AF_DCMI); //PC9,AF13  DCMI_D3</span><br><span class="line">	GPIO_PinAFConfig(GPIOC,GPIO_PinSource11,GPIO_AF_DCMI);//PC11,AF13 DCMI_D4 </span><br><span class="line">	GPIO_PinAFConfig(GPIOB,GPIO_PinSource6,GPIO_AF_DCMI); //PB6,AF13  DCMI_D5 </span><br><span class="line">	GPIO_PinAFConfig(GPIOE,GPIO_PinSource5,GPIO_AF_DCMI); //PE5,AF13  DCMI_D6</span><br><span class="line">	GPIO_PinAFConfig(GPIOE,GPIO_PinSource6,GPIO_AF_DCMI); //PE6,AF13  DCMI_D7</span><br><span class="line"></span><br><span class="line">	</span><br><span class="line">	DCMI_DeInit();//清除原来的设置 </span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">  DCMI_InitStructure.DCMI_CaptureMode=DCMI_CaptureMode_Continuous;//连续模式</span><br><span class="line">	DCMI_InitStructure.DCMI_CaptureRate=DCMI_CaptureRate_All_Frame;//全帧捕获</span><br><span class="line">	DCMI_InitStructure.DCMI_ExtendedDataMode= DCMI_ExtendedDataMode_8b;//8位数据格式  </span><br><span class="line">	DCMI_InitStructure.DCMI_HSPolarity= DCMI_HSPolarity_Low;//HSYNC 低电平有效</span><br><span class="line">	DCMI_InitStructure.DCMI_PCKPolarity= DCMI_PCKPolarity_Rising;//PCLK 上升沿有效</span><br><span class="line">	DCMI_InitStructure.DCMI_SynchroMode= DCMI_SynchroMode_Hardware;//硬件同步HSYNC,VSYNC</span><br><span class="line">	DCMI_InitStructure.DCMI_VSPolarity=DCMI_VSPolarity_Low;//VSYNC 低电平有效</span><br><span class="line">	DCMI_Init(&amp;DCMI_InitStructure);</span><br><span class="line"></span><br><span class="line">	DCMI_ITConfig(DCMI_IT_FRAME,ENABLE);//开启帧中断 </span><br><span class="line">	</span><br><span class="line">	DCMI_Cmd(ENABLE);	//DCMI使能</span><br><span class="line"></span><br><span class="line">  NVIC_InitStructure.NVIC_IRQChannel = DCMI_IRQn;</span><br><span class="line">	NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority=0;//抢占优先级1</span><br><span class="line">	NVIC_InitStructure.NVIC_IRQChannelSubPriority =0;		//子优先级3</span><br><span class="line">	NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;			//IRQ通道使能</span><br><span class="line">	NVIC_Init(&amp;NVIC_InitStructure);	//根据指定的参数初始化VIC寄存器、</span><br><span class="line"> </span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>

<ul>
<li>配置引脚：PA4、PA6、PB6、PB7、PC6-PC9、PC11、PE5、PE6。</li>
<li>DCMI配置<ul>
<li>捕获模式：连续模式</li>
<li>捕获速率：全帧</li>
<li>数据个数：8位</li>
<li>信号极性<ul>
<li>HSYNC：低电平有效</li>
<li>VSYNC低电平有效</li>
<li>PCLK上升沿有效</li>
</ul>
</li>
<li>同步模式：硬件同步</li>
</ul>
</li>
<li>使能帧中断</li>
<li>配置NVIC</li>
</ul>
</li>
<li><p>DMA配置</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void DCMI_DMA_Init(u32 DMA_Memory0BaseAddr,u32 DMA_Memory1BaseAddr,u16 DMA_BufferSize,u32 DMA_MemoryDataSize,u32 DMA_MemoryInc)</span><br><span class="line">&#123; </span><br><span class="line">	DMA_InitTypeDef  DMA_InitStructure;</span><br><span class="line">	NVIC_InitTypeDef NVIC_InitStructure;</span><br><span class="line">	</span><br><span class="line">  RCC_AHB1PeriphClockCmd(RCC_AHB1Periph_DMA2,ENABLE);//DMA2时钟使能 </span><br><span class="line">	DMA_DeInit(DMA2_Stream1);//等待DMA2_Stream1</span><br><span class="line">	while (DMA_GetCmdStatus(DMA2_Stream1) != DISABLE)&#123;&#125;//等待DMA2_Stream1可配置 </span><br><span class="line">	</span><br><span class="line">  /* 配置 DMA Stream */</span><br><span class="line">  DMA_InitStructure.DMA_Channel = DMA_Channel_1;  //通道1 DCMI通道 </span><br><span class="line">  DMA_InitStructure.DMA_PeripheralBaseAddr = (u32)&amp;DCMI-&gt;DR;//外设地址为:DCMI-&gt;DR</span><br><span class="line">  DMA_InitStructure.DMA_Memory0BaseAddr = DMA_Memory0BaseAddr;//DMA 存储器0地址</span><br><span class="line">  DMA_InitStructure.DMA_DIR = DMA_DIR_PeripheralToMemory;//外设到存储器模式</span><br><span class="line">  DMA_InitStructure.DMA_BufferSize = DMA_BufferSize;//数据传输量 </span><br><span class="line">  DMA_InitStructure.DMA_PeripheralInc = DMA_PeripheralInc_Disable;//外设非增量模式</span><br><span class="line">  DMA_InitStructure.DMA_MemoryInc = DMA_MemoryInc;//存储器增量模式</span><br><span class="line">  DMA_InitStructure.DMA_PeripheralDataSize = DMA_PeripheralDataSize_Word;//外设数据长度:32位</span><br><span class="line">  DMA_InitStructure.DMA_MemoryDataSize = DMA_MemoryDataSize;//存储器数据长度 </span><br><span class="line">  DMA_InitStructure.DMA_Mode = DMA_Mode_Circular;// 使用循环模式 </span><br><span class="line">  DMA_InitStructure.DMA_Priority = DMA_Priority_High;//高优先级</span><br><span class="line">  DMA_InitStructure.DMA_FIFOMode = DMA_FIFOMode_Enable; //FIFO模式        </span><br><span class="line">  DMA_InitStructure.DMA_FIFOThreshold = DMA_FIFOThreshold_Full;//使用全FIFO </span><br><span class="line">  DMA_InitStructure.DMA_MemoryBurst = DMA_MemoryBurst_Single;//外设突发单次传输</span><br><span class="line">  DMA_InitStructure.DMA_PeripheralBurst = DMA_PeripheralBurst_Single;//存储器突发单次传输</span><br><span class="line">  DMA_Init(DMA2_Stream1, &amp;DMA_InitStructure);//初始化DMA Stream</span><br><span class="line">		</span><br><span class="line">	if(DMA_Memory1BaseAddr)</span><br><span class="line">  &#123;</span><br><span class="line">		DMA_DoubleBufferModeCmd(DMA2_Stream1,ENABLE);//双缓冲模式</span><br><span class="line">	  DMA_MemoryTargetConfig(DMA2_Stream1,DMA_Memory1BaseAddr,DMA_Memory_1);//配置目标地址1</span><br><span class="line">		DMA_ITConfig(DMA2_Stream1,DMA_IT_TC,ENABLE);//开启传输完成中断</span><br><span class="line">		</span><br><span class="line">		NVIC_InitStructure.NVIC_IRQChannel = DMA2_Stream1_IRQn;</span><br><span class="line">		NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority=0;//抢占优先级0</span><br><span class="line">		NVIC_InitStructure.NVIC_IRQChannelSubPriority =0;		//子优先级0</span><br><span class="line">		NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;			//IRQ通道使能</span><br><span class="line">		NVIC_Init(&amp;NVIC_InitStructure);	//根据指定的参数初始化VIC寄存器、</span><br><span class="line">	&#125;</span><br><span class="line">		</span><br><span class="line">	</span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>

<ul>
<li>传输方向：（DCMI-DR）外设到存储器</li>
<li>双缓冲模式可选。</li>
<li>启用FIFO模式、阈值为满</li>
</ul>
</li>
<li><p>DCMI的中断服务函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//DCMI中断服务函数</span><br><span class="line">void DCMI_IRQHandler(void)</span><br><span class="line">&#123;</span><br><span class="line">	if(DCMI_GetITStatus(DCMI_IT_FRAME)==SET)//捕获到一帧图像</span><br><span class="line">	&#123;</span><br><span class="line">		jpeg_data_process(); 	//jpeg数据处理	</span><br><span class="line">		DCMI_ClearITPendingBit(DCMI_IT_FRAME);//清除帧中断</span><br><span class="line">		LED1=!LED1;</span><br><span class="line">		ov_frame++;  </span><br><span class="line">	&#125;</span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>

<ul>
<li>当DCMI捕捉到一帧图像时触发。</li>
</ul>
</li>
<li><p>DMA中断服务函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void DMA2_Stream1_IRQHandler(void)</span><br><span class="line">&#123;</span><br><span class="line">    if(DMA_GetFlagStatus(DMA2_Stream1, DMA_FLAG_TCIF1)==SET) // 检查传输完成标志</span><br><span class="line">    &#123;</span><br><span class="line">        DMA_ClearFlag(DMA2_Stream1, DMA_FLAG_TCIF1); // 清除标志</span><br><span class="line">        dcmi_rx_callback(); // 调用回调函数处理数据</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>当传输一帧数据完成时触发。</li>
<li>调用回调函数处理数据。</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>stm32F407</category>
      </categories>
      <tags>
        <tag>stm32F407</tag>
        <tag>多媒体项目</tag>
      </tags>
  </entry>
  <entry>
    <title>TFTLCD</title>
    <url>/2025/03/20/TFTLCD/</url>
    <content><![CDATA[<h1 id="TFTLCD显示"><a href="#TFTLCD显示" class="headerlink" title="TFTLCD显示"></a>TFTLCD显示</h1><h4 id="显示器简介"><a href="#显示器简介" class="headerlink" title="显示器简介"></a>显示器简介</h4><p>显示器属于计算机的I&#x2F;O设备，即输入输出设备。它是一种将特定电子信息输出到屏幕上再反射到人眼的显示工具。常见的有CRT显示器、 液晶显示器、LED点阵显示器及OLED显示器。</p>
<span id="more"></span>

<h4 id="液晶显示器"><a href="#液晶显示器" class="headerlink" title="液晶显示器"></a>液晶显示器</h4><p>液晶显示器，简称LCD。LCD显示器具有功耗低、体积小、承载信息量大以及不伤眼的优点。</p>
<h5 id="1、液晶"><a href="#1、液晶" class="headerlink" title="1、液晶"></a>1、液晶</h5><p>液晶是一种介于固体和液体之间的特殊物质，他是一种有机化合物，常态下呈现液态，到那时它的分子排列和固态晶体的排列方式一样的有规则，所以称为液晶。</p>
<p>如果给液晶施加电场，会改变分子排列，从而改变光线的传播方向，配合着偏振光片，他就具备了控制光线透过率的作用。</p>
<p>在配合着彩色滤光片就可以改变某种颜色的透光量。</p>
<p>通过这种结构和原理，可以做出红、绿、蓝，不同强度的输出光。</p>
<p>把着三种颜色放到一个显示单位，通过不同颜色的强弱可以配置更多的颜色。这样一个显示单位称为像素。</p>
<p><img src="/2025/03/20/TFTLCD/LCD003.jpeg" alt="液晶屏的绿色显示结构"></p>
<h5 id="2、LED和OLED显示器"><a href="#2、LED和OLED显示器" class="headerlink" title="2、LED和OLED显示器"></a>2、LED和OLED显示器</h5><p>LED点阵彩色显示器的单个像素点内包含红绿蓝三色LED灯。通过控制红绿蓝颜色的强度进行混色，实现全彩颜色输出，多个像素点构成一个屏幕。由于每个像素点都是LED灯自发光的， 所以在户外白天也显示得非常清晰，但由于LED灯体积较大，导致屏幕的像素密度低，所以它一般只适合用于广场上的巨型显示器。</p>
<p>OLED显示和LED点阵彩色咸宁市器类似，但是它采用的是像素单元式有机发光二极管，所以像素密集度和普通LED点整显示高。</p>
<p><img src="/2025/03/20/TFTLCD/LCD005.jpeg" alt="OLED像素结构"></p>
<h5 id="3、显示器的基本参数"><a href="#3、显示器的基本参数" class="headerlink" title="3、显示器的基本参数"></a>3、显示器的基本参数</h5><ul>
<li>像素<ul>
<li>像素式组成图像的基本单元要素。</li>
</ul>
</li>
<li>分辨率<ul>
<li>行像素值 * 列像素值：表示屏幕的分辨率。</li>
</ul>
</li>
</ul>
<h2 id="TFTLCD"><a href="#TFTLCD" class="headerlink" title="TFTLCD"></a>TFTLCD</h2><h5 id="1、TFTLCD技术概括"><a href="#1、TFTLCD技术概括" class="headerlink" title="1、TFTLCD技术概括"></a>1、TFTLCD技术概括</h5><p>全称：薄膜晶体管液晶显示器（TFTLCD），因其具有全彩色、高亮度以及支持视频显示的特性，成为了现在显示的主导地位。从消费电子产品到工业设备，TFTLCD的应用范围十分广泛 。为了在基于微控制器的项目中有效地利用TFTLCD，深入理解其工作原理和接口方法至关重要。TFTLCD的普及得益于其成熟的制造工艺，这使得其成本，尤其是在较大尺寸方面，可能相对低于更新的技术，如OLED。随着TFTLCD技术的不断成熟，行业内的重点逐渐转向优化其与微控制器的接口技术，以满足各种特定应用的需求，这推动了多种接口标准的开发和完善。  </p>
<h5 id="2、TFTLCD工作原理"><a href="#2、TFTLCD工作原理" class="headerlink" title="2、TFTLCD工作原理"></a>2、TFTLCD工作原理</h5><p>TFT-LCD是液晶显示器LCD的一种变体，它利用率薄膜晶体管技术（TFT）提高图像显示的质量，例如可寻址性和对比度。与传统的LCD相比较，TFTLCD属于有源矩阵LCD，每个像素由一道四个晶体管控制。液晶材料是LCD的核心，特性是在被施加电厂后，能够改变透光率。</p>
<p>TFT的作用是类似与开关，控制着施加到每个像素的电压。通过精准的控制每个像素的电压，可以产生各种颜色。</p>
<p>有源矩阵寻址技术使得TFTLCD想较于无缘矩阵来说，能够实现更高的分辨率和更快的响应速度。</p>
<h5 id="3、TFTLCD的结构和结构"><a href="#3、TFTLCD的结构和结构" class="headerlink" title="3、TFTLCD的结构和结构"></a>3、TFTLCD的结构和结构</h5><p>TFTLCD的基本结构：</p>
<ul>
<li>背光单元：提供光源</li>
<li>偏光片：用于过滤光线</li>
<li>玻璃基板：上面由TFT薄膜晶体管</li>
<li>另外一个玻璃基板：由彩色滤光片（RGB）</li>
<li>液晶：核心元件，在两个玻璃基板中间</li>
<li>透明电极：例如，氧化铟锡（ITO），用于对液晶施加电场</li>
<li>触摸板：电阻式或者电容式，位于最外层。</li>
</ul>
<p>TFTLCD的特点：</p>
<ul>
<li>显示分辨率：320 * 480</li>
<li>接口使用：8080接口</li>
<li>自带触摸屏，作为输入</li>
</ul>
<h5 id="4、显示控制器的作用"><a href="#4、显示控制器的作用" class="headerlink" title="4、显示控制器的作用"></a>4、显示控制器的作用</h5><p>显示控制器用于控制TFTLCD的运行。主要职责包括接收微控制器发送的命令和数据。为TFT面板生成必要的时序和控制信号。（HSYNC、VSYNC、时钟使能等），通过不断的向LCD发送像素数据以特定速率刷新显示，管理存储显示像素数据的帧缓冲，从内存获取数据，已执行显示相关的功能。</p>
<h5 id="5、微控制器与TFTLCD的接口"><a href="#5、微控制器与TFTLCD的接口" class="headerlink" title="5、微控制器与TFTLCD的接口"></a>5、微控制器与TFTLCD的接口</h5><ul>
<li>TFTLCD模块原理接口图<br>  <img src="/2025/03/20/TFTLCD/142829.png" alt="image-20250321142846299"></li>
<li>TFTLCD采用16位并行方式和外部进行连接。<ul>
<li>为什么不用8位数据线？<ul>
<li>因为TFTLCD要显示的像素数据非常多，如果使用8位数据线进行传输，显示速率会很慢。</li>
</ul>
</li>
</ul>
</li>
<li>TFTLCD使用8080并口接口方式<ul>
<li>什么是8080并行接口？<ul>
<li>8080并行接口是intel 8080微处理器相关的并行通信接口。</li>
</ul>
</li>
<li>8080接口使用的信号线：<ul>
<li>CS：TFTLCD的片选信号</li>
<li>WR：向TFTLCD写入数据</li>
<li>RD：向TFTLCD读取数据</li>
<li>D【0-15】：16位数据线</li>
<li>RST：硬件复位TFTLCD</li>
<li>RS：写数据还是写命令判断</li>
</ul>
</li>
</ul>
</li>
<li>TFTLCD的驱动芯片：ILI9341控制器（例子）<ul>
<li>作用：<ul>
<li>数据处理：接收主机发送的数据或者控制命令；保存接收数据到GRAM。<ul>
<li>GRAM是一个二维内存阵列，可以理解为二维数组。</li>
<li>GRAM每一个地址对应一位像素。</li>
</ul>
</li>
<li>信号生成：控制TFTLCD像素的点亮；通过时序信号。</li>
<li>电源管理：根据数据，生成电压。</li>
</ul>
</li>
<li>自带显存，存储数据为RGB565格式的像素数据。</li>
<li>ILI9341的18根数据线与MPU的16为数据线，以及GRAM的对饮关系<br>  <img src="/2025/03/20/TFTLCD/150050.png"></li>
</ul>
</li>
</ul>
<h5 id="6、TFTLCD的重要命令"><a href="#6、TFTLCD的重要命令" class="headerlink" title="6、TFTLCD的重要命令"></a>6、TFTLCD的重要命令</h5><ul>
<li><p>写命令和写数据共用0-15的16根数据线，但是ILI9341的命令是8位，所以高8位无效。</p>
</li>
<li><p>命令：0XD3</p>
<p>  <img src="/2025/03/20/TFTLCD/150543.png"></p>
<ul>
<li>作用：读取ILI9341的ID信息。</li>
<li>返回：4个字节数据，包含驱动芯片ID</li>
<li>用途：<ul>
<li>验证是否为ILI9341驱动芯片</li>
<li>验证是否正常通信</li>
</ul>
</li>
</ul>
</li>
<li><p>命令：OX36</p>
<p>  <img src="/2025/03/20/TFTLCD/151038.png"></p>
<p>  <img src="/2025/03/20/TFTLCD/151136.png"></p>
<ul>
<li><p>作用：设置内存访问控制参数。</p>
</li>
<li><p>用途：</p>
<ul>
<li>调整图像显示的方向</li>
<li>设置扫描顺序</li>
</ul>
</li>
<li><p>写入参数：（8位i）</p>
<ul>
<li><p>bit7：MY：行地址顺序</p>
<ul>
<li><p>0：从上到下写入GRAM（默认）。</p>
<p>  1：从下到上写入GRAM。</p>
</li>
</ul>
</li>
<li><p>bit6：MX：列地址顺序</p>
<ul>
<li><p>0：从左到右写入GRAM（默认）。</p>
<p>  1：从右到左写入GRAM。</p>
</li>
</ul>
</li>
<li><p>bit5：MV：行&#x2F;列交换</p>
<ul>
<li><p>0：正常模式（行和列不交换）。</p>
<p>  1：行和列交换（实现90°或270°旋转）。</p>
</li>
</ul>
</li>
<li><p>bit4：ML：垂直刷新顺序</p>
<ul>
<li><p>0：从上到下刷新显示。</p>
<p>  1：从下到上刷新显示。</p>
</li>
</ul>
</li>
<li><p>bit3：BGR：RGB-BGR顺序</p>
<ul>
<li>0：RGB颜色顺序（默认）。</li>
<li>1：BGR颜色顺序（红蓝通道交换）。</li>
</ul>
</li>
<li><p>bit2：MH：水平书信顺序</p>
<ul>
<li><p>0：从左到右刷新显示。</p>
<p>  1：从右到左刷新显示。</p>
</li>
</ul>
</li>
<li><p>bit0-1：未使用</p>
</li>
</ul>
</li>
</ul>
</li>
<li><p>命令：0x2A<br>  <img src="/2025/03/20/TFTLCD/152810.png"></p>
<ul>
<li>作用：设置GRAM的列地址范围，定义显示窗口的左右边界</li>
<li>写入参数：4字节<ul>
<li>列起始地址SC</li>
<li>列结束地址EC</li>
</ul>
</li>
</ul>
</li>
<li><p>命令：0x2B<br>  <img src="/2025/03/20/TFTLCD/153224.png"></p>
<ul>
<li>作用：设置页地址方位，定义显示窗口的上下边界</li>
<li>写入参数：4字节<ul>
<li>行起始地址SP</li>
<li>行结束地址EP</li>
</ul>
</li>
</ul>
</li>
<li><p>命令：0x2C<br>  <img src="/2025/03/20/TFTLCD/154139.png"></p>
<ul>
<li>作用：写入GRAM指令。</li>
<li>支持连续写入。</li>
<li>GRAM的地址递增方式按照，MY、MX、MV设置的扫描方向递增。</li>
</ul>
</li>
<li><p>命令：0x2E</p>
<p>  <img src="/2025/03/20/TFTLCD/155057.png"></p>
<ul>
<li>作用：读取GRAM数据。</li>
<li>过程：<ul>
<li>ILI9341接收到0x2E命令。</li>
<li>第一次输出两个字节的无效数据。</li>
<li>第二次输出的两个字节的数据（从坐标SC，SP开始）</li>
<li>每个颜色分量占 8 个位，一次输出 2 个颜色分量。比如：第一次 输出是 R1G1，随后的规律为：B1R2→G2B2→R3G3→B3R4→G4B4→R5G5… 以此类推。如果 我们只需要读取一个点的颜色值，那么只需要接收到参数 3 即可，如果要连续读取（利用 GRAM 地址自增，方法同上），那么就按照上述规律去接收颜色数据。</li>
</ul>
</li>
</ul>
</li>
<li><p>使用流程<br>  <img src="/2025/03/20/TFTLCD/155428.png"></p>
</li>
</ul>
]]></content>
      <categories>
        <category>stm32F407</category>
      </categories>
      <tags>
        <tag>stm32F407</tag>
        <tag>多媒体项目</tag>
      </tags>
  </entry>
  <entry>
    <title>stm32F407HAL库学习-USART串口</title>
    <url>/2025/02/28/stm32F407HAL%E5%BA%93%E5%AD%A6%E4%B9%A0-USART%E4%B8%B2%E5%8F%A3/</url>
    <content><![CDATA[<h1 id="stm32F407HAL库学习-USART串口"><a href="#stm32F407HAL库学习-USART串口" class="headerlink" title="stm32F407HAL库学习-USART串口"></a>stm32F407HAL库学习-USART串口</h1><p>前期准备：</p>
<ul>
<li>stm32F407ZGT6开发板</li>
<li>STM32CubIDE软件</li>
<li>串口：使用USART1，PA9、PA10</li>
</ul>
<span id="more"></span>

<h4 id="1、工程创建"><a href="#1、工程创建" class="headerlink" title="1、工程创建"></a>1、工程创建</h4><ul>
<li><p>1、设置RCC</p>
<ul>
<li><p>设置为告诉外部时钟HSE ，选择外部时钟源</p>
<p>  ![](屏幕截图 2025-02-28 092839.png)</p>
</li>
</ul>
</li>
<li><p>2、设置串口</p>
<ul>
<li><p>点击USART1</p>
</li>
<li><p>设置Mode为<strong>异步通信(Asynchronous)</strong> 。</p>
<ul>
<li>其他模式选项包括：<ul>
<li>Synchronous（同步模式）：用于需要时钟信号的通信，适合与 SPI 或其他同步设备通信。</li>
<li>Single Wire (Half-Duplex)（单线半双工）：使用单根数据线进行双向通信，常见于节省引脚的应用。</li>
<li>Multiprocessor Communication（多处理器通信）：用于多个设备通过同一总线通信。</li>
<li>IrDA（红外数据通信）：用于红外通信。</li>
<li>LIN（本地互联网络）：用于汽车电子领域的低速通信。</li>
<li>SmartCard（智能卡）：用于与智能卡通信。</li>
</ul>
</li>
</ul>
</li>
<li><p>基础参数：<strong>波特率为115200 Bits&#x2F;s。传输数据长度为8 Bit。奇偶检验无，停止位1   接收和发送都使能</strong> </p>
</li>
<li><p>GPIO引脚设置 USART1_RX&#x2F;USART_TX</p>
</li>
<li><p>NVIC Settings 一栏使能接收中断</p>
<p>  ![](屏幕截图 2025-02-28 093134.png)</p>
</li>
</ul>
</li>
<li><p>3、设置时钟</p>
<p>  ![](屏幕截图 2025-02-28 093359.png)</p>
</li>
<li><p>4、生成代码</p>
</li>
</ul>
<h4 id="重新定义Printf函数"><a href="#重新定义Printf函数" class="headerlink" title="重新定义Printf函数"></a>重新定义Printf函数</h4><ul>
<li><p>在main.c文件中加入下面代码。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &quot;stdio.h&quot;</span><br><span class="line"></span><br><span class="line">#ifdef __GNUC__</span><br><span class="line">#define PUTCHAR_PROTOTYPE int __io_putchar(int ch)</span><br><span class="line">#else</span><br><span class="line">#define PUTCHAR_PROTOTYPE int fputc(int ch, FILE *f)</span><br><span class="line">#endif</span><br><span class="line"></span><br><span class="line">PUTCHAR_PROTOTYPE</span><br><span class="line">&#123;</span><br><span class="line">	HAL_UART_Transmit(&amp;huart1, (uint8_t*)&amp;ch,1,HAL_MAX_DELAY);</span><br><span class="line">    return ch;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="中断方式接收数据"><a href="#中断方式接收数据" class="headerlink" title="中断方式接收数据"></a>中断方式接收数据</h4><p>1、USART中断接收的基本原理</p>
<p>USART（通用同步异步收发器）shi为控制器常用的通信模块，通过串口与外部设备进行通信。中断接收是当USART接收到数据时，硬件会触发一个中断，通知CPU执行预定义的回调函数来处理数据。</p>
<p>2、stm32HAL库中的中断接收</p>
<ul>
<li><p>函数原型：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">HAL_StatusTypeDef HAL_UART_Receive_IT(UART_HandleTypeDef *huart, uint8_t *pData, uint16_t Size);</span><br></pre></td></tr></table></figure>

<ul>
<li>huart：UART 句柄，例如 &amp;huart1，指向已初始化的 UART 实例。</li>
<li>pData：接收数据的缓冲区指针。</li>
<li>Size：期望接收的字节数。</li>
</ul>
</li>
<li><p>工作机制：</p>
<ul>
<li>调用HAL_UART_Receive_IT后，硬件会收指定size的数据后触发中断</li>
<li>当中断发生后，HAL库会调用回调函数，HAL_UART_RxCpltCallback。</li>
<li>在回调函数中，可以处理接收到的数据，并重新调用HAL_UART_Receive_IT 继续接收。</li>
</ul>
</li>
</ul>
<p>3、中断接收实现</p>
<p>实现一个基于环形缓冲区的UART中断接收机制。</p>
<ul>
<li><p>初始化</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main(void)</span><br><span class="line">&#123;</span><br><span class="line">    HAL_Init();</span><br><span class="line">    SystemClock_Config();</span><br><span class="line">    MX_GPIO_Init();</span><br><span class="line">    MX_USART1_UART_Init();  // 初始化 USART1</span><br><span class="line">    uint8_t rxBuffer[1] = &#123;0&#125;;</span><br><span class="line">    HAL_UART_Receive_IT(&amp;huart1, rxBuffer, 1);  // 启动中断接收</span><br><span class="line">    while (1)</span><br><span class="line">    &#123;</span><br><span class="line">        while (!UART_RX_IsEmpty()) &#123;</span><br><span class="line">            uint8_t data = UART_RX_GetByte();</span><br><span class="line">            HAL_UART_Transmit(&amp;huart1, &amp;data, 1, 100);  // 回显数据</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>MX_USART1_UART_Init：初始化USART1</li>
<li>HAL_UART_Receive_IT(&amp;huart1, rxBuffer, 1);：启动中断接收，每次接收1字节并存储到rxBuffer中。</li>
</ul>
</li>
<li><p>中断回调处理</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)</span><br><span class="line">&#123;</span><br><span class="line">    if (huart-&gt;Instance == USART1) &#123;</span><br><span class="line">        UART_RX_PutByte(rxBuffer[0]);  // 将接收到的字节存入环形缓冲区</span><br><span class="line">        HAL_UART_Receive_IT(&amp;huart1, rxBuffer, 1);  // 重新启动接收</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>数据储存：将接收的数据存入环形缓冲区。</li>
<li>持续接收</li>
</ul>
</li>
<li><p>环形缓冲区管理</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef struct &#123;</span><br><span class="line">    uint8_t buffer[UART_RX_BUFFER_SIZE];</span><br><span class="line">    volatile uint16_t head;  // 写指针</span><br><span class="line">    volatile uint16_t tail;  // 读指针</span><br><span class="line">&#125; UART_RX_CircularBuffer;</span><br><span class="line"></span><br><span class="line">// 判断缓冲区是否满</span><br><span class="line">bool UART_RX_IsFull(void) &#123;</span><br><span class="line">    return ((uart_rx_buffer.head + 1) % UART_RX_BUFFER_SIZE) == uart_rx_buffer.tail;</span><br><span class="line">&#125;</span><br><span class="line">// 判断缓冲区是否空</span><br><span class="line">bool UART_RX_IsEmpty(void) &#123;</span><br><span class="line">    return (uart_rx_buffer.head == uart_rx_buffer.tail);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 写入一个字节到缓冲区</span><br><span class="line">void UART_RX_PutByte(uint8_t data) &#123;</span><br><span class="line">    if (!UART_RX_IsFull()) &#123;</span><br><span class="line">        uart_rx_buffer.buffer[uart_rx_buffer.head] = data;</span><br><span class="line">        uart_rx_buffer.head = (uart_rx_buffer.head + 1) % UART_RX_BUFFER_SIZE;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 从缓冲区读取一个字节</span><br><span class="line">uint8_t UART_RX_GetByte(void) &#123;</span><br><span class="line">    uint8_t data = 0;</span><br><span class="line">    if (!UART_RX_IsEmpty()) &#123;</span><br><span class="line">        data = uart_rx_buffer.buffer[uart_rx_buffer.tail];</span><br><span class="line">        uart_rx_buffer.tail = (uart_rx_buffer.tail + 1) % UART_RX_BUFFER_SIZE;</span><br><span class="line">    &#125;</span><br><span class="line">    return data;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>环形缓冲区：通过head和tail指针实现循环使用，避免数据覆盖。</p>
</li>
<li><p>中断安全：head和tail使用volation修饰，请避免了错误</p>
</li>
<li><p><strong>UART_RX_IsFull</strong> ：判断缓冲区是否已满。</p>
</li>
<li><p><strong>UART_RX_IsEmpty</strong>：判断缓冲区是否已空</p>
</li>
<li><p><strong>UART_RX_PutByte</strong>：写入一个字节到缓冲区，如果未满，则写入head当前的下一个位置，并且用%的方法来循环写入缓冲区。</p>
</li>
<li><p><strong>UART_RX_GetByte</strong>：读取一个字节到缓冲区，如果未满，则读取tail当前的下一个位置，并且用%的方法来循环读取缓冲区。</p>
</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>stm32F407</category>
      </categories>
      <tags>
        <tag>stm32</tag>
        <tag>stm32cubide</tag>
        <tag>HAL</tag>
        <tag>USART</tag>
      </tags>
  </entry>
  <entry>
    <title>FreeRTOS学习</title>
    <url>/2025/03/23/FreeRTOS%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<h1 id="FreeRTOS学习"><a href="#FreeRTOS学习" class="headerlink" title="FreeRTOS学习"></a>FreeRTOS学习</h1><p>​	以下内容根据刀哥的文档，进行学习和总结。</p>
<pre><code>### FreeRTOS概述
</code></pre>
<p><img src="/2025/03/23/FreeRTOS%E5%AD%A6%E4%B9%A0/113615.png"></p>
<ul>
<li>如图，是我们从官网下载的FreeRTOS的文件及其每个文件的讲解。</li>
<li>移植到工程中，暂时不在这里讲，其他地方有讲，可以进行搜索。</li>
<li>主要的文件是：<ul>
<li>Demo<ul>
<li>存在着，在不同系统上的示例工程。</li>
</ul>
</li>
<li>source<br>  <img src="/2025/03/23/FreeRTOS%E5%AD%A6%E4%B9%A0/114209.png"><ul>
<li>根目录的和兴文件。</li>
</ul>
</li>
</ul>
</li>
<li><img src="/2025/03/23/FreeRTOS%E5%AD%A6%E4%B9%A0/114321.png"></li>
</ul>
<span id="more"></span>

<h3 id="编程规范"><a href="#编程规范" class="headerlink" title="编程规范"></a>编程规范</h3><ul>
<li><p>TickType_t：</p>
<ul>
<li><p>TickType_t是一个无符号整数类型，用于系统节拍（tick）的计数。</p>
</li>
<li><p>在FreeRTOS中配置着一个周期性的中断：Tick Interrupt。</p>
</li>
<li><p>每次发生中断，中断有一个TICK_Count计数累加。FreeRTOS 启动以来发生的系统时钟节拍的总数。</p>
</li>
<li><p>TICK_Count变量的类型是TickType_t类型的。</p>
</li>
<li><p>TickType_t可能是uint16-t或者uint32-t的。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">在32位架构中，我们一般设置为uint32-t。</span><br><span class="line"></span><br><span class="line">FreeRTOSConfig.h</span><br><span class="line">#define configUSE_16_BIT_TICKS					0                       //系统节拍计数器变量数据类型，</span><br><span class="line"></span><br><span class="line">portmacro.h</span><br><span class="line">#if( configUSE_16_BIT_TICKS == 1 )</span><br><span class="line">	typedef uint16_t TickType_t;</span><br><span class="line">	#define portMAX_DELAY ( TickType_t ) 0xffff</span><br><span class="line">#else</span><br><span class="line">	typedef uint32_t TickType_t;</span><br><span class="line">	#define portMAX_DELAY ( TickType_t ) 0xffffffffUL</span><br></pre></td></tr></table></figure>

</li>
<li><p>时间单位：每个系统节拍维持时间由configTICK_RATE_HZ这个宏来定义。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">FreeRTOSConfig.h</span><br><span class="line">#define configTICK_RATE_HZ						(1000)                  //时钟节拍频率，也就是每秒的时钟节拍数，这里设置为1000，周期就是1ms，1秒钟中1000次节拍，1ms一次节拍</span><br><span class="line"></span><br><span class="line">portmacro.h</span><br><span class="line">#define portTICK_PERIOD_MS			( ( TickType_t ) 1000 / configTICK_RATE_HZ )</span><br><span class="line">1000表示1000ms</span><br><span class="line">1000 / configTICK_RATE_HZ：表示每个节拍维持的时间为1ms。</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>BaseType_t</p>
<ul>
<li><p>一个基本整数类型，通常用于布尔值或者函数的调用状态。</p>
</li>
<li><p>在32位架构中，他是uint32-t类型。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">portmacro.h</span><br><span class="line">typedef long BaseType_t;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>变量名前缀：<br>  <img src="/2025/03/23/FreeRTOS%E5%AD%A6%E4%B9%A0/121040.png"></p>
</li>
<li><p>函数名前缀<br>  <img src="/2025/03/23/FreeRTOS%E5%AD%A6%E4%B9%A0/121236.png"></p>
</li>
<li><p>宏的前缀<br>  <img src="/2025/03/23/FreeRTOS%E5%AD%A6%E4%B9%A0/121346.png"></p>
</li>
</ul>
<h3 id="内存管理"><a href="#内存管理" class="headerlink" title="内存管理"></a>内存管理</h3><ul>
<li>task、queue、semaphores、event group等，通常使用的是动态分配：用时分配，不用时释放。</li>
<li>在C语言库函数中，由malloc、free函数，但是在FreeRTOS中不适用：<ul>
<li>不适合用在资源紧缺的嵌入式系统中。</li>
<li>可能会造成内存碎片化</li>
<li>函数实现过于复杂，占用太多代码空间。</li>
</ul>
</li>
<li>在FreeRTOS由专门的内存管理文件：<br>  <img src="/2025/03/23/FreeRTOS%E5%AD%A6%E4%B9%A0/121941.png"><ul>
<li>heap_1.c文件<ul>
<li>只分配，不释放：在代码中只实现了pvPortMalloc函数，没有实现vPortFree函数。</li>
<li>原理：先定义一个大数组，调用pvPortMalloc时，在数组中分配空间。</li>
</ul>
</li>
<li>heap_2.c文件<ul>
<li>可分配，可释放：在函数中实现了pvPortMalloc和vPortFree。</li>
<li>采用了最佳匹配算法来分配空间。</li>
<li>碎片化：他不会合并空闲相邻的内存，会造成严重的碎片化问题。</li>
<li>速度：比heap1分配速度要慢，但是比C库函数malloc、free速度要快。</li>
</ul>
</li>
<li>heap_3.c文件<ul>
<li>直接使用C库中malloc、free函数。</li>
<li><strong>不是线程安全的</strong>。标准库的 <code>malloc()</code> 和 <code>free()</code> 通常不是为多任务环境设计的，可能存在竞态条件。因此，FreeRTOS 会在调用这些函数时禁用调度器，以确保线程安全，但这可能会影响实时性。</li>
</ul>
</li>
<li>heap_4.c文件<ul>
<li>同样使用大数组分配内存。</li>
<li>合并空闲内存：heap4使用first fit来分配内存，并且可以将相邻的空闲内存块合并。</li>
</ul>
</li>
<li>heap_5.c文件<ul>
<li>heap_5: 分配内存、释放内存的算法跟heap_4相同。</li>
<li>heap_5不在局限于在一个大的数组中进行内存管理。允许从<strong>多个不连续的内存区域</strong>分配内存。</li>
</ul>
</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>FreeRTOS</category>
      </categories>
      <tags>
        <tag>stm32学习</tag>
        <tag>操作系统</tag>
        <tag>FreeRTOS</tag>
      </tags>
  </entry>
  <entry>
    <title>stm32F407HAL库学习-点亮LED灯</title>
    <url>/2025/02/27/stm32F407HAL%E5%BA%93%E5%AD%A6%E4%B9%A0-%E7%82%B9%E4%BA%AELED%E7%81%AF/</url>
    <content><![CDATA[<h1 id="stm32F407HAL库学习-点亮LED灯"><a href="#stm32F407HAL库学习-点亮LED灯" class="headerlink" title="stm32F407HAL库学习-点亮LED灯"></a>stm32F407HAL库学习-点亮LED灯</h1><p>前期准备：</p>
<ul>
<li>stm32F407ZGT6开发板</li>
<li>stm32cubeIDE软件</li>
<li>STM32F4xxHAL库</li>
</ul>
<span id="more"></span>

<h3 id="1、创建工程"><a href="#1、创建工程" class="headerlink" title="1、创建工程"></a>1、创建工程</h3><p>我从这开始想学习使用stm32CubIDE工具来写代码。所以这算一个新的教程。</p>
<p>1、在双击stm32CubIDE工具进入到主界面中，点击左上角file-&gt;NEW-&gt;stm32_Prject.</p>
<p><img src="/2025/02/27/stm32F407HAL%E5%BA%93%E5%AD%A6%E4%B9%A0-%E7%82%B9%E4%BA%AELED%E7%81%AF/143658.png"></p>
<p>然后计入到选择MCU的界面。</p>
<p><img src="/2025/02/27/stm32F407HAL%E5%BA%93%E5%AD%A6%E4%B9%A0-%E7%82%B9%E4%BA%AELED%E7%81%AF/144215.png"></p>
<p>在这个中，有一个Part Number Search的一行，下面有个搜索的图标。在方框中输入型号。我这次使用的是stm32F407ZGT6.下面图片中不一样，是我截取网上的图片。</p>
<p><img src="/2025/02/27/stm32F407HAL%E5%BA%93%E5%AD%A6%E4%B9%A0-%E7%82%B9%E4%BA%AELED%E7%81%AF/144547.png"></p>
<p>选中你的芯片型号只有，点击右下角的Next，然后进入到创建项目的界面，如下图。</p>
<p><img src="/2025/02/27/stm32F407HAL%E5%BA%93%E5%AD%A6%E4%B9%A0-%E7%82%B9%E4%BA%AELED%E7%81%AF/144655.png"></p>
<p>在上面界面中 的Project Name这行输入名称，这个名称就是你项目的名称，这里我们输入LED_Project.完成后，点击下面的完成。</p>
<p>然后进入了硬件配置的界面。</p>
<p><img src="/2025/02/27/stm32F407HAL%E5%BA%93%E5%AD%A6%E4%B9%A0-%E7%82%B9%E4%BA%AELED%E7%81%AF/145446.png"></p>
<p>这里比较重要详细讲一下，分为三大块。</p>
<ul>
<li><p><strong>1、MCU外设资源的选择。</strong> </p>
<p>  提供了三种方法找到外设。可以直接搜索或者通过字母顺序的查找。还可以在大的分类里面找。</p>
</li>
<li><p><strong>2、外设配置</strong> </p>
<p>  选择好外设后，在这里构建好外设的功能。</p>
</li>
<li><p><strong>3、预览界面</strong> </p>
<p>  这里分为引脚预览和系统预览。</p>
</li>
</ul>
<h4 id="2、配置引脚"><a href="#2、配置引脚" class="headerlink" title="2、配置引脚"></a>2、配置引脚</h4><p>工程已经创建好了，下面开始配置GPIO引脚和一些必要的外设配置。</p>
<h5 id="1、配置GPIO引脚用于LED"><a href="#1、配置GPIO引脚用于LED" class="headerlink" title="1、配置GPIO引脚用于LED"></a>1、配置GPIO引脚用于LED</h5><ul>
<li><p>在 STM32CubeMX 界面中，点击左侧的 Pinout &amp; Configuration 标签，进入芯片引脚配置视图。</p>
</li>
<li><p>在芯片引脚图上找到PF9、pF10，点击它。</p>
</li>
<li><p>在弹出的选项中，选择 GPIO_Output，表示将该引脚配置为通用输出模式。</p>
<p>  <img src="/2025/02/27/stm32F407HAL%E5%BA%93%E5%AD%A6%E4%B9%A0-%E7%82%B9%E4%BA%AELED%E7%81%AF/152227.png"></p>
</li>
<li><p>左侧会显示 GPIO 选项，点击它。</p>
</li>
<li><p>在 GPIO 配置窗口中，找到 PF9、PF10，可以设置以下参数：</p>
<ul>
<li>GPIO mode: Output Push Pull（推挽输出）</li>
<li>GPIO Pull-up&#x2F;Pull-down: No pull-up and no pull-down（无上拉&#x2F;下拉）</li>
<li>Maximum output speed: Low（低速即可，LED 不需要高速）</li>
<li>User Label: 输入 LED（可选，方便代码中识别）。</li>
</ul>
</li>
</ul>
<h5 id="2、配置时钟"><a href="#2、配置时钟" class="headerlink" title="2、配置时钟"></a>2、配置时钟</h5><ul>
<li><p>点击顶部的 Clock Configuration 选项卡。</p>
</li>
<li><p>如果你使用外部晶振（HSE，例如 8 MHz，常见于发现板），在 HSE 输入框中选择 Crystal&#x2F;Ceramic Resonator。</p>
</li>
<li><p>默认情况下，STM32CubeMX 会自动配置 PLL（锁相环）以达到较高频率（例如 168 MHz，这是 STM32F407 的最大主频）。</p>
<p>  在 System Clock 输入框中，确认频率（比如 168 MHz），或者根据需要调整。</p>
<p>  点击 Resolve Clock Issues 按钮（如果有提示），让工具自动优化时钟树。</p>
</li>
</ul>
<h5 id="3、生成代码"><a href="#3、生成代码" class="headerlink" title="3、生成代码"></a>3、生成代码</h5><p>配置完成后按下ctrl + S ,保存，就会生成初始化代码。</p>
<h4 id="3、编写代码"><a href="#3、编写代码" class="headerlink" title="3、编写代码"></a>3、编写代码</h4><ul>
<li><p>在工程目录的 Core &gt; Src &gt; main.c 中打开 main.c。</p>
</li>
<li><p>在 main() 函数中，找到 while (1) 循环，这是主循环区域。</p>
</li>
<li><p>你会看到类似下面的代码框架：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main(void) &#123;</span><br><span class="line">  /* 初始化代码已由 CubeMX 生成 */</span><br><span class="line">  HAL_Init();</span><br><span class="line">  SystemClock_Config();</span><br><span class="line">  MX_GPIO_Init();</span><br><span class="line">  </span><br><span class="line">  /* USER CODE BEGIN 2 */</span><br><span class="line">  /* USER CODE END 2 */</span><br><span class="line"></span><br><span class="line">  while (1) &#123;</span><br><span class="line">    /* USER CODE BEGIN 3 */</span><br><span class="line">    /* USER CODE END 3 */</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>添加 LED 闪烁代码：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* USER CODE BEGIN 3 */</span><br><span class="line">HAL_GPIO_TogglePin(GPIOD, GPIO_PIN_12); // 翻转 PD12 引脚状态</span><br><span class="line">HAL_Delay(500);                        // 延时 500 毫秒</span><br><span class="line">/* USER CODE END 3 */</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="4、编译并下载"><a href="#4、编译并下载" class="headerlink" title="4、编译并下载"></a>4、编译并下载</h4><ol>
<li>编译代码：<ul>
<li>点击 STM32CubeIDE 顶部工具栏的“锤子”图标（Build），或者按 Ctrl + B。</li>
<li>检查输出窗口，确保没有错误。</li>
</ul>
</li>
<li>连接硬件：<ul>
<li>将 STM32F407ZGT6 开发板通过调试器（比如 ST-Link）连接到电脑。</li>
</ul>
</li>
<li>下载程序：<ul>
<li>点击“绿色播放”按钮（Run），或者选择菜单 Run &gt; Run。</li>
<li>程序会下载到芯片并自动运行。</li>
</ul>
</li>
</ol>
<h3 id="复习一遍GPIO的八种模式"><a href="#复习一遍GPIO的八种模式" class="headerlink" title="复习一遍GPIO的八种模式"></a>复习一遍GPIO的八种模式</h3><h4 id="1、输入模式"><a href="#1、输入模式" class="headerlink" title="1、输入模式"></a>1、输入模式</h4><h5 id="1、输入浮空模式（Input-Floating）"><a href="#1、输入浮空模式（Input-Floating）" class="headerlink" title="1、输入浮空模式（Input Floating）"></a>1、输入浮空模式（Input Floating）</h5><ul>
<li>原理：GPIO引脚不接受任何内部的上拉和下拉电阻，处于悬浮状态。它的电平状态完全由外部信号决定。</li>
<li>用途：适用于外部电路已经提供了明确的高低电平信号。</li>
<li>注意：如果外部不接任何东西，电平会不稳定，容易受到干扰。</li>
</ul>
<h5 id="2、输入上拉模式（Input-Pull-Up）"><a href="#2、输入上拉模式（Input-Pull-Up）" class="headerlink" title="2、输入上拉模式（Input Pull-Up）"></a>2、输入上拉模式（Input Pull-Up）</h5><ul>
<li>原理：GPIO引脚内部连接一个上拉电阻，连接3.3v电源。如果外部没有信号，则默认处于高点平。</li>
<li>用途：常用于检测低电平触发信号，比如按键按下时，引脚从高变为低。</li>
<li>注意：上拉电阻通常比较弱，外部信号足够强的话可以拉低电平。</li>
</ul>
<h5 id="3、输入下拉模式（Input-Pull-Down）"><a href="#3、输入下拉模式（Input-Pull-Down）" class="headerlink" title="3、输入下拉模式（Input Pull-Down）"></a>3、输入下拉模式（Input Pull-Down）</h5><ul>
<li>原理：GPIO引脚在内部连接一个下拉电阻，连接着地。如果外部没有信号，则默认处于低电平。</li>
<li>用途：常用于检测一个高电平信号，当一个传感器输出高电平时触发。</li>
<li>注意：和上拉电阻一样，电阻很弱，当外部信号足够强时可以改变其电平</li>
</ul>
<h5 id="4、模拟输入模式（Analog-Input）"><a href="#4、模拟输入模式（Analog-Input）" class="headerlink" title="4、模拟输入模式（Analog Input）"></a>4、模拟输入模式（Analog Input）</h5><ul>
<li>原理：GPIO不在做数字信号处理，直接作为模拟信号输入，连接内部ADC。</li>
<li>用途：测量连续变化的电压值，比如温度传感器</li>
<li>注意：这种模式下，引脚不能用于输入输入输出，只能给模拟电路用。</li>
</ul>
<h4 id="2、输出模式"><a href="#2、输出模式" class="headerlink" title="2、输出模式"></a>2、输出模式</h4><h5 id="5、开漏输出模式（Open-Drain-Output）"><a href="#5、开漏输出模式（Open-Drain-Output）" class="headerlink" title="5、开漏输出模式（Open-Drain Output）"></a>5、开漏输出模式（Open-Drain Output）</h5><ul>
<li>原理：GPIO引脚只能输出低电平，高电平处于开路状态，需要外部上拉电阻拉高。</li>
<li>用途：常用于IIC、SMBus等通信协议。</li>
<li>注意：必须外部连接上拉电阻，否则高电平无法实现。</li>
</ul>
<h5 id="6、推挽输出模式（Push-Pull-Output）"><a href="#6、推挽输出模式（Push-Pull-Output）" class="headerlink" title="6、推挽输出模式（Push-Pull Output）"></a>6、推挽输出模式（Push-Pull Output）</h5><ul>
<li>原理：GPIO可以主动输出高电平和低电平，由内部电路直接驱动。</li>
<li>用途：最常用的输出模式</li>
<li>注意：驱动能力强，但是不能对各引脚直接并联输出。会短路。</li>
</ul>
<h5 id="7、复用功能模式（Alternate-Function）"><a href="#7、复用功能模式（Alternate-Function）" class="headerlink" title="7、复用功能模式（Alternate Function）"></a>7、复用功能模式（Alternate Function）</h5><ul>
<li>原理：GPIO引脚配置为硬件外设功能，比如USART的TX、RX等。</li>
<li>用途：用于特定的外设功能。例如串口通信</li>
<li>注意：配置时需要指定其具体的功能。</li>
</ul>
<h5 id="8、复用开漏、推挽模式（Alternate-Function-Open-Drain-Push-Pull）"><a href="#8、复用开漏、推挽模式（Alternate-Function-Open-Drain-Push-Pull）" class="headerlink" title="8、复用开漏、推挽模式（Alternate Function Open-Drain&#x2F;Push-Pull）"></a>8、复用开漏、推挽模式（Alternate Function Open-Drain&#x2F;Push-Pull）</h5><ul>
<li>原理：结合复用功能和开漏、推挽输出。例如IIC用开漏，PWM用推挽。</li>
<li>用途：根据外设需求选择输出类型，比如I2C需要开漏，PWM需要推挽。</li>
<li>注意：具体模式取决于外设协议要求。</li>
</ul>
]]></content>
      <categories>
        <category>stm32F407</category>
      </categories>
      <tags>
        <tag>stm32</tag>
        <tag>stm32cubide</tag>
        <tag>HAL</tag>
      </tags>
  </entry>
  <entry>
    <title>ubuntu根文件系统移植</title>
    <url>/2025/02/20/ubuntu%E6%A0%B9%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%A7%BB%E6%A4%8D/</url>
    <content><![CDATA[<h1 id="ubuntu根文件系统移植"><a href="#ubuntu根文件系统移植" class="headerlink" title="ubuntu根文件系统移植"></a>ubuntu根文件系统移植</h1><p>现在有更好的教程了。还是两个结合的看把。</p>
<p><a href="https://blog.csdn.net/weixin_51963312/article/details/142416613?ops_request_misc=&request_id=&biz_id=102&utm_term=%E5%B0%86ubuntu%E7%A7%BB%E6%A4%8D%E5%88%B0stm32mp157%E5%BC%80%E5%8F%91%E6%9D%BF%E4%B8%8A%E3%80%82&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduweb~default-1-142416613.142%5Ev101%5Epc_search_result_base8&spm=1018.2226.3001.4187">https://blog.csdn.net/weixin_51963312/article/details/142416613?ops_request_misc=&amp;request_id=&amp;biz_id=102&amp;utm_term=%E5%B0%86ubuntu%E7%A7%BB%E6%A4%8D%E5%88%B0stm32mp157%E5%BC%80%E5%8F%91%E6%9D%BF%E4%B8%8A%E3%80%82&amp;utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduweb~default-1-142416613.142^v101^pc_search_result_base8&amp;spm=1018.2226.3001.4187</a></p>
<h3 id="移植前的准备"><a href="#移植前的准备" class="headerlink" title="移植前的准备"></a>移植前的准备</h3><p>在移植之前，我们首先要在电脑上安装Ubuntu。在ubuntu中的终端操作下面步骤。</p>
<p>在移植之前我们需要一个辅助工具，用来对根文件系统进行一个基本的搭建，就能模拟出根文件系统运行的状态。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo apt-get install qemu-user-static</span><br></pre></td></tr></table></figure>

<p>其中qemu-user-static安装包的作用时一个QEMU的一个子模块。QEMU是一个开源的虚拟化和模拟器的工具。而子模块专注于用户模式模拟，允许你在当前主机架构上运行另一个架构的二进制文件，而无需启动完整的虚拟机。</p>
<span id="more"></span>

<h3 id="下载根文件系统"><a href="#下载根文件系统" class="headerlink" title="下载根文件系统"></a>下载根文件系统</h3><p>ubuntu以提供了基本的可移植到嵌入式的根文件系统，因此我们去官网下载即可。</p>
<p><a href="http://cdimage.ubuntu.com/">http://cdimage.ubuntu.com/</a></p>
<p>点击网址后进入网站，点击红色标注的选项。</p>
<p><img src="/2025/02/20/ubuntu%E6%A0%B9%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%A7%BB%E6%A4%8D/140807.png">S</p>
<p>点击后，进入下面界面，继续点击。</p>
<p><img src="/2025/02/20/ubuntu%E6%A0%B9%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%A7%BB%E6%A4%8D/141134.png"></p>
<p>然后选择其中一个版本，点击下载。</p>
<p><img src="/2025/02/20/ubuntu%E6%A0%B9%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%A7%BB%E6%A4%8D/141337.png"></p>
<p><img src="/2025/02/20/ubuntu%E6%A0%B9%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%A7%BB%E6%A4%8D/141435.png"></p>
<p>下载好安装包，等会要用。</p>
<h3 id="开始移植"><a href="#开始移植" class="headerlink" title="开始移植"></a>开始移植</h3><h4 id="1、在根目录中创建一个文件。"><a href="#1、在根目录中创建一个文件。" class="headerlink" title="1、在根目录中创建一个文件。"></a>1、在根目录中创建一个文件。</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mkdir rootfs</span><br></pre></td></tr></table></figure>

<h4 id="2、解压刚才下载的安装包，到rootfs文件中。"><a href="#2、解压刚才下载的安装包，到rootfs文件中。" class="headerlink" title="2、解压刚才下载的安装包，到rootfs文件中。"></a>2、解压刚才下载的安装包，到rootfs文件中。</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo tar -vzxf ubuntu-base-16.04.6-base-armhf.tar.gz </span><br></pre></td></tr></table></figure>

<h4 id="3、复制之前下载的qemu-user-static其中某些文件到刚刚解压的根文件系统中。"><a href="#3、复制之前下载的qemu-user-static其中某些文件到刚刚解压的根文件系统中。" class="headerlink" title="3、复制之前下载的qemu-user-static其中某些文件到刚刚解压的根文件系统中。"></a>3、复制之前下载的qemu-user-static其中某些文件到刚刚解压的根文件系统中。</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">h@h-virtual-machine:~/h/rootfs$ sudo cp /usr/bin/qemu-arm-static ./usr/bin/</span><br><span class="line">h@h-virtual-machine:~/h/rootfs$ sudo cp /etc/resolv.conf ./etc/resolv.conf</span><br><span class="line">将主机的DNS配置文件（/etc/resolv.conf）复制到根文件系统的/etc/resolv.conf。</span><br><span class="line">根文件系统可以解析域名（例如访问apt源），从而正常联网。</span><br></pre></td></tr></table></figure>

<p>复制完毕后，进入到resolv.conf文件中，修改其中的内容。保存退出。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">h@h-virtual-machine:~/h/rootfs$ sudo vim /etc/resolv.conf</span><br></pre></td></tr></table></figure>

<p>修改后的resolv.conf文件中界面如下：<br><strong>nameserver 202.96.128.86。</strong> </p>
<p>202.96.128.86是电脑网络的DNS服务器端口。配置成功后可以通过下面测试网络</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ping www.baidu.com</span><br></pre></td></tr></table></figure>

<ul>
<li>如果能解析并ping，说明DNS配置成功。</li>
</ul>
<h4 id="4、挂载VFS"><a href="#4、挂载VFS" class="headerlink" title="4、挂载VFS"></a>4、挂载VFS</h4><ul>
<li>VFS是虚拟的文件系统，用于统一管理各种文件系统。</li>
<li>挂载VFS是为了根文件系统太难记必要的虚拟文件系统支持。</li>
</ul>
<h5 id="1、创建一个mk-sh的脚本。"><a href="#1、创建一个mk-sh的脚本。" class="headerlink" title="1、创建一个mk.sh的脚本。"></a>1、创建一个mk.sh的脚本。</h5><p>在rootfs的父目录中创建一个名字为mk.sh的脚本文件，并修改其的权限。在文件中添加下面代码。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">h@h-virtual-machine:~/h$ touch mk.sh</span><br><span class="line">h@h-virtual-machine:~/h$ chomd 777 mk.sh</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#!/bin/bash</span><br><span class="line">mnt () </span><br><span class="line">&#123;</span><br><span class="line">    echo &quot;MOUNTING&quot;</span><br><span class="line">    sudo mount -t proc /proc $&#123;2&#125;proc</span><br><span class="line">    sudo mount -t sysfs /sys $&#123;2&#125;sys</span><br><span class="line">    sudo mount -o bind /dev $&#123;2&#125;dev</span><br><span class="line">    sudo mount -o bind /dev/pts $&#123;2&#125;dev/pts      </span><br><span class="line">    sudo chroot $&#123;2&#125;    </span><br><span class="line">&#125;</span><br><span class="line">umnt ()</span><br><span class="line">&#123;</span><br><span class="line">    echo &quot;UNMOUNTING&quot;</span><br><span class="line">    sudo umount $&#123;2&#125;proc</span><br><span class="line">    sudo umount $&#123;2&#125;sys</span><br><span class="line">    sudo umount $&#123;2&#125;dev/pts</span><br><span class="line">    sudo umount $&#123;2&#125;dev </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">if [ &quot;$1&quot; = &quot;-m&quot; ] &amp;&amp; [ -n &quot;$2&quot; ];</span><br><span class="line">then</span><br><span class="line">    mnt $1 $2</span><br><span class="line">    echo &quot;mnt -m pwd&quot;</span><br><span class="line">elif [ &quot;$1&quot; = &quot;-u&quot; ] &amp;&amp; [ -n &quot;$2&quot; ];</span><br><span class="line">then</span><br><span class="line">    umnt $1 $2</span><br><span class="line">    echo &quot;mnt -u pwd&quot;</span><br><span class="line">else</span><br><span class="line">    echo &quot;&quot;</span><br><span class="line">    echo &quot;Either 1&#x27;st, 2&#x27;nd or bothparameters were missing&quot;</span><br><span class="line">    echo &quot;&quot;</span><br><span class="line">    echo &quot;1&#x27;st parameter can be one ofthese: -m(mount) OR -u(umount)&quot;</span><br><span class="line">    echo &quot;2&#x27;nd parameter is the rootfs path&quot;</span><br><span class="line">    echo &quot;&quot;</span><br><span class="line">    echo &quot;For example: ./mk.sh -m ./rootfs/&quot;</span><br><span class="line">    echo &quot;&quot;</span><br><span class="line">    echo 1st parameter : $&#123;1&#125;</span><br><span class="line">    echo 2nd parameter : $&#123;2&#125;</span><br><span class="line">fi</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>保存退出后，执行下面代码，就可以进入到我们制作的rootfs根文件系统中。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">h@h-virtual-machine:~/h$ ./mk.sh -m ./rootfs/</span><br></pre></td></tr></table></figure>

<p>如果成功挂载，会出现下面这样的：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">MOUNTING</span><br><span class="line">[sudo] h 的密码：</span><br><span class="line">root@h-virtual-machine:/#</span><br><span class="line">root@h-virtual-machine:/#</span><br><span class="line">root@h-virtual-machine:/# ls</span><br><span class="line">bin   dev  home  media  opt   root  sbin  sys  usr</span><br><span class="line">boot  etc  lib   mnt    proc  run   srv   tmp  var</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>如果想退出，可以输入exit，在退出后，接着输入**.&#x2F;mk.sh -u .&#x2F;rootfs&#x2F;** 。退出 chroot 后，挂载的VFS（proc、sysfs、dev 等）仍然存在，需要清理以避免影响下次操作。</p>
<h4 id="5、安装常用的命令和软件"><a href="#5、安装常用的命令和软件" class="headerlink" title="5、安装常用的命令和软件"></a>5、安装常用的命令和软件</h4><p>在根文件挂载的界面操作下，执行下面命令。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">apt update</span><br><span class="line">apt install sudo</span><br><span class="line">apt install vim</span><br><span class="line">apt install kmod net-tools ethtool ifupdown language-pack-en-base rsyslog htop iputils-ping</span><br></pre></td></tr></table></figure>

<h4 id="6、设置root用户密码"><a href="#6、设置root用户密码" class="headerlink" title="6、设置root用户密码"></a>6、设置root用户密码</h4><p>设置一下 root 用户的密码，命令如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">passwd root </span><br></pre></td></tr></table></figure>

<p>输入“passwd root”以后会让你输入 root 用户密码，输入两次</p>
<h4 id="7、创建用户"><a href="#7、创建用户" class="headerlink" title="7、创建用户"></a>7、创建用户</h4><p>添加一个用户sdr：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">useradd -s &#x27;/bin/bash&#x27; -m -G adm,sudo sdr</span><br></pre></td></tr></table></figure>

<p>给用户设置密码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">passwd sdr</span><br></pre></td></tr></table></figure>

<h4 id="8、设置主机名和IP"><a href="#8、设置主机名和IP" class="headerlink" title="8、设置主机名和IP"></a>8、设置主机名和IP</h4><p>输入如下命令设置本机名称和IP地址</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">echo &quot;sdr&quot; &gt; /etc/hostname</span><br><span class="line">echo &quot;127.0.0.1 localhost&quot; &gt;&gt; /etc/hosts</span><br><span class="line">echo &quot;127.0.0.1 sdr&quot; &gt;&gt; /etc/hosts</span><br></pre></td></tr></table></figure>

<p>127.0.0.1代表的本机的意思，不用改。</p>
<h4 id="9、终端设置"><a href="#9、终端设置" class="headerlink" title="9、终端设置"></a>9、终端设置</h4><p>因为我们的开发板是使用串口作为终端的，所以需要设置一下，比如我们开发板使用的UART1对应的串口设备文件为ttySTM0、我们输入如下命令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ln -s /lib/systemd/system/getty@.service /etc/systemd/system/getty.target.wants/getty@ttySTM0.service</span><br><span class="line"></span><br><span class="line">注意：ttySAC2为串口节点，具体根据自己的板子设置</span><br></pre></td></tr></table></figure>

<h4 id="10、退出配置"><a href="#10、退出配置" class="headerlink" title="10、退出配置"></a>10、退出配置</h4><p>这样基本配置我们就设置好了，我们退出这个根文件系统，然后取消挂载。设置好以后就可以退出根文件系统了，输入如下命令退出：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">exit</span><br><span class="line">./mk.sh -u ./rootfs/</span><br></pre></td></tr></table></figure>

<h4 id="11、将根文件系统下载进开发板中"><a href="#11、将根文件系统下载进开发板中" class="headerlink" title="11、将根文件系统下载进开发板中"></a>11、将根文件系统下载进开发板中</h4><p>之前我们在开发板上运行linux，使用的是老师给的。现在我们只改变了根文件系统，uboot和linux内核不变，所以我们修改原来的rootfs。为了做区别，我使用newrootfs为新的根文件系统的名称。</p>
<ul>
<li><p>1、我们要把newrootfs转移到opt目录下。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mv newrootfs /opt/</span><br></pre></td></tr></table></figure>
</li>
<li><p>2、修改&#x2F;etc&#x2F;exports</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 在主机终端执行</span><br><span class="line">sudo vi /etc/exports</span><br></pre></td></tr></table></figure>

<p>  在文件中，加入下面这段话。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/opt/newrootfs *(rw,sync,no_root_squash,no_subtree_check)</span><br></pre></td></tr></table></figure>
</li>
<li><p>3、重新运行一下。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo exportfs -ra</span><br></pre></td></tr></table></figure>
</li>
<li><p>4、重新启动开发板，在开始输出东西时，一直按下enter键。直到到进入U-boot模式。</p>
<p>  <img src="/2025/02/20/ubuntu%E6%A0%B9%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%A7%BB%E6%A4%8D/203017.png"></p>
<p>  第一个红框时U-boot的版本信息。</p>
<p>  第二个是进入U-boot的结果。</p>
</li>
<li><p>5、输入下面命令，可以产看参数。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 在U-Boot控制台执行</span><br><span class="line">printenv bootargs</span><br></pre></td></tr></table></figure>

<p>  输出如下：</p>
<p>  <img src="/2025/02/20/ubuntu%E6%A0%B9%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%A7%BB%E6%A4%8D/203258.png"></p>
</li>
<li><p>6、执行 <code>setenv</code> 命令修改 <code>bootargs</code> 环境变量：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">setenv bootargs &#x27;root=/dev/nfs nfsroot=192.168.60.30:/opt/newrootfs intr rsize=1024 wsize=1024 rootwait rw earlyprintk ip=192.168.60.90 console=ttySTM0,115200&#x27;</span><br></pre></td></tr></table></figure>

<p>  上面的截图是我修改过后的，原先是下面这样</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bootargs=root=/dev/nfs nfsroot=192.168.60.30:/opt/rootfs intr rsize=1024 wsize=1024 rootwait rw earlyprintk ip=192.168.60.90 console=ttySTM0,115200</span><br></pre></td></tr></table></figure>
</li>
<li><p>7、执行 <code>saveenv</code> 命令保存环境变量</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">saveenv</span><br></pre></td></tr></table></figure>
</li>
<li><p>8、重启开发板，最后会看到下图：<br>  <img src="/2025/02/20/ubuntu%E6%A0%B9%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%A7%BB%E6%A4%8D/204228.png"></p>
</li>
</ul>
<p>到此在stm32mp157的开发板上就跑起来了linux。</p>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>linux移植</tag>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>图片显示</title>
    <url>/2025/03/16/%E5%9B%BE%E7%89%87%E6%98%BE%E7%A4%BA/</url>
    <content><![CDATA[<h1 id="图片显示"><a href="#图片显示" class="headerlink" title="图片显示"></a>图片显示</h1><span id="more"></span>

<h3 id="BMP格式解码"><a href="#BMP格式解码" class="headerlink" title="BMP格式解码"></a>BMP格式解码</h3><p>BMP是一种常见的图片文件格式，广泛的用于存储像素数据。</p>
<h4 id="1、BMP文件的基本构成"><a href="#1、BMP文件的基本构成" class="headerlink" title="1、BMP文件的基本构成"></a>1、BMP文件的基本构成</h4><p>BMP文件主要由以下部分组成：</p>
<ul>
<li>头文件：描述文件的基本信息，例如文件大小和数据偏移量</li>
<li>信息头：描述图像的详细信息，例如宽度、高度、颜色等。</li>
<li>调色板【可选】：对于地位深度的图像，会包含调色板，用于将索引映射带具体的RGB颜色。</li>
<li>像素数据：实际的图像数据，存储方式却决于颜色深度和压缩格式。</li>
</ul>
<h4 id="2、解码步骤"><a href="#2、解码步骤" class="headerlink" title="2、解码步骤"></a>2、解码步骤</h4><ul>
<li><p>读取文件头（14字节）</p>
<ul>
<li>2字节：文件标识符，通常BM（0x424D），表示这是BMP文件。</li>
<li>4字节：文件总大小，字节为单位。</li>
<li>4字节：保留字段，通常为0，无意义。</li>
<li>4字节：像素数据起始偏移量，告诉我们从文件开头到像素数据的距离。</li>
</ul>
</li>
<li><p>读取信息头（通常为40字节）</p>
<ul>
<li><p>4字节：信息头大小。</p>
</li>
<li><p>4字节：图像宽度。</p>
</li>
<li><p>4字节：图像高度。</p>
</li>
<li><p>2字节：颜色平面数，通产为1</p>
</li>
<li><p>2字节：每像素位数，常见的1、4、8、16、24、32.</p>
</li>
<li><p>4字节：压缩方式，0：无压缩、1：PLE8位；2、RLE 4位。</p>
</li>
<li><p>偏移 34-37（4字节）：像素数据大小（字节），若无压缩可忽略。</p>
<p>  偏移 38-41（4字节）：水平分辨率（像素&#x2F;米），可忽略。</p>
<p>  偏移 42-45（4字节）：垂直分辨率（像素&#x2F;米），可忽略。</p>
<p>  偏移 46-49（4字节）：调色板颜色数，0表示默认值（2^n，n为颜色深度）。</p>
<p>  偏移 50-53（4字节）：重要颜色数，通常为0。</p>
</li>
</ul>
</li>
<li><p>读取调色板（如果存在）</p>
<ul>
<li><p>如果颜色深度≤8（如1位、4位、8位），会有调色板。</p>
<p>  每个颜色占用4字节，按BGR顺序存储（蓝、绿、红、保留字节，通常为0）。</p>
<p>  调色板大小 &#x3D; 颜色数 × 4字节，颜色数由信息头中的字段或默认值（2^颜色深度）确定。</p>
<p>  24位及以上颜色深度通常没有调色板，直接使用RGB值。</p>
</li>
</ul>
</li>
<li><p>读取像素数据</p>
<ul>
<li>像素数据的存储方式取决于颜色深度和压缩格式。</li>
<li>对于无压缩的BMP：<ul>
<li>行字节数：根据宽度和颜色深度计算。每行字节数必须是4的倍数，若不足则填充0</li>
<li>存储顺序：通常从图像底部开始，自左向右逐行向上（若高度为正值）。</li>
<li>颜色格式：<ul>
<li>24位：每个像素3字节，按BGR顺序（蓝、绿、红）。</li>
<li>32位：每个像素4字节，按BGRA顺序（蓝、绿、红、透明度）。</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="代码分析"><a href="#代码分析" class="headerlink" title="代码分析"></a>代码分析</h3><p>1、全局变量定义</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">char bmp_files[MAX_BMP_FILES][MAX_PATH_LEN];</span><br><span class="line">uint8_t bmp_file_count = 0;</span><br><span class="line">int8_t current_bmp_index = -1;</span><br><span class="line">FIL img_file;</span><br><span class="line">uint8_t *img_buffer = NULL;</span><br><span class="line">lv_img_dsc_t img_dsc;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>bmp_files：二维字符数组，用于存储 BMP 文件的完整路径（如 “0:&#x2F;PICTURE&#x2F;image.bmp”）。MAX_BMP_FILES 和 MAX_PATH_LEN 在头文件中定义。</p>
<p>  bmp_file_count：记录扫描到的 BMP 文件数，初始为 0。</p>
<p>  current_bmp_index：当前显示的 BMP 文件索引，初始为 -1（未选择）。</p>
<p>  img_file：FATFS 的文件对象，用于操作 SD 卡上的文件。</p>
<p>  img_buffer：指向动态分配的内存，用于存储 BMP 像素数据，初始为 NULL。</p>
<p>  img_dsc：LVGL 的图像描述符，定义图像的宽度、高度、颜色格式等。</p>
</li>
</ul>
<p>2、stricmp 函数：不区分大小写的字符串比较</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int stricmp(const char *s1, const char *s2) &#123;</span><br><span class="line">    while (*s1 &amp;&amp; *s2) &#123;</span><br><span class="line">        char c1 = (*s1 &gt;= &#x27;A&#x27; &amp;&amp; *s1 &lt;= &#x27;Z&#x27;) ? *s1 + 32 : *s1;</span><br><span class="line">        char c2 = (*s2 &gt;= &#x27;A&#x27; &amp;&amp; *s2 &lt;= &#x27;Z&#x27;) ? *s2 + 32 : *s2;</span><br><span class="line">        if (c1 != c2) return c1 - c2;</span><br><span class="line">        s1++;</span><br><span class="line">        s2++;</span><br><span class="line">    &#125;</span><br><span class="line">    return *s1 - *s2;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>如果字符是大写字母A到Z，则通过+32转换为小写。</li>
<li>如果字符不相同，返回插值。</li>
</ul>
<p>3、scan_bmp_files 函数：扫描BMP文件</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void scan_bmp_files(void) &#123;</span><br><span class="line">    DIR dir;</span><br><span class="line">    FILINFO fno;</span><br><span class="line">    FRESULT res;</span><br><span class="line"></span><br><span class="line">    bmp_file_count = 0;</span><br><span class="line">    memset(bmp_files, 0, sizeof(bmp_files));</span><br><span class="line"></span><br><span class="line">    res = f_opendir(&amp;dir, &quot;0:/PICTURE&quot;);</span><br><span class="line">    if (res != FR_OK) &#123;</span><br><span class="line">        printf(&quot;打开目录 &#x27;PICTURE&#x27; 失败，错误码: %d\r\n&quot;, res);</span><br><span class="line">        return;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    while (bmp_file_count &lt; MAX_BMP_FILES) &#123;</span><br><span class="line">        res = f_readdir(&amp;dir, &amp;fno);</span><br><span class="line">        if (res != FR_OK || fno.fname[0] == 0) break;</span><br><span class="line"></span><br><span class="line">        char *ext = strrchr(fno.fname, &#x27;.&#x27;);</span><br><span class="line">        if (ext &amp;&amp; stricmp(ext, &quot;.bmp&quot;) == 0) &#123;</span><br><span class="line">            snprintf(bmp_files[bmp_file_count], MAX_PATH_LEN, &quot;0:/PICTURE/%s&quot;, fno.fname);</span><br><span class="line">            printf(&quot;添加 BMP 文件 [%d]: %s\r\n&quot;, bmp_file_count, bmp_files[bmp_file_count]);</span><br><span class="line">            bmp_file_count++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    f_closedir(&amp;dir);</span><br><span class="line">    printf(&quot;共找到 %d 个 BMP 文件\r\n&quot;, bmp_file_count);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>打开图片目录。</li>
<li>读取目录项：使用f_readdir 逐个读取目录中文件的信息，存储在fno中。</li>
<li>使用strrchr找文件名中.后面的名字，提取字符串。</li>
<li>输出打印找到的每个BMP文件路劲和总数。</li>
</ul>
<p>4、load_image_from_sd 函数：加载并显示BMP图像</p>
<ul>
<li><p>参数检查</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if (index &lt; 0 || index &gt;= bmp_file_count) &#123;</span><br><span class="line">    printf(&quot;无效的 BMP 索引: %d\r\n&quot;, index);</span><br><span class="line">    return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>释放就缓冲区</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if (img_buffer != NULL) &#123;</span><br><span class="line">    myfree(SRAMEX, img_buffer);</span><br><span class="line">    img_buffer = NULL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>打开找到的BMP文件</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">res = f_open(&amp;img_file, bmp_files[index], FA_READ);</span><br><span class="line">if (res != FR_OK) &#123;</span><br><span class="line">    printf(&quot;打开文件失败: %s，错误码: %d\r\n&quot;, bmp_files[index], res);</span><br><span class="line">    return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>检查文件大小</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">uint32_t file_size = f_size(&amp;img_file);</span><br><span class="line">if (file_size &gt; MAX_BMP_SIZE) &#123;</span><br><span class="line">    printf(&quot;BMP 文件过大: %s，大小: %lu 字节\r\n&quot;, bmp_files[index], file_size);</span><br><span class="line">    f_close(&amp;img_file);</span><br><span class="line">    return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>读取BMP文件头</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">res = f_read(&amp;img_file, header_buf, sizeof(header_buf), &amp;br);</span><br><span class="line">if (res != FR_OK || br != sizeof(header_buf)) &#123;</span><br><span class="line">    printf(&quot;读取 BMP 文件头失败\r\n&quot;);</span><br><span class="line">    f_close(&amp;img_file);</span><br><span class="line">    return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>读取前 54 字节（文件头 14 字节 + 信息头 40 字节），存储到 header_buf 中。</li>
</ul>
</li>
<li><p>解析BMP信息</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bmp_header = (BITMAPINFOHEADER *)(header_buf + 14);</span><br><span class="line">width = bmp_header-&gt;biWidth;</span><br><span class="line">height = bmp_header-&gt;biHeight;</span><br><span class="line">bpp = bmp_header-&gt;biBitCount;</span><br><span class="line">data_offset = *(uint32_t *)(header_buf + 10);</span><br><span class="line">img_size = width * height * (bpp / 8);</span><br></pre></td></tr></table></figure>

<ul>
<li>从头信息中提取高度、宽度、位深度、像素偏移量</li>
<li>计算像素数据大小。</li>
</ul>
</li>
<li><p>检查位深度</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if (bpp != 16 &amp;&amp; bpp != 24 &amp;&amp; bpp != 32) &#123;</span><br><span class="line">    printf(&quot;不支持的 BMP 格式，位深度: %d\r\n&quot;, bpp);</span><br><span class="line">    f_close(&amp;img_file);</span><br><span class="line">    return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>分配缓冲区，并读取像素数据</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">img_buffer = mymalloc(SRAMEX, img_size);</span><br><span class="line">f_lseek(&amp;img_file, data_offset);</span><br><span class="line">res = f_read(&amp;img_file, img_buffer, img_size, &amp;br);</span><br></pre></td></tr></table></figure>
</li>
<li><p>转换为RGB565格式</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if (bpp == 32) &#123;</span><br><span class="line">    uint8_t *src = img_buffer;</span><br><span class="line">    uint16_t *dst = (uint16_t *)mymalloc(SRAMEX, width * height * 2);</span><br><span class="line">    for (uint32_t i = 0; i &lt; width * height; i++) &#123;</span><br><span class="line">        uint8_t b = src[i * 4 + 0] &gt;&gt; 3;</span><br><span class="line">        uint8_t g = src[i * 4 + 1] &gt;&gt; 2;</span><br><span class="line">        uint8_t r = src[i * 4 + 2] &gt;&gt; 3;</span><br><span class="line">        uint8_t a = src[i * 4 + 3];</span><br><span class="line">        dst[i] = (a &lt; 128) ? 0x0000 : (r &lt;&lt; 11) | (g &lt;&lt; 5) | b;</span><br><span class="line">    &#125;</span><br><span class="line">    myfree(SRAMEX, img_buffer);</span><br><span class="line">    img_buffer = (uint8_t *)dst;</span><br><span class="line">    img_size = width * height * 2;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>将 BGRA（蓝绿红透明）转换为 RGB565（5位红、6位绿、5位蓝）。</p>
</li>
<li><p>如果透明度（a）&lt; 128，则像素设为透明（0x0000）。</p>
</li>
<li><p>RGB565是一种16位的颜色编码格式：</p>
<ul>
<li><p>R：5位</p>
</li>
<li><p>G：6位</p>
</li>
<li><p>B：5位</p>
</li>
<li><p>在内存中表示如下：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">RRRRRGGG GGGBBBBB</span><br></pre></td></tr></table></figure>
</li>
<li><p>32位代表着R&#x3D;8位、G&#x3D;8位、B&#x3D;8位、透明色&#x3D;8位。</p>
</li>
<li><p>8位颜色代表着颜色的每个颜色，就像8位的红色右2^8&#x3D;256种颜色细分。</p>
</li>
<li><p>5 位颜色：用 5 位表示，只能表示 0 到 31 的值（2⁵ &#x3D; 32 种可能）。</p>
</li>
<li><p>6 位颜色：用 6 位表示，能表示 0 到 63 的值（2⁶ &#x3D; 64 种可能）。</p>
</li>
</ul>
</li>
<li><p>BMP文件种假设使用32位的，而屏幕使用是RGB565.则需要从8位转化为5位或者6位。需要一一对应起来。</p>
<ul>
<li>从8位转换为5位：相当于256种颜色转换为32种颜色。所以要比例是：256&#x2F;32&#x3D;8.</li>
<li>在二进制种8可以用右移3位来表示。</li>
</ul>
</li>
</ul>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">else if (bpp == 24) &#123;</span><br><span class="line">    uint8_t *src = img_buffer;</span><br><span class="line">    uint16_t *dst = (uint16_t *)img_buffer;</span><br><span class="line">    for (uint32_t i = 0; i &lt; width * height; i++) &#123;</span><br><span class="line">        uint8_t b = src[i * 3 + 0] &gt;&gt; 3;</span><br><span class="line">        uint8_t g = src[i * 3 + 1] &gt;&gt; 2;</span><br><span class="line">        uint8_t r = src[i * 3 + 2] &gt;&gt; 3;</span><br><span class="line">        dst[i] = (r &lt;&lt; 11) | (g &lt;&lt; 5) | b;</span><br><span class="line">    &#125;</span><br><span class="line">    img_size = width * height * 2;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>将 BGR 转换为 RGB565，直接覆盖原缓冲区。</li>
</ul>
</li>
<li><p>垂直翻转图像</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">uint16_t *buf = (uint16_t *)img_buffer;</span><br><span class="line">for (uint16_t y = 0; y &lt; height / 2; y++) &#123;</span><br><span class="line">    for (uint16_t x = 0; x &lt; width; x++) &#123;</span><br><span class="line">        uint16_t temp = buf[y * width + x];</span><br><span class="line">        buf[y * width + x] = buf[(height - 1 - y) * width + x];</span><br><span class="line">        buf[(height - 1 - y) * width + x] = temp;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>BMP 文件存储顺序：BMP 文件的像素数据通常从图像的底部开始，按行顺序存储到顶部（如果信息头中的高度字段 biHeight 为正值）。</li>
</ul>
</li>
<li><p>配置LVGL并显示</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">img_dsc.header.w = width;</span><br><span class="line">img_dsc.header.h = height;</span><br><span class="line">img_dsc.header.cf = LV_IMG_CF_TRUE_COLOR;</span><br><span class="line">img_dsc.data_size = img_size;</span><br><span class="line">img_dsc.data = img_buffer;</span><br><span class="line"></span><br><span class="line">lv_img_set_src(img_obj, &amp;img_dsc);</span><br><span class="line">lv_img_set_zoom(img_obj, 128);  // 50% 缩放</span><br><span class="line">lv_obj_set_pos(img_obj, 100, 100);</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>stm32F407</category>
      </categories>
      <tags>
        <tag>stm32F407</tag>
        <tag>多媒体项目</tag>
      </tags>
  </entry>
  <entry>
    <title>stm32F407移植LVGL+GUI</title>
    <url>/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/</url>
    <content><![CDATA[<h1 id="stm32F407移植LVGL-GUI"><a href="#stm32F407移植LVGL-GUI" class="headerlink" title="stm32F407移植LVGL+GUI"></a>stm32F407移植LVGL+GUI</h1><h3 id="前期准备"><a href="#前期准备" class="headerlink" title="前期准备"></a>前期准备</h3><ul>
<li>我现在使用的开发板是STM32F407ZGT6.</li>
<li>从网站上下载LVGL。官方网址：<a href="https://github.com/lvgl/lvgl%EF%BC%8C%E6%88%91%E5%9C%A8%E8%BF%99%E6%AC%A1%E7%A7%BB%E6%A4%8D%E8%BF%87%E7%A8%8B%E4%B8%AD%E4%BD%BF%E7%94%A8%E7%9A%84%E7%89%88%E6%9C%AC%E4%B8%BAv8.2%E3%80%82%EF%BC%88%E5%BB%BA%E8%AE%AE%E4%BD%BF%E7%94%A8%E7%9B%B8%E5%90%8C%E7%89%88%E6%9C%AC%E7%9A%84%EF%BC%8C%E5%9C%A8%E5%90%8E%E9%9D%A2%E5%A6%82%E6%9E%9C%E7%89%88%E6%9C%AC%E4%B8%8D%E5%8C%B9%E9%85%8D%E7%9A%84%E8%AF%9D%EF%BC%8C%E4%BC%9A%E5%87%BA%E7%8E%B0%E5%90%84%E7%A7%8D%E9%97%AE%E9%A2%98%EF%BC%8C%E6%89%80%E4%BB%A5%E5%BB%BA%E8%AE%AE%E4%BD%BF%E7%94%A8%E5%92%8C%E6%88%91%E4%B8%80%E6%A0%B7%E7%9A%84%E7%89%88%E6%9C%AC%EF%BC%89">https://github.com/lvgl/lvgl，我在这次移植过程中使用的版本为v8.2。（建议使用相同版本的，在后面如果版本不匹配的话，会出现各种问题，所以建议使用和我一样的版本）</a></li>
<li>移植之前，准备好LCD显示实验、触摸屏实验、基本定时器实验、内存管理实验。（这里使用的所有文件都是正点原子的例程代码）<br>  <img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/165612.png"></li>
<li>我们以内存管理实现为基础，向其添加其他文件所用的相关代码。并编译后，没有报错的话，开始正式移植。</li>
</ul>
<span id="more"></span>

<h3 id="移植LVGL"><a href="#移植LVGL" class="headerlink" title="移植LVGL"></a>移植LVGL</h3><h4 id="1、准备LVGL源码"><a href="#1、准备LVGL源码" class="headerlink" title="1、准备LVGL源码"></a>1、准备LVGL源码</h4><p>1、当你从上述网址中下载了LVGL的源码后，你会得到一个压缩包，解压后会得到一个如图所示的文件夹。</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/170022.png"></p>
<p>2、将上图中的lv_conf_template.h 文件改名为 lv_conf.h。然后打开这个文件，在最开始的地方，找到下面的代码，并按下面的要求修改。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">修改前：</span><br><span class="line"></span><br><span class="line">#if 0 /*Set it to &quot;1&quot; to enable content*/</span><br><span class="line"></span><br><span class="line">修改后：</span><br><span class="line"></span><br><span class="line">#if 1 /* 把#if 0 修改成</span><br></pre></td></tr></table></figure>



<p>3、删除一些我们不要的文件。</p>
<p>在lvgl-8.2.0目录下的文件，除了demos、examples、src、lv_conf.h、lvgl.h，其他文件全部删除。最后的结果如图</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/170831.png"></p>
<p>然后打开文件examples，将其中的文件除了porting，其他全部删除。结果如图。</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/171049.png"></p>
<h3 id="2、裸机移植LVGL"><a href="#2、裸机移植LVGL" class="headerlink" title="2、裸机移植LVGL"></a>2、裸机移植LVGL</h3><p>打开内存管理实验文件夹，没有移植之前的工程目录大概如下。</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/171409.png"></p>
<p>我们在其中新建一些文件夹，用来存放我们要移植的代码，从Middlewares开始创建，内容如图.</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/171555.png"></p>
<p>创建完成后，我们可以把处理好的LVGL中的文件examples、src、lv_conf.h、lvgl.h这四个复制到Middlewares&#x2F;LVGL&#x2F;GUI&#x2F;lvgl目录下。结果如图</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/171908.png"></p>
<p>打开内存管理的工程，进入到keil界面的操作。</p>
<p>点击图中红色方框。</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/172136.png"></p>
<p>进入到下图这个界面按照图中创建分组，并添加相关的代码。（请注意，添加的代码比较烦，所以一定要注意）</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/172319.png"></p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/172602.png"></p>
<p>添加相关的头文件路径（按照图中自上而下顺序操作）</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/172743.png"></p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/172917.png"></p>
<p>开启C99模式</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/173013.png"></p>
<p>然后编译一下，如果报错了，请检查一下，文件是否添加进来了，头文件路径是否正确。如果正常没有报错，但是会有一些报错。不必理会。</p>
<h3 id="修改代码"><a href="#修改代码" class="headerlink" title="修改代码"></a>修改代码</h3><p>1、为LVGL提供时基。</p>
<p>打开我们工程中的timer.c。在头部添加LVGL的头文件。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &quot;lvgl.h&quot;  </span><br></pre></td></tr></table></figure>

<p>在中断中添加lvgl的心跳函数。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//定时器3中断服务函数</span><br><span class="line">void TIM3_IRQHandler(void)</span><br><span class="line">&#123;</span><br><span class="line">	if(TIM_GetITStatus(TIM3,TIM_IT_Update)==SET) //溢出中断</span><br><span class="line">	&#123;</span><br><span class="line">		 lv_tick_inc ( 1 );</span><br><span class="line">	&#125;</span><br><span class="line">	TIM_ClearITPendingBit(TIM3,TIM_IT_Update);  //清除中断标志位</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上述源码中，定时器中断回调函数调用了 LVGL 的 lv_tick_inc 函数，该函数可以让 LVGL内部的时基参数加 1 （入口参数为 1 的情况下）， 这里要注意，你定时器设置多少毫秒进入一次中断，那么， lv_tick_inc();函数的入口参数就设置为多少，不然时基就不准确。</p>
<p>接下来打开main.c文件，在mian函数中添加初始化。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">TIM3_Int_Init(999,167);</span><br></pre></td></tr></table></figure>



<p>2、配置显示屏、触摸输入驱动。</p>
<p>打开 Middlewares&#x2F;lvgl&#x2F;examples&#x2F;porting 分组中的 lv_port_disp_template.c&#x2F;h（显示屏相关）和 lv_port_indev_template.c&#x2F;h （触摸输入相关）文件，将这 4 个文件中的条件编译指令 #if 0 都修改成#if 1 ，如下源码所示：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">修改前：</span><br><span class="line"></span><br><span class="line">#if 0 /*Set it to &quot;1&quot; to enable content*/</span><br><span class="line"></span><br><span class="line">修改后：</span><br><span class="line"></span><br><span class="line">#if 1 /* 把#if 0 修改成</span><br></pre></td></tr></table></figure>

<ul>
<li><p>1、修改lv_port_disp_template.c文件。（基本按照图中改好，可能跟下面的截图不同，以下为准，因为这是我添加别的功能，修改了代码）<br>  <img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/code.png"></p>
<ul>
<li><p>添加lcd的头文件，我们要使用lcd显示界面。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &quot;lcd.h&quot;</span><br></pre></td></tr></table></figure>
</li>
<li><p>配置屏幕的分辨率。我是用的3.5存的TFTLCD屏幕，代码如下</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define MY_DISP_HOR_RES 320   /* 屏幕宽度 */</span><br><span class="line">#define MY_DISP_VER_RES 480   /* 屏幕高度 */</span><br></pre></td></tr></table></figure>

<p>  <img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/175313.png"></p>
</li>
<li><p>在lv_port_disp_init函数中，按照图片注释下面的，保留上面的。初始化缓冲区，我们默认使用单行缓冲区。</p>
<p>  <img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/175409.png"></p>
</li>
<li><p>修改分辨率。</p>
<p>  <img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/175438.png"></p>
</li>
<li><p>在disp_init函数中，添加LCD的初始化。</p>
<p>  <img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/175501.png"></p>
</li>
<li><p>配置花点函数，这个函数在lcd.h中可以找到。</p>
<p>  <img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/175534.png"></p>
</li>
</ul>
</li>
<li><p>2、修改lv_port_indev_template.c文件</p>
<p>  <img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/code2.png"></p>
<ul>
<li><p>添加头文件</p>
<p>  <img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/180544.png"></p>
</li>
<li><p>注销处理显示屏之外的东西</p>
<p>  <img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/180611.png"></p>
</li>
<li><p>触摸屏初始化</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static void touchpad_init(void)</span><br><span class="line">&#123;</span><br><span class="line">    /*Your code comes here*/</span><br><span class="line">   tp_dev . init ();</span><br><span class="line">        /* 电阻屏坐标矫正 */</span><br><span class="line">        if (KEY_Scan( 0 ) == KEY0_PRES) /* KEY0 按下 , 则执行校准程序 */</span><br><span class="line">        &#123;</span><br><span class="line">        LCD_Clear(WHITE); /* 清屏 */</span><br><span class="line">        TP_Adjust(); /* 屏幕校准 */</span><br><span class="line">        TP_Save_Adjdata();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">	//TP_Init();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>检测屏幕是否被按下，</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/*Return true is the touchpad is pressed*/</span><br><span class="line">static bool touchpad_is_pressed(void)</span><br><span class="line">&#123;</span><br><span class="line">    /*Your code comes here*/</span><br><span class="line">    tp_dev.scan(0);</span><br><span class="line">    if(tp_dev.sta &amp; TP_PRES_DOWN)</span><br><span class="line">        return true;</span><br><span class="line">    return false;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>返回按下的坐标</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static void touchpad_get_xy(lv_coord_t * x, lv_coord_t * y)</span><br><span class="line">&#123;</span><br><span class="line">    /*Your code comes here*/</span><br><span class="line">    (*x) = tp_dev.x[0];</span><br><span class="line">    (*y) = tp_dev.y[0];</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>3、修改main.c文件</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &quot;sys.h&quot;</span><br><span class="line">#include &quot;delay.h&quot;</span><br><span class="line">#include &quot;usart.h&quot;</span><br><span class="line">#include &quot;led.h&quot;</span><br><span class="line">#include &quot;lcd.h&quot;</span><br><span class="line">#include &quot;key.h&quot;    </span><br><span class="line">#include &quot;malloc.h&quot; </span><br><span class="line"></span><br><span class="line"></span><br><span class="line">#include &quot;timer.h&quot;</span><br><span class="line">#include &quot;sram.h&quot; </span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">/* LVGL */</span><br><span class="line">#include &quot;lvgl.h&quot;</span><br><span class="line">#include &quot;lv_port_indev_template.h&quot;</span><br><span class="line">#include &quot;lv_port_disp_template.h&quot;</span><br><span class="line"></span><br><span class="line">#include &quot;lv_demo_stress.h&quot;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">int main(void)</span><br><span class="line">&#123;        </span><br><span class="line">    NVIC_PriorityGroupConfig(NVIC_PriorityGroup_2);</span><br><span class="line">    delay_init(168);</span><br><span class="line">    uart_init(115200);</span><br><span class="line">    LED_Init();</span><br><span class="line">    LCD_Init();</span><br><span class="line">    KEY_Init(); </span><br><span class="line">    TIM3_Int_Init(999,167);</span><br><span class="line"> </span><br><span class="line">    lv_init();</span><br><span class="line">    lv_port_disp_init();</span><br><span class="line">    lv_port_indev_init();</span><br><span class="line">    </span><br><span class="line">	lv_obj_t *label = lv_label_create(lv_scr_act());</span><br><span class="line">   	lv_label_set_text(label,&quot;Hello KUrumi!!!&quot;);</span><br><span class="line">   	lv_obj_center(label);/</span><br><span class="line"></span><br><span class="line">    while(1)</span><br><span class="line">    &#123;	</span><br><span class="line">        lv_task_handler();</span><br><span class="line">        delay_ms(5);</span><br><span class="line">    &#125;	</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
</li>
<li><p>然后你会在屏幕上看到相对应的字符串。说明移植成功。</p>
<p>  <img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/181528.png"></p>
</li>
</ul>
<h3 id="移植GUI"><a href="#移植GUI" class="headerlink" title="移植GUI"></a>移植GUI</h3><p>1、首先你要下载一个软件，GUI-Guider工具，提供了界面设计和代码生成的功能，使用这个工具能够让我们很快的设计好一个界面。（注意：你所下载的版本最好和LVGL的版本所匹配，GUI是基于LVGL的一个工具。）</p>
<p>2、下载后，打开软件，你会看到下面这个界面。点击创建新建项目。</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/131252.png"></p>
<p>3、然后你会进入一个选择LVGL版本的界面，如图，选择你项目中使用的版本号。前面使用的v8.2，所以我们选择v8.2。点击下一步。</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/131409.png"></p>
<p>跳转到下面的界面，双击simulator（模拟器），这一步是选择空白的模拟器。再点击下一步。</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/131853.png"></p>
<p>进入到了Templates模板界面中，我们选择EmptyUI。</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/132016.png"></p>
<p>进入带项目配置信息界面。再右侧输入栏中。我们先填写项目的名称，例如我们输入xianshi。下面是工程的路径，最后生成的代码也在这。在下面是面板类型，我们在下拉菜单中选择Custom…，下面出现新的一行，我们依次输入LCD，宽度：320；高度：480。输入完成后，点击创建。</p>
<p>然后进入到了下面的界面，你就可以创建你的界面了。</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/132920.png"></p>
<p>下面编辑界面我就不讲解了，讲起来比较麻烦，截图太多。下面我讲解你设计好界面后如何生成代码，如何加入到工程中。</p>
<p>插一句：如果你设计好了界面，想看看效果，GUI提供了模拟器，可以查看，点击图中绿色的按钮，选择C，就可以模拟。但是有一个前提，就是要在你的电脑上安装Java环境，才能用模拟器。</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/133552.png"></p>
<p>如何导出代码。有两种办法。</p>
<ul>
<li><p>你可以去你创建时的那个目录中寻找。</p>
</li>
<li><p>你可以直接在工具栏中选择工具-》代码导出-》路径选择桌面。</p>
<p>  <img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/133832.png"></p>
</li>
</ul>
<p>我用的第二种方法，在桌面上你可以看到两个文件夹，custom和generated。</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/134002.png"></p>
<p>复制这两个文件夹到我们工程目录的中 “移植gui\Middlewares\LVGL\GUI_APP\“目录中</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/134203.png"></p>
<p>打开工程，在keil添加分组，为分组中添加文件中所有的.c文件。再添加文件路径。</p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/134359.png"></p>
<p><img src="/2025/03/07/stm32F407%E7%A7%BB%E6%A4%8DLVGL-GUI/134444.png"></p>
<p>这样就成功把GUi生成的代码加入到了我们工程中了。</p>
<p>然后再修改mian.c中的内容。</p>
<ul>
<li><p>添加头文件</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">#include &quot;gui_guider.h&quot;</span><br><span class="line">#include &quot;events_init.h&quot;</span><br><span class="line">#include &quot;custom.h&quot;</span><br></pre></td></tr></table></figure>
</li>
<li><p>添加全局变量</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">lv_ui guider_ui;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在mian函数中添加初始化。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">setup_ui(&amp;guider_ui);</span><br><span class="line">   events_init(&amp;guider_ui);</span><br><span class="line">   custom_init(&amp;guider_ui);</span><br></pre></td></tr></table></figure>
</li>
<li><p>编译没有报错，你就可以在你的LCD屏幕上看到你设计的界面了。</p>
</li>
<li><p>如果编译报错，如下面这样，提示#include “lv_font.h”出错，找到guider_fonts.h文件，修改#include “lv_font.h”。修改完成后如下，或者你可直接找到lv_font.h文件，直接复制到generated文件夹中。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">..\Middlewares\LVGL\GUI_APP\generated\guider_fonts\guider_fonts.h(8): error:  #5: cannot open source input file &quot;lv_font.h&quot;: No such file or directory</span><br><span class="line">      #include &quot;lv_font.h&quot;</span><br></pre></td></tr></table></figure>

  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#ifndef GUIDER_FONTS_H</span><br><span class="line">#define GUIDER_FONTS_H</span><br><span class="line">#ifdef __cplusplus</span><br><span class="line">extern &quot;C&quot; &#123;</span><br><span class="line">#endif</span><br><span class="line"></span><br><span class="line">#include &quot;../../GUI/lvgl/src/font/lv_font.h&quot;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">#ifdef __cplusplus</span><br><span class="line">&#125;</span><br><span class="line">#endif</span><br><span class="line">#endif</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>stm32F407</category>
      </categories>
      <tags>
        <tag>stm32F407</tag>
        <tag>多媒体项目</tag>
      </tags>
  </entry>
  <entry>
    <title>基于SD卡的FATFS文件管理系统</title>
    <url>/2025/03/19/%E5%9F%BA%E4%BA%8ESD%E5%8D%A1%E7%9A%84FATFS%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<h2 id="1-引言：理解嵌入式系统中的-FATFS-和-SD-卡"><a href="#1-引言：理解嵌入式系统中的-FATFS-和-SD-卡" class="headerlink" title="1. 引言：理解嵌入式系统中的 FATFS 和 SD 卡"></a>1. 引言：理解嵌入式系统中的 FATFS 和 SD 卡</h2><p>随着嵌入式系统在各个领域的日益普及，对高效数据存储解决方案的需求也随之增长。在众多可用的文件系统中，FATFS 因其轻量级特性和对 FAT 文件格式的广泛兼容性而成为嵌入式应用的热门选择 。与此同时，SD 卡已成为嵌入式系统中无处不在的存储介质，其种类繁多，容量各异，满足了各种应用场景的需求 。本报告旨在深入探讨 FATFS 在 SD 卡环境下的应用，涵盖其特性、实现、应用场景、性能、挑战以及与其他文件系统的比较。  </p>
<p>FATFS 和 SD 卡在嵌入式系统中的广泛使用表明这两种技术之间存在强大的协同效应。这很可能归因于 FATFS 的设计原则与许多嵌入式应用的资源限制和存储需求高度契合。研究资料始终将 FATFS 描述为专为资源有限的小型嵌入式系统而设计 ，这与通常使用 SD 卡进行扩展存储的设备的典型特征直接相关。此外，SD 卡种类繁多，规格各异，这意味着开发人员需要深入了解这些特性，以便为他们的应用选择合适的卡，并确保与 FATFS 的兼容性。关于 SD 卡类型（SD、SDHC、SDXC、SDUC）及其容量和速度规格的众多资料  强调了选择最佳存储介质所涉及的复杂性。 </p>
<span id="more"></span>

<h1 id="2-FATFS-文件系统"><a href="#2-FATFS-文件系统" class="headerlink" title="2.FATFS 文件系统"></a>2.FATFS 文件系统</h1><h4 id="1、核心架构"><a href="#1、核心架构" class="headerlink" title="1、核心架构"></a>1、核心架构</h4><p>FATFS是一个通用的文件系统模块。其核心设计理念是将文件系统层与迪岑的磁盘控制层分离。FATFS 支持 FAT12、FAT16、FAT32 以及可选的 exFAT 文件系统类型 。由于其代码体积小，RAM 使用率低，FATFS 非常适合资源有限的微控制器 。</p>
<p>FATFS 的设计目标是独立于平台和存储介质，通过一个由实现者提供的介质访问接口（磁盘 I&#x2F;O 层）与物理存储设备进行通信 。</p>
<h4 id="2、主要特性和功能"><a href="#2、主要特性和功能" class="headerlink" title="2、主要特性和功能"></a>2、主要特性和功能</h4><p>FATFS提供了全套的API函数，用于文件访问（打开、关闭、读取、写入、定位、截断、同步等）；目录访问（打开、关闭、读取目录项、查找第一个&#x2F;下一个）；文件和目录管理（状态、取消链接、重命名、更改属性、更改时间戳、创建目录、更改当前目录、更改当前驱动器、检索当前目录）；</p>
<p><strong>上一章的SPI Flash芯片驱动只完成了向物理地址写入数据的工作，而根据文件系统格式的逻辑转换部分则需要额外的代码来完成。 实质上，这个逻辑转换部分可以理解为当我们需要写入一段数据时，由它来求解向什么物理地址写入数据、以什么格式写入及写入一些原始数据以外的信息(如目录)。 这个逻辑转换部分代码我们也习惯称之为文件系统。</strong> </p>
<p><strong>所以我们利用前面写好的SPI Flash芯片驱动，把FatFs文件系统代码移植到工程之中， 就可以利用文件系统的各种函数，对SPI Flash芯片以“文件”格式进行读写操作了。</strong></p>
<h4 id="3、FATFS源码"><a href="#3、FATFS源码" class="headerlink" title="3、FATFS源码"></a>3、FATFS源码</h4><p>官网：<a href="http://elm-chan.org/fsw/ff/00index_e.html">http://elm-chan.org/fsw/ff/00index_e.html</a></p>
<p>下载后如下：<br><img src="/2025/03/19/%E5%9F%BA%E4%BA%8ESD%E5%8D%A1%E7%9A%84FATFS%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86%E7%B3%BB%E7%BB%9F/FLASHF002.png" alt="FatFs文件目录"></p>
<p>打开src文件夹，如图：<br><img src="/2025/03/19/%E5%9F%BA%E4%BA%8ESD%E5%8D%A1%E7%9A%84FATFS%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86%E7%B3%BB%E7%BB%9F/FLASHF004.png" alt="src文件夹的文件目录"></p>
<p>src文件夹下的源码文件功能简介如下：</p>
<ul>
<li>integer.h：文件中包含了一些数值类型定义。</li>
<li>diskio.c：包含底层存储介质的操作函数，这些函数需要用户自己实现，主要添加底层驱动函数。</li>
<li>ff.c： FatFs核心文件，文件管理的实现方法。该文件独立于底层介质操作文件的函数，利用这些函数实现文件的读写。</li>
<li>cc936.c：本文件在option目录下，是简体中文支持所需要添加的文件，包含了简体中文的GBK和Unicode相互转换功能函数。</li>
<li>ffconf.h: 这个头文件包含了对FatFs功能配置的宏定义，通过修改这些宏定义就可以裁剪FatFs的功能。如需要支持简体中文， 需要把 ffconf.h中的_CODE_PAGE 的宏改成936并把上面的cc936.c文件加入到工程之中。</li>
</ul>
<p><strong>建议阅读这些源码的顺序为： integer.h –&gt; diskio.c –&gt; ff.c。 阅读文件系统源码ff.c文件需要一定的功底，建议读者先阅读FAT32的文件格式，再去分析ff.c文件。若仅为使用文件系统， 则只需要理解integer.h及diskio.c文件并会调用ff.c文件中的函数就可以了。</strong> </p>
<h4 id="4、FATFS结构体图"><a href="#4、FATFS结构体图" class="headerlink" title="4、FATFS结构体图"></a>4、FATFS结构体图</h4><p><img src="/2025/03/19/%E5%9F%BA%E4%BA%8ESD%E5%8D%A1%E7%9A%84FATFS%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86%E7%B3%BB%E7%BB%9F/FLASHF005.png" alt="FatFs程序结构图"></p>
<p>用户应用程序需要由用户编写，想实现什么功能就编写什么的程序，一般我们只用到f_mount()、f_open()、f_write()、f_read()就可以实现文件的读写操作。</p>
<p>FatFs组件是FatFs的主体，文件都在源码src文件夹中，其中ff.c、ff.h、integer.h以及diskio.h四个文件我们不需要改动，只需要修改ffconf.h和diskio.c两个文件。</p>
<h1 id="3、SDIO-SD卡"><a href="#3、SDIO-SD卡" class="headerlink" title="3、SDIO - SD卡"></a>3、SDIO - SD卡</h1><h3 id="1、SDIO简介"><a href="#1、SDIO简介" class="headerlink" title="1、SDIO简介"></a>1、SDIO简介</h3><p>微控制器通过两种方式控制SD卡：SPI接口、SDIO接口。</p>
<p>SDIO全称安全数字输入&#x2F;输出接口，多媒体（MMC）、SD卡、SDI&#x2F;O都有SDIO接口。STM32F4系列有一个SDIO主机接口。并且一般设计SD I&#x2F;O卡是可以插入到SD的插槽。</p>
<p>SD卡协会网站 <a href="http://www.sdcard.org/">www.sdcard.org</a> 中提供了SD存储卡和SDIO卡系统规范。</p>
<p><img src="/2025/03/19/%E5%9F%BA%E4%BA%8ESD%E5%8D%A1%E7%9A%84FATFS%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86%E7%B3%BB%E7%BB%9F/SDIO002.png" alt="SDIO接口的设备"></p>
<h4 id="2、SD卡物理结构"><a href="#2、SD卡物理结构" class="headerlink" title="2、SD卡物理结构"></a>2、SD卡物理结构</h4><p>一张SDka包括：</p>
<ul>
<li>存储单元<ul>
<li>存储数据的地方。</li>
<li>存储单元通过存储单元接口与卡控制单元进行数据传输。</li>
</ul>
</li>
<li>存储单元接口</li>
<li>电源检测<ul>
<li>电源检测控制SD卡在一个合适电压环境下。</li>
</ul>
</li>
<li>卡及接口控制器<ul>
<li>控制SD卡的运行状态。</li>
<li>包含8个寄存器。</li>
</ul>
</li>
<li>接口驱动器。<ul>
<li>控制引脚的输入输出。</li>
</ul>
</li>
</ul>
<p><img src="/2025/03/19/%E5%9F%BA%E4%BA%8ESD%E5%8D%A1%E7%9A%84FATFS%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86%E7%B3%BB%E7%BB%9F/SDIO003.png" alt="SD卡物理结构"></p>
<p>SD卡总共8个寄存器，用于设定和表示SD的信息。这些寄存器只能通过相对因的命令访问。</p>
<p>SD卡的寄存器有些特殊，需要通过命令才能控制，SDIO中设定了64个命令，每个命令都有自己都有的意义。SD卡会根据接收到命令对SD卡进行配置。</p>
<p><img src="/2025/03/19/%E5%9F%BA%E4%BA%8ESD%E5%8D%A1%E7%9A%84FATFS%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86%E7%B3%BB%E7%BB%9F/SDIO01.png" alt="SD卡寄存器"></p>
<h4 id="3、SDIO总线"><a href="#3、SDIO总线" class="headerlink" title="3、SDIO总线"></a>3、SDIO总线</h4><p>SD卡支持SPI和SDIO两种接口。我们治理用到SDIO接口操作方式。</p>
<p>SDIO总线的结构如图，虽然可以共用总线， 但不推荐多卡槽共用总线信号，要求一个单独SD总线应该连接一个单独的SD卡。</p>
<p><img src="/2025/03/19/%E5%9F%BA%E4%BA%8ESD%E5%8D%A1%E7%9A%84FATFS%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86%E7%B3%BB%E7%BB%9F/SDIO004.png" alt="SD卡总线拓扑"></p>
]]></content>
      <categories>
        <category>stm32F407</category>
      </categories>
      <tags>
        <tag>stm32F407</tag>
        <tag>多媒体项目</tag>
      </tags>
  </entry>
  <entry>
    <title>基于WVP-GB28181-POR[STM32MP157]局域网分屏监控</title>
    <url>/2025/02/26/%E5%9F%BA%E4%BA%8EWVP-GB28181-POR-STM32MP157-%E5%B1%80%E5%9F%9F%E7%BD%91%E5%88%86%E5%B1%8F%E7%9B%91%E6%8E%A7/</url>
    <content><![CDATA[<p><a href="https://www.yuque.com/daoge-r0f6y/xzzl3u/gn1f9lgyrf5bpxzh">https://www.yuque.com/daoge-r0f6y/xzzl3u/gn1f9lgyrf5bpxzh</a></p>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>内存管理</title>
    <url>/2025/03/06/%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<h1 id="内存管理"><a href="#内存管理" class="headerlink" title="内存管理"></a>内存管理</h1><p>前面学习了FSMC外设驱动外部SRAM，拓展STM32F407的内存，而且STM32F407本身就有192K的内存。</p>
<p>下面是对内存的动态管理。</p>
<span id="more"></span>

<h3 id="1、内存管理简介"><a href="#1、内存管理简介" class="headerlink" title="1、内存管理简介"></a>1、内存管理简介</h3><p>内存管理就是，在软件运行时对计算机的内存资源进行分配和使用。主要目的就是如何高效、快速的分配，并在适当的时候回收这些资源。内存管理的实现方法有很多中，主要实现的是两个 函数：malloc、free。</p>
<p>下面介绍一个一个简单的方法来实现：分块式内存管理。</p>
<p><img src="/2025/03/06/%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/094306.png"></p>
<p>从图片中，可以看出内存管理分为内存池和内存管理表。内存池和内存管理表都分为N块，并且一一对应。</p>
<p>其中内存管理表的项值代表着内存池中占用的数量。当项值为0时，表示未有内存被占用。当项值为10，表示有10个内存块被占用。</p>
<p>内存分配的方向是从顶到低。即寻找最低端的空内存。当内存初始化时，内存表会被清空。</p>
<p><strong>分配原理</strong> </p>
<p>当指针调用malloc内存分配函数时，首先要知道指针需要的内存块数，然后从第N项开始，向下寻找，知道找到指针需要的内存块数，最后，把这个空内存块的地址放回给指针。如果内存不足，则返回NULL。</p>
<p><strong>释放原理</strong> </p>
<p>当指针的内存使用完成后，需要释放时，调用free内存释放函数。free函数会根据传入的指针，找到其指向的内存块，在找到相对应的内存管理表项，根据其项值，知道了使用的内存块数量，然后将这个写内存块全部清零。</p>
]]></content>
      <categories>
        <category>stm32F407</category>
      </categories>
      <tags>
        <tag>多媒体项目</tag>
        <tag>stm32</tag>
      </tags>
  </entry>
  <entry>
    <title>多媒体项目报告</title>
    <url>/2025/03/19/%E5%A4%9A%E5%AA%92%E4%BD%93%E9%A1%B9%E7%9B%AE%E6%8A%A5%E5%91%8A/</url>
    <content><![CDATA[<h1 id="多媒体项目报告"><a href="#多媒体项目报告" class="headerlink" title="多媒体项目报告"></a>多媒体项目报告</h1><p>本项目总结了一个基于STM32F407ZGT6微控制器的多媒体应用，涵盖音频播放、视频播放、相册展示和摄像头功能。以下是项目的详细分析，包括硬件和软件组件、功能实现以及相关技术考量，力求全面反映项目的设计与实现。  </p>
<p>项目背景与概述</p>
<p>STM32F407ZGT6是STMicroelectronics生产的高性能微控制器，基于ARM Cortex-M4核心，运行频率达168 MHz，配备1 MB Flash和192 KB SRAM，适合处理多媒体任务如音频、视频解码和实时显示。该项目利用其强大的计算能力，结合LVGL图形库，开发了一个多功能嵌入式系统，支持音频、视频、图片管理和摄像头操作。用户界面通过3.5英寸TFT LCD呈现，存储介质为SD卡，辅以外部SRAM和W25QXX闪存以提升性能。  </p>
<span id="more"></span>

<p>硬件组件详解</p>
<p>以下是项目中使用的关键硬件及其作用：  </p>
<ul>
<li>微控制器：STM32F407ZGT6 (<a href="https://www.st.com/en/microcontrollers-microprocessors/stm32f407zg.html">STM32F407ZGT6规格</a>)，Cortex-M4核心，168 MHz，1 MB Flash，192 KB SRAM，支持DSP和FPU，适合多媒体处理。  </li>
<li>显示屏：3.5英寸TFT LCD，研究表明可能通过FSMC（Flexible Static Memory Controller）并行接口连接 (<a href="https://controllerstech.com/interface-tft-display-with-stm32/">TFT显示与STM32接口</a>)，确保高带宽需求如视频播放，适合480x272分辨率。  </li>
<li>音频编解码器：WM8978 (<a href="https://www.st.com/resource/en/datasheet/wm8978.pdf">WM8978数据表</a>)，低功耗立体声音频编解码器，支持耳机驱动，适合音频播放。  </li>
<li>摄像头：OV2640 (<a href="https://www.ovt.com/products/sensor/ov2640/">OV2640数据表</a>)，200万像素CMOS摄像头，支持VGA分辨率，捕获实时视频和静态图片。  </li>
<li>存储：SD卡，通过FATFS文件系统访问，存储WAV音频、AVI视频、BMP图片和摄像头捕获的照片。  </li>
<li>外部内存：外部SRAM，用于缓冲大容量数据如视频帧和音频流，缓解内部SRAM的内存压力。  </li>
<li>闪存：W25QXX系列（如W25Q128），可能用于存储固件或额外数据，增强系统灵活性。</li>
</ul>
<p>软件组件与功能实现</p>
<p>项目的软件部分包括多个库和模块，具体功能如下：  </p>
<ul>
<li>GUI库：使用LVGL (<a href="https://docs.lvgl.io/">LVGL文档</a>)，一个开源嵌入式图形库，设计用户界面，支持多屏幕（如音频、视频、相册、摄像头模式），提供按钮和控件交互。  </li>
<li>文件系统：FATFS (<a href="https://github.com/elm-chan/RTOS">FATFS库</a>)，用于SD卡文件访问，支持读取WAV、AVI、BMP文件和保存摄像头照片。  </li>
<li>图像解码：BMP解码库，用于读取和解码SD卡中的BMP图片，显示在LVGL相册界面。  </li>
<li>视频解码：研究表明项目可能使用MJPEG解码播放AVI文件 (<a href="https://community.st.com/t5/stm32-mcus-products/alientek-explorer-stm32f407-board-support-avi-mjpeg-player/m-p/479130">MJPEG视频播放示例</a>)，MJPEG格式适合STM32F407的硬件JPEG编解码器或软件解码，显示在TFT LCD上。  </li>
<li>音频播放：支持WAV文件格式，从SD卡读取音频数据，通过WM8978编解码器播放，LVGL界面提供播放、暂停、停止等控制。  </li>
<li>摄像头驱动：为OV2640摄像头开发驱动，支持实时视频捕获，显示在TFT LCD上，并支持拍照功能，照片保存至SD卡。  </li>
<li>内存管理：项目提到内存管理，可能是通过外部SRAM实现自定义内存分配策略，用于缓冲视频帧、音频流和图片数据，确保系统性能。</li>
</ul>
<p>功能详细描述</p>
<p>以下是四个主要功能的实现方式：  </p>
<ol>
<li>音频播放  <ul>
<li>从SD卡读取WAV文件，通过FATFS访问文件系统。  </li>
<li>音频数据传输至WM8978编解码器进行播放。  </li>
<li>LVGL界面提供播放控制，如播放、暂停、停止，显示当前播放状态。</li>
</ul>
</li>
<li>视频播放  <ul>
<li>从SD卡读取AVI文件（可能为MJPEG格式），通过FATFS访问。  </li>
<li>视频解码可能利用STM32F407的硬件JPEG编解码器或软件库，解码后帧数据通过FSMC接口传输至TFT LCD。  </li>
<li>LVGL界面提供视频播放控制，如播放、暂停、快进，支持分辨率如480x272，帧率可能达25fps（320x240）或10fps（480x272）。</li>
</ul>
</li>
<li>相册展示  <ul>
<li>从SD卡读取BMP图片文件，通过FATFS访问。  </li>
<li>使用BMP解码库解码图片，显示在LVGL设计的相册界面，用户可浏览图片。  </li>
<li>界面可能包括翻页、缩放等功能，依赖TFT LCD的显示能力。</li>
</ul>
</li>
<li>摄像头功能  <ul>
<li>OV2640摄像头捕获实时视频流，数据传输至STM32F407处理。  </li>
<li>视频流通过LVGL显示在TFT LCD上，提供实时预览。  </li>
<li>支持拍照功能，捕获的图片（可能为BMP格式）保存至SD卡，依赖FATFS文件系统。</li>
</ul>
</li>
</ol>
<p>技术考量与挑战</p>
<ul>
<li>内存管理：外部SRAM用于缓冲大容量数据，如视频帧和音频流，确保音频和视频同步播放无卡顿。  </li>
<li>实时性能：视频和音频播放需要优化，可能使用DMA（直接内存访问）加速SD卡读取、显示刷新和音频输出。  </li>
<li>功耗管理：在显示屏、摄像头和音频同时工作时，需优化功耗，适合便携式应用。  </li>
<li>用户界面设计：LVGL界面需设计直观，支持多模式切换（如音频、视频、相册、摄像头），确保用户体验。</li>
</ul>
<p>对比与意想不到的细节</p>
<p>一个意想不到的细节是，项目可能依赖STM32F407的硬件JPEG编解码器来提升AVI视频播放效率，这在嵌入式系统中并不常见，但适合处理MJPEG格式的视频，显著降低CPU负载。相比传统软件解码，这种方法在性能上更有优势，尤其在分辨率较高时。  </p>
<p>以下是硬件和软件组件的总结表：  </p>
<table>
<thead>
<tr>
<th>组件类别</th>
<th>具体组件</th>
<th>主要作用</th>
</tr>
</thead>
<tbody><tr>
<td>微控制器</td>
<td>STM32F407ZGT6</td>
<td>核心处理单元，运行频率168 MHz</td>
</tr>
<tr>
<td>显示</td>
<td>3.5英寸TFT LCD</td>
<td>显示LVGL界面、视频和摄像头画面</td>
</tr>
<tr>
<td>音频</td>
<td>WM8978</td>
<td>音频播放，低功耗立体声编解码器</td>
</tr>
<tr>
<td>摄像头</td>
<td>OV2640</td>
<td>实时视频捕获和拍照，200万像素</td>
</tr>
<tr>
<td>存储</td>
<td>SD卡</td>
<td>存储WAV、AVI、BMP文件和照片</td>
</tr>
<tr>
<td>外部内存</td>
<td>外部SRAM</td>
<td>缓冲视频帧、音频流，扩展内存</td>
</tr>
<tr>
<td>闪存</td>
<td>W25QXX（如W25Q128）</td>
<td>可能存储固件或额外数据</td>
</tr>
<tr>
<td>GUI库</td>
<td>LVGL</td>
<td>设计用户界面，支持多功能交互</td>
</tr>
<tr>
<td>文件系统</td>
<td>FATFS</td>
<td>SD卡文件访问，读写音频、视频、图片</td>
</tr>
<tr>
<td>图像解码</td>
<td>BMP解码库</td>
<td>解码BMP图片，显示相册</td>
</tr>
<tr>
<td>视频解码</td>
<td>可能MJPEG解码</td>
<td>解码AVI视频，显示在TFT LCD上</td>
</tr>
<tr>
<td>音频播放</td>
<td>WAV支持</td>
<td>播放WAV文件，通过WM8978输出</td>
</tr>
<tr>
<td>摄像头驱动</td>
<td>OV2640驱动</td>
<td>实时视频显示和拍照，保存至SD卡</td>
</tr>
<tr>
<td>内存管理</td>
<td>自定义策略</td>
<td>利用外部SRAM优化内存分配</td>
</tr>
</tbody></table>
<p>结论</p>
<p>本项目展示了STM32F407ZGT6在多媒体应用中的强大能力，通过LVGL设计用户界面，结合SD卡、TFT LCD、WM8978和OV2640实现音频、视频、相册和摄像头功能。内存管理依赖外部SRAM，视频播放可能利用硬件JPEG编解码器，体现了嵌入式系统的高效设计。  </p>
]]></content>
      <categories>
        <category>stm32F407</category>
      </categories>
      <tags>
        <tag>stm32F407</tag>
        <tag>多媒体项目</tag>
      </tags>
  </entry>
  <entry>
    <title>杂项设备</title>
    <url>/2025/01/14/%E6%9D%82%E9%A1%B9%E8%AE%BE%E5%A4%87/</url>
    <content><![CDATA[<h1 id="杂项设备的使用流程"><a href="#杂项设备的使用流程" class="headerlink" title="杂项设备的使用流程"></a>杂项设备的使用流程</h1><h5 id="1、杂项设备（misc-device）"><a href="#1、杂项设备（misc-device）" class="headerlink" title="1、杂项设备（misc device）"></a>1、杂项设备（misc device）</h5><ul>
<li><p>概念：</p>
<p> 杂项设备是linux内核提供的一种字符设备驱动矿建，用于管理那些功能比较简单、数量较少的设备。</p>
</li>
<li><p>特点：</p>
<p>  主设备号固定为10，通过次设备号来区分不同的杂项设备。</p>
<p>  结构简单</p>
<p>  提供基本的读写操作接口</p>
</li>
</ul>
<span id="more"></span>

<h5 id="2、驱动开发流程及其讲解"><a href="#2、驱动开发流程及其讲解" class="headerlink" title="2、驱动开发流程及其讲解"></a>2、驱动开发流程及其讲解</h5><p>​	1、包含头文件</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &lt;linux/module.h&gt;</span><br><span class="line">#include &lt;linux/fs.h&gt;</span><br><span class="line">#include &lt;linux/miscdevice.h&gt;</span><br></pre></td></tr></table></figure>

<p>​	2、定义文件操作结构体</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">const struct file_operations led_fops = &#123;</span><br><span class="line">    .owner = THIS_MODULE,</span><br><span class="line">    .open   = led_pdrv_open,</span><br><span class="line">    .write  = led_pdrv_write,</span><br><span class="line">    .release  = led_pdrv_close,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>​	3、定义杂项设备结构体</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct miscdevice misc;</span><br><span class="line"></span><br><span class="line">struct miscdevice  &#123;</span><br><span class="line">	int minor;</span><br><span class="line">	const char *name;</span><br><span class="line">	const struct file_operations *fops;</span><br><span class="line">	struct list_head list;</span><br><span class="line">	struct device *parent;</span><br><span class="line">	struct device *this_device;</span><br><span class="line">	const struct attribute_group **groups;</span><br><span class="line">	const char *nodename;</span><br><span class="line">	umode_t mode;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>​	参数：</p>
<pre><code>	* minor：次设备号。
	* name：设备节点名称。
	* fops：文件操作结构体指针。
	* 其他的用到的不多，用到可以在搜。
</code></pre>
<p>​	4、注册杂项设备</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">    ret = misc_register(&amp;led_dev-&gt;misc);</span><br><span class="line">    if (ret) &#123;</span><br><span class="line">        printk(&quot;misc register failed\n&quot;);</span><br><span class="line">        goto err_kfree;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">int misc_register(struct miscdevice *misc)</span><br><span class="line">&#123;</span><br><span class="line">	dev_t dev;</span><br><span class="line">	int err = 0;</span><br><span class="line">	bool is_dynamic = (misc-&gt;minor == MISC_DYNAMIC_MINOR);</span><br><span class="line"></span><br><span class="line">	INIT_LIST_HEAD(&amp;misc-&gt;list);</span><br><span class="line"></span><br><span class="line">	mutex_lock(&amp;misc_mtx);</span><br><span class="line"></span><br><span class="line">	if (is_dynamic) &#123;</span><br><span class="line">		int i = find_first_zero_bit(misc_minors, DYNAMIC_MINORS);</span><br><span class="line"></span><br><span class="line">		if (i &gt;= DYNAMIC_MINORS) &#123;</span><br><span class="line">			err = -EBUSY;</span><br><span class="line">			goto out;</span><br><span class="line">		&#125;</span><br><span class="line">		misc-&gt;minor = DYNAMIC_MINORS - i - 1;</span><br><span class="line">		set_bit(i, misc_minors);</span><br><span class="line">	&#125; else &#123;</span><br><span class="line">		struct miscdevice *c;</span><br><span class="line"></span><br><span class="line">		list_for_each_entry(c, &amp;misc_list, list) &#123;</span><br><span class="line">			if (c-&gt;minor == misc-&gt;minor) &#123;</span><br><span class="line">				err = -EBUSY;</span><br><span class="line">				goto out;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	dev = MKDEV(MISC_MAJOR, misc-&gt;minor);</span><br><span class="line"></span><br><span class="line">	misc-&gt;this_device =</span><br><span class="line">		device_create_with_groups(misc_class, misc-&gt;parent, dev,</span><br><span class="line">					  misc, misc-&gt;groups, &quot;%s&quot;, misc-&gt;name);</span><br><span class="line">	if (IS_ERR(misc-&gt;this_device)) &#123;</span><br><span class="line">		if (is_dynamic) &#123;</span><br><span class="line">			int i = DYNAMIC_MINORS - misc-&gt;minor - 1;</span><br><span class="line"></span><br><span class="line">			if (i &lt; DYNAMIC_MINORS &amp;&amp; i &gt;= 0)</span><br><span class="line">				clear_bit(i, misc_minors);</span><br><span class="line">			misc-&gt;minor = MISC_DYNAMIC_MINOR;</span><br><span class="line">		&#125;</span><br><span class="line">		err = PTR_ERR(misc-&gt;this_device);</span><br><span class="line">		goto out;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	/*</span><br><span class="line">	 * Add it to the front, so that later devices can &quot;override&quot;</span><br><span class="line">	 * earlier defaults</span><br><span class="line">	 */</span><br><span class="line">	list_add(&amp;misc-&gt;list, &amp;misc_list);</span><br><span class="line"> out:</span><br><span class="line">	mutex_unlock(&amp;misc_mtx);</span><br><span class="line">	return err;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​	5、注销杂项设备</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">misc_deregister(&amp;led_dev-&gt;misc);</span><br></pre></td></tr></table></figure>



<h5 id="3、杂项设备的使用"><a href="#3、杂项设备的使用" class="headerlink" title="3、杂项设备的使用"></a>3、杂项设备的使用</h5><ul>
<li><p>创建设备节点：</p>
<p> 注册杂项设备后，在系统的&#x2F;dev&#x2F;目录下会自动创建一个设备节点。</p>
</li>
<li><p>应用程序访问：</p>
<p>  应用程序可以通过系统调用（如open、write、read）来访问该设备节点，从而与驱动进行交互。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Linux驱动</category>
      </categories>
      <tags>
        <tag>Linux学习</tag>
        <tag>杂项设备</tag>
        <tag>平台总线</tag>
      </tags>
  </entry>
  <entry>
    <title>嵌入式八股文</title>
    <url>/2025/01/28/%E5%B5%8C%E5%85%A5%E5%BC%8F%E5%85%AB%E8%82%A1%E6%96%87/</url>
    <content><![CDATA[<h1 id="嵌入式八股文"><a href="#嵌入式八股文" class="headerlink" title="嵌入式八股文"></a>嵌入式八股文</h1><span id="more"></span>

<h3 id="运算符"><a href="#运算符" class="headerlink" title="运算符"></a>运算符</h3><ul>
<li><p>算术运算符</p>
<ul>
<li>+：加法</li>
<li>-：减法</li>
<li>*：乘法</li>
<li>&#x2F;：除法</li>
<li>%：取模</li>
</ul>
</li>
<li><p>关系运算符</p>
<ul>
<li>&#x3D;&#x3D;：等于</li>
<li>！&#x3D;：不等于</li>
<li><code>&gt;</code>：大于</li>
<li>&lt;：小于</li>
<li><code>&gt;=</code>：大于等于</li>
<li><code>&lt;=</code>：小于等于</li>
</ul>
</li>
<li><p>逻辑运算符：</p>
<ul>
<li>&amp;&amp;：逻辑与</li>
<li>||：逻辑或</li>
<li>！：逻辑非</li>
</ul>
</li>
<li><p>位运算符</p>
<ul>
<li>&amp;：按位与</li>
<li>|：按位或</li>
<li>^：按位非</li>
<li>~：按位取反</li>
<li>&lt;&lt;：左移</li>
<li><code>&gt;&gt;</code>：右移</li>
</ul>
</li>
<li><p>赋值运算符</p>
<ul>
<li><code>=</code>：简单赋值</li>
<li><code>+=</code>：加后赋值</li>
<li><code>-=</code>：减后赋值</li>
<li><code>*=</code>：乘后赋值</li>
<li><code>/=</code>：除后赋值</li>
<li><code>%=</code>：取模后赋值</li>
<li><code>&lt;&lt;=</code>：左移后赋值</li>
<li><code>&gt;&gt;=</code>：右移后赋值</li>
<li><code>&amp;=</code>：按位与后赋值</li>
<li><code>|=</code>：按位或后赋值</li>
<li><code>^=</code>：按位异或后赋值</li>
</ul>
</li>
<li><p>条件运算符</p>
<ul>
<li>？：：三元运算符</li>
</ul>
</li>
<li><p>其他运算符</p>
<ul>
<li>sizeof：返回变量或者数据类型的字节数</li>
<li>&amp;：取地址运算符</li>
<li>*：取值运算符</li>
</ul>
</li>
<li><p>运算符优先级</p>
<p>  C语言中的运算符优先级决定了表达式中运算的顺序。</p>
<ul>
<li>括号（）的优先级最高</li>
<li>算数运算符的优先级高于关系运算符</li>
<li>简单的记：（）》 ！》算术运算符》关系运算符》&amp;&amp; 》|| 》复制运算符</li>
</ul>
</li>
</ul>
<h3 id="源码、补码、反码"><a href="#源码、补码、反码" class="headerlink" title="源码、补码、反码"></a>源码、补码、反码</h3><p>在计算机中，源码、补码、反码是表示有符号整数的三种形式。他们只要用于解决计算机中负数的表示和运算问题。</p>
<ul>
<li>源码：最直观的表示方式。用一个符号位和数值为表示一个有符号的整数。</li>
<li>反码：反码是为了解决源码中加减运算的问题而提出的。<ul>
<li>正数的反码和源码相同</li>
<li>负数的符号位位1，数值按位取反。</li>
</ul>
</li>
<li>补码：解决了反码中两个零的问题。<ul>
<li>正数：补码与源码相同。</li>
<li>负数：符号位为1，数值按位取反加一。</li>
</ul>
</li>
</ul>
<p>在源码中，加减的运算规则：</p>
<ul>
<li>判断符号位<ul>
<li>如果符号位相同：直接对数值进行加减法运算。</li>
<li>如果不相同，需要比较两个数的绝对值，用较大的绝对值减去较小的绝对值，结果的符号位和绝对值较大的数相同。</li>
<li>如果加法运算中产生了进位，则需要调整结果。</li>
</ul>
</li>
</ul>
<p>在源码运算时，运算比较复杂，而且存在两个0的问题，在源码中存在+0和-0，导致比较时和判断时需要额外的处理。反码和补码通过改进负数的表示方法，简化了加减运算，成为现代计算机中表示有符号整数的标准方法。</p>
<p>在反码中，核心思想是将减法转换为加法。在反码中，<code>A - B</code> 可以转换为 <code>A + (-B)</code>，其中 <code>-B</code> 是 <code>B</code> 的反码表示。</p>
<h4 id="题目："><a href="#题目：" class="headerlink" title="题目："></a>题目：</h4><ul>
<li><pre><code>  int a = 5,b =3;
  !a &amp;&amp; (b++);
  printf(&quot;%d,%d\n&quot;, a, b); //5,3
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">    ！a是对a进行取反，a=5是正数，！逻辑非运算符，！a=0；</span><br><span class="line"></span><br><span class="line">    &amp;&amp;逻辑与，但是！a为false，所以（b++）不执行</span><br><span class="line"></span><br><span class="line">    所以最后a=5，b=3.！a是对a进行逻辑判断，不赋值。所以a不变。</span><br><span class="line"></span><br><span class="line">* ```</span><br><span class="line">    #include &lt;stdio.h&gt;</span><br><span class="line">    int main()</span><br><span class="line">    &#123;</span><br><span class="line">    int a = -7;</span><br><span class="line">    unsigned int b = 3;</span><br><span class="line">    if( (a + b) &gt; 0) //如果打印‘+’说明 a+b &gt; 0,即有符号转⽆符号运算的时候</span><br><span class="line">    &#123;</span><br><span class="line">    printf(&quot;+\n&quot;); //+</span><br><span class="line">    &#125;</span><br><span class="line">    printf(&quot;%u\n&quot;,a + b);//4294967295</span><br><span class="line">    printf(&quot;%d\n&quot;,a + b);//-1</span><br><span class="line">    return 0;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

  在 C 语言中，当有符号整数和无符号整数进行运算时，**有符号整数会被隐式转换为无符号整数**，然后再进行运算。

    11111111 11111111 11111111 11111001 (-7 的补码)

  +00000000 00000000 00000000 00000011 (3 的无符号表示)

  -------------------------------------
   11111111 11111111 11111111 11111100 (结果的二进制表示)

  - 如果解释为**无符号整数**，其值为 `4294967292`。
  - 如果解释为**有符号整数**（补码），其值为 `-4`。
</code></pre>
</li>
</ul>
<h3 id="关键字"><a href="#关键字" class="headerlink" title="关键字"></a>关键字</h3><h5 id="Continue"><a href="#Continue" class="headerlink" title="Continue"></a>Continue</h5><p>​	在C语言中，continue是一个控制语句，主要用于循环结构（比如for、while、do-while循环）。<strong>它的作用是跳过当前循环的剩余部分，直接进行下一次的迭代</strong>。</p>
<h5 id="Break"><a href="#Break" class="headerlink" title="Break"></a>Break</h5><p>​	在C语言中，break是一个控制语句。</p>
<p>作用：</p>
<ul>
<li>终止循环：在循环结构中，break语句会立即终止整个循环。</li>
<li>退出switch语句：在switch语句中，break用于结束当前的case块。</li>
</ul>
<p>注意点：</p>
<ul>
<li>break在多层循环嵌套中，只退出当前循环。</li>
</ul>
<h5 id="return"><a href="#return" class="headerlink" title="return"></a>return</h5><p>​	用法和作用：</p>
<ul>
<li><strong>结束函数执行，return语句会立即结束当前函数的执行</strong>，并将控制前返回到调用函数的地方。</li>
<li>返回值：可以将一个值或者表达式的计算结果返回给调用者。如果函数声明了返回类型，通常返回一个同样类型的一个值。</li>
<li>多返回点：一个函数可以有多个return语句。</li>
</ul>
<p>注意点：</p>
<ul>
<li><strong>必须返回值</strong>：如果函数声明了非void的返回类型，则在所有可能的执行路径上都需要有一个return语句，否则编译器可能会发出警告或错误。</li>
<li><strong>返回类型一致性</strong>：返回的值或表达式必须与函数声明中的返回类型相匹配，或者通过隐式转换匹配。</li>
<li><strong>资源管理</strong>：在return之前，确保释放或正确管理分配的资源（如内存、文件句柄等）。</li>
<li><strong>优化</strong>：编译器可能会对return语句进行尾调用优化，特别是在递归函数中，这可以提高性能。</li>
<li><strong>早期返回</strong>：有时为了减少嵌套或提高代码可读性，开发者会使用return提前结束函数执行，这被称为“提前返回”。</li>
</ul>
<h5 id="volatile"><a href="#volatile" class="headerlink" title="volatile"></a>volatile</h5><p>在C语言中，volatile是一个类型限定符，用于告知编译器某些变量的值可能会在代码的显示操作之外发生变化。</p>
<p>用法和作用：</p>
<ul>
<li><strong>防止优化</strong>：volatile 告诉编译器不要对该变量进行优化。编译器在处理非volatile变量时，可能会假设变量值在没有显式修改的情况下不会改变，从而进行优化（如寄存器缓存）。<strong>对于volatile变量，编译器每次使用时都会从内存中重新读取值。</strong></li>
<li><strong>硬件交互</strong>：常用于与硬件相关的编程，如访问内存映射的I&#x2F;O设备、硬件寄存器、中断等，这些地方的值可能在软件控制之外被改变。</li>
<li><strong>多线程环境</strong>：在多线程或多任务的系统中，一个线程可能修改一个变量，而另一个线程需要看到这个变化。volatile确保线程能够读取最新的值。</li>
</ul>
<p>注意点：</p>
<ul>
<li><strong>不提供原子性</strong>：volatile 只保证编译器不优化对变量的访问，但不提供任何形式的原子操作或线程安全。如果需要这些保证，需要使用其他机制如互斥锁或原子操作。</li>
<li><strong>不应滥用</strong>：滥用volatile可能导致性能下降，因为它会阻止编译器进行一些优化。应该只在确实需要的地方使用它。</li>
<li><strong>与const一起使用</strong>：可以声明const volatile变量，这表示变量值可能被外部因素改变，但程序本身不会去改变它。</li>
<li><strong>内存屏障</strong>：虽然volatile确保每次读取或写入变量时都会从内存获取或更新值，但它不是内存屏障。复杂的多线程场景可能需要使用内存屏障指令。</li>
<li><strong>理解与volatile相关的优化</strong>：虽然volatile会阻止某些优化，但理解编译器的优化策略对于正确使用volatile非常重要。例如，编译器可能仍然会重排序指令，除非有显式的内存屏障。</li>
</ul>
<p>题目：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Volatile int a=20,b,c;</span><br><span class="line">b=a;</span><br><span class="line">C=a;</span><br></pre></td></tr></table></figure>

<ul>
<li>如果没有volatile：<ul>
<li>编译器读取 a 的值 20 到一个寄存器中（假设编译器决定这样做）。</li>
<li>编译器可以直接从寄存器中取值，将 20 赋给 b。这意味着没有必要再次从内存中读取 a。</li>
<li>同样，编译器可能会选择从寄存器中取值，而不是从内存中再次读取 a，因为没有 volatile 表明 a 的值可能在两次读取之间发生了变化。因此，c 也会得到 20。</li>
</ul>
</li>
<li>如果有volatile：<ul>
<li>都是从内存中读取a的值。</li>
</ul>
</li>
</ul>
<h5 id="计算占用空间的大小问题"><a href="#计算占用空间的大小问题" class="headerlink" title="计算占用空间的大小问题"></a>计算占用空间的大小问题</h5><p>在C语言中，计算数据结构占用空间的大小是编程中一个常见任务。理解如何计算这些大小对于内存管理、性能优化和理解程序至关重要。</p>
<p>字节：C语言中，最小的可寻址内存单元通常为字节。</p>
<p>对齐：为了提高内存访问效率，编译器可能对数据进行对齐，使得数据的内存地址满足某些规则。</p>
<p>使用sizeof操作字符可以计算其参数的内存大小，返回是size—t类型。</p>
<p>对于不同的操作系统，个别数据类型大小不一样。</p>
<ul>
<li>char：通常是1字节</li>
<li>short：通产是2字节</li>
<li>int：可能是2、4、8字节，通产为4字节</li>
<li>long：可能是4、8字节</li>
<li>long long：通常为8字节</li>
<li>float：通常为4字节</li>
<li>double：通常为8字节</li>
</ul>
<p>计算的时候需要考虑字节对齐的问题：</p>
<ul>
<li>所占空间必须是成员变量中字节最大的整数被。</li>
<li>每个变量类型的偏移量必须是该变量类型的整数倍</li>
<li>对于来联合体来说，是所有变量成员公用一块内存，结构体则是内存的叠加。</li>
</ul>
<h5 id="题目：-1"><a href="#题目：-1" class="headerlink" title="题目："></a>题目：</h5><ul>
<li><pre><code>  以下哪个语句可以正确计算数组arr的总大小，假设arr是一个包含10个元素的int型数组？
  A) sizeof(arr[10])
  B) sizeof(int) * 10
  C) sizeof(arr)
  D) sizeof(arr) / 10
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">    解答：</span><br><span class="line"></span><br><span class="line">    * A选项，给出的是数组中第11个元素的大小，但是arr只有10个元素。</span><br><span class="line">    * B选项，结果是对的，但是过程不对。</span><br><span class="line">    * C选项，真确，sizeof作用于数组名时，返回的是整个数组的大小</span><br><span class="line">    * D选项，可以得到单个元素的大小。</span><br><span class="line"></span><br><span class="line">* ```</span><br><span class="line">    联合体union &#123; char c; int i; &#125;的大小在大多数系统上是____字节。</span><br></pre></td></tr></table></figure>

  解答：

  * 联合体的内存大小取决于其成员最大的那个成员，因为所有成员共享一块内存。
  * char占用1字节，int占用4字节，所以答案为4字节。
</code></pre>
</li>
<li><pre><code>  声明一个包含5个double型元素的数组，并使用sizeof运算符计算并打印出这个数组的总大小。
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">解答：</span><br><span class="line"></span><br><span class="line">* ```</span><br><span class="line">    double arr[5];</span><br><span class="line">    printf(&quot;%zu&quot;, sizeof(arr)); // 使用%zu来打印size_t类型的值</span><br></pre></td></tr></table></figure>
</code></pre>
</li>
<li><pre><code>  编写一个C程序，定义一个结构体，包含一个char，一个int和一个double。然后计算并打印这个结构体的大小，以及各个成员的单独大小，验证是否考虑了对齐。
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">解答：</span><br><span class="line"></span><br><span class="line">* ```</span><br><span class="line">    #include &lt;stdio.h&gt;</span><br><span class="line">    </span><br><span class="line">    struct arr &#123;</span><br><span class="line">        char a;</span><br><span class="line">        int b;</span><br><span class="line">        double c; // 修正了缺少的成员名</span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line">    int main() &#123;</span><br><span class="line">        printf(&quot;Structure size: %zu bytes\n&quot;, sizeof(struct arr));</span><br><span class="line">        printf(&quot;Size of char: %zu bytes\n&quot;, sizeof(char));</span><br><span class="line">        printf(&quot;Size of int: %zu bytes\n&quot;, sizeof(int));</span><br><span class="line">        printf(&quot;Size of double: %zu bytes\n&quot;, sizeof(double));</span><br><span class="line">    </span><br><span class="line">        if (sizeof(struct arr) == 16) &#123;</span><br><span class="line">            printf(&quot;字节对齐已考虑。\n&quot;);</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            printf(&quot;未考虑字节对齐或对齐不同。\n&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    </span><br><span class="line">        return 0;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
</code></pre>
</li>
</ul>
<h5 id="Typedef和define"><a href="#Typedef和define" class="headerlink" title="Typedef和define"></a>Typedef和define</h5><ul>
<li><p>#define</p>
<p>  定义：#define是C语言预处理指令，用于定义宏。宏定义允许你给一个名称或者表达式指定一个值或者代码片段，<strong>在编译前进行文本替换</strong>。</p>
<p>  例子：</p>
<ul>
<li><p>文本替换：宏定义只是简单的文本替换，不进行类型检查。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define PI 3.1415926</span><br></pre></td></tr></table></figure>
</li>
<li><p>宏函数：定义带参数的宏，即宏函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define SQUARE(X) ((X) * (X))</span><br></pre></td></tr></table></figure>
</li>
<li><p>作用域：宏定义的作用域式从定义点开始，直到文件结束或者遇到#undef指令为止。</p>
</li>
</ul>
</li>
<li><p>typedef</p>
<p>  定义：用于存在的类型定义一个新的名称的关键字。它在编译时创建一个新的类型名，而不是进行文本替换。</p>
<ul>
<li><p>类型别名</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef int myint;</span><br><span class="line">myint number = 5;</span><br></pre></td></tr></table></figure>
</li>
<li><p>结构体和联合体的别名</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef struct &#123;</span><br><span class="line">	int x;</span><br><span class="line">	int y;</span><br><span class="line">&#125; point;</span><br><span class="line">point p;// 比写 struct &#123; ... &#125; p; 更简洁</span><br></pre></td></tr></table></figure>
</li>
<li><p>指针类型别名</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef char * string;</span><br><span class="line">string str = &quot;hello&quot;;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<h5 id="Const"><a href="#Const" class="headerlink" title="Const"></a>Const</h5><p>用于声明常量。其主要用途是告知编译器某些值在程序的执行过程不应该再改变。</p>
<p>基本用法：</p>
<ul>
<li><p>常量变量：const可以用来定义一个不能被改变的变量。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">const int max = 100;//max是一个常量，并且不可被修改。</span><br></pre></td></tr></table></figure>
</li>
<li><p>指针和常量</p>
<ul>
<li><p>指向常量的指针。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int a = 10;</span><br><span class="line">const int *ptr = &amp;a;</span><br><span class="line">ptr = &amp;b;</span><br><span class="line">//const 修饰的是int *，所以ptr的类型不变。</span><br></pre></td></tr></table></figure>
</li>
<li><p>常量指针</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int a = 10;</span><br><span class="line">int *const ptr = &amp;a;</span><br><span class="line">*ptr = 20;</span><br><span class="line">//const修饰的ptr，所以变量ptr不可改变。</span><br></pre></td></tr></table></figure>
</li>
<li><p>指向常量的常量指针</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int a = 10;</span><br><span class="line">const int * const ptr = &amp;a;</span><br></pre></td></tr></table></figure>
</li>
<li><p>记忆技巧</p>
<ul>
<li><strong>const在前</strong>（紧挨着类型名）：表示指向的数据是常量。</li>
<li><strong>const在后</strong>（紧挨着指针名）：表示指针本身是常量。</li>
</ul>
</li>
</ul>
</li>
<li><p>使用场景</p>
<ul>
<li><p>接口设计：在函数参数中使用const可以保证函数不会修改传入的参数。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void print_array(const int *arr, int size) &#123;</span><br><span class="line">    // 保证函数不会修改arr的值</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>保护数据：使用const来保护数据不会被轻易的改变。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">const double PI = 3.14159; // 常量PI不应被修改</span><br></pre></td></tr></table></figure>
</li>
<li><p>全局变量：定义全局变量，避免在多个文件中重复定义。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// in a header file</span><br><span class="line">extern const int MAX_USERS;</span><br><span class="line"></span><br><span class="line">// in one source file</span><br><span class="line">const int MAX_USERS = 1000;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>注意事项</p>
<ul>
<li><p>const必须在定义时初始化。</p>
</li>
<li><p>与volatile连用：const volatile 表示一个变化值不可由程序修改但是可能由硬件或者其他外部原因而改变。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">const volatile int *ptr; // ptr指向的值可能由外部更改，但程序不能修改它</span><br></pre></td></tr></table></figure>
</li>
<li><p>与宏定义连用：const提供了安全性。</p>
</li>
</ul>
</li>
<li><p><strong>分析如下const关键字的用法（刀哥版）</strong> </p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">const int* ptr;</span><br><span class="line">int const* ptr; </span><br><span class="line">int* const ptr; </span><br><span class="line">const int* const ptr; </span><br></pre></td></tr></table></figure>

  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">const int* ptr</span><br><span class="line">	指向常量的指针，指针指向的是常量，指针可变</span><br><span class="line">	</span><br><span class="line">int const* ptr</span><br><span class="line">	指向常量的指针</span><br><span class="line">	</span><br><span class="line">int * const ptr</span><br><span class="line">	指向整型的常量指针，指针指向的是整形的，可变；但是指针是常量，不可变</span><br><span class="line">	</span><br><span class="line">const int * const ptr</span><br><span class="line">	指向常量的常量指针，指针和指针指向的数据都是常量，不可变。</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="extern（外部链接）"><a href="#extern（外部链接）" class="headerlink" title="extern（外部链接）"></a>extern（外部链接）</h5><ul>
<li><p>定义：extern是一个关键字，用于声明变量或者函数。其主要目的是告知编译器这个变量或者函数在别处定义。</p>
</li>
<li><p>基本用法</p>
<ul>
<li><p>变量</p>
<ul>
<li><p>声明但不定义：extern用于在某个文件中声明一个变量，但是这个变量的定义在其他的文件中，这意味着你可以使用这个变量，但是编译器会从其他文件中寻找其实际的定义</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// file1.c</span><br><span class="line">extern int global_var; // 声明</span><br><span class="line"></span><br><span class="line">// file2.c</span><br><span class="line">int global_var = 10; // 定义</span><br></pre></td></tr></table></figure>
</li>
<li><p>全局变量的多次声明：extern允许你在一个程序的多个文件中声明同一个全局变量，而不用担心重复定义的问题。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// header.h</span><br><span class="line">extern int shared_var;</span><br><span class="line"></span><br><span class="line">// source1.c</span><br><span class="line">#include &quot;header.h&quot;</span><br><span class="line">void function1() &#123;</span><br><span class="line">    shared_var = 5;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// source2.c</span><br><span class="line">#include &quot;header.h&quot;</span><br><span class="line">void function2() &#123;</span><br><span class="line">    printf(&quot;%d\n&quot;, shared_var);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>函数</p>
<ul>
<li><p>函数声明：在函数的声明中添加extern可以明确的指出这个函数的定义在其他地方。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// header.h</span><br><span class="line">extern void my_function(void);</span><br><span class="line"></span><br><span class="line">// source.c</span><br><span class="line">void my_function(void) &#123;</span><br><span class="line">    // 函数实现</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
</li>
<li><p>注意事项</p>
<ul>
<li><p>链接性：extern表示外部链接，这意味着变量或者函数可以被其他文件引用。如果不适用extern，<strong>在全局作用域下，声明的变量会自动具有外部链接，但是局部变量默认是内部链接。</strong> </p>
</li>
<li><p>初始化：在使用extern声明的变量不能在声明处初始化，因为这会变成一个定义，而不是声明。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 错误的用法</span><br><span class="line">extern int x = 10; // 这实际上是定义了一个变量，而不是声明</span><br><span class="line"></span><br><span class="line">// 正确的用法</span><br><span class="line">extern int x; // 只声明</span><br><span class="line">int x = 10; // 在其他地方定义并初始化</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>与静态变量对比</strong>：extern对比static：</p>
<ul>
<li>extern变量可以在多个文件中被访问。</li>
<li>static变量只在定义它的文件内可见（内部链接性），或在块作用域内只在定义它的块中可见。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h5 id="static"><a href="#static" class="headerlink" title="static"></a>static</h5><p>静态局部变量</p>
<ul>
<li><p>定义：当static引用于函数内的局部变量时，他改变了该变量的生命周期，使其在函数调用之间保持其值。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void count() &#123;</span><br><span class="line">    static int counter = 0; // 初始化只发生在第一次调用时</span><br><span class="line">    counter++;</span><br><span class="line">    printf(&quot;%d\n&quot;, counter);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>初始化只初始化一次，在第一次进入函数时。</li>
</ul>
</li>
</ul>
<p>静态全局变量</p>
<ul>
<li><p>定义：当static用于全局变量时，它将全局变量的外部链接，改为内部链接。这意味着变量只能在本文件中使用。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// file1.c</span><br><span class="line">static int global_counter = 0; // 只能在file1.c中访问</span><br><span class="line"></span><br><span class="line">void increment() &#123;</span><br><span class="line">    global_counter++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<p>静态函数</p>
<ul>
<li><p>定义：将函数使用static，同样改变其链接型，是函数只能在定义它的文件中使用。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// file1.c</span><br><span class="line">static void helper_function() &#123;</span><br><span class="line">    // 只在file1.c中可用</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<p>注意事项</p>
<ul>
<li>初始化：静态变量只初始化一次，如未显示初始化，则默认初始化为0.</li>
<li>生命周期：静态变量的生命周期于程序的生命周期相同，即他们在程序开始时分配内存，在程序结束时释放内存。</li>
<li>作用域和链接型：<ul>
<li>静态局部变量的作用域任然时局部的，但是生命周期是全局的。</li>
<li>静态全局变量的作用域是该在文件作用域，但是他们是内部链接。</li>
</ul>
</li>
</ul>
<h5 id="a-和-a的区别"><a href="#a-和-a的区别" class="headerlink" title="a++和++a的区别"></a>a++和++a的区别</h5><p>功能：</p>
<ul>
<li>两个都是将变量a自增加一。</li>
<li>区别在于两个的返回值和执行顺序。</li>
</ul>
<p>区别：</p>
<ul>
<li>a++：先返回当前值，在将a的值加一。例如a的初始值为5，a++先返回5，然后a再加一变为6.</li>
<li>++a：先将a的值加1，再返回新的a值。例如，++aa先让a加一变为6，再返回。</li>
</ul>
<p>注意点：</p>
<ul>
<li>计算过程中着重考虑的是需要的是返回值还是自身。a++，返回值不变，自身加一；++a，返回值加一，自身也加一。</li>
</ul>
]]></content>
      <categories>
        <category>stm32学习</category>
      </categories>
      <tags>
        <tag>stm32学习</tag>
        <tag>面试</tag>
        <tag>C语言</tag>
      </tags>
  </entry>
  <entry>
    <title>测试文章</title>
    <url>/2025/01/21/%E6%B5%8B%E8%AF%95%E6%96%87%E7%AB%A0/</url>
    <content><![CDATA[<p>这是以个测试</p>
<p><img src="/2025/01/21/%E6%B5%8B%E8%AF%95%E6%96%87%E7%AB%A0/20241108123904.jpg" alt="微信图片_20241108123904"></p>
]]></content>
      <tags>
        <tag>测试</tag>
      </tags>
  </entry>
  <entry>
    <title>设备树</title>
    <url>/2025/01/07/%E8%AE%BE%E5%A4%87%E6%A0%91/</url>
    <content><![CDATA[<h1 id="设备树"><a href="#设备树" class="headerlink" title="设备树"></a>设备树</h1><p>linux3.0以后版本才引进了设备树，在此之前的linux内核代码中描述硬件平台信息的代码散乱的分布在arch&#x2F;arm&#x2F;plat-XXX 和 arch&#x2F;arm&#x2F;mach-XXX等目录中。所以为了改善这一情况，设备树孕育于出，它将这些硬件描述信息从内核代码中剥离出来，使用一种树形结构来描述硬件平台的组成和配置信息。</p>
<p>具体关于设备树的介绍可以参考：<a href="https://www.devicetree.org/">https://www.devicetree.org/</a></p>
<span id="more"></span>

<h2 id="1、设备树简介"><a href="#1、设备树简介" class="headerlink" title="1、设备树简介"></a>1、设备树简介</h2><h5 id="1、什么是设备树？（Device-Tree）"><a href="#1、什么是设备树？（Device-Tree）" class="headerlink" title="1、什么是设备树？（Device Tree）"></a>1、什么是设备树？（Device Tree）</h5><p>​	在linux内核早期，平台硬件的信息（例如内存地址、外设配置等）是直接编码在内核代码中的。这意味着一旦硬件平台发生改变，就要重新编写代码。这种方式难以维护和移植。</p>
<p>​	为了解决这一问题，linux内核在3.X版本之后开时引用了设备树机制。<strong>简单的来说，设备树就是一个描述硬件平台的信息的文本文件，内核通过解析这个文件来了解硬件平台的配置。</strong></p>
<h5 id="2、设备树的作用"><a href="#2、设备树的作用" class="headerlink" title="2、设备树的作用"></a>2、设备树的作用</h5><p>​	设备的主要作用：</p>
<ul>
<li>描述硬件的拓扑结构：使用树形结构可以清晰的描述硬件平台上的各种设备以及相互的关系。例如哪个USART链接着哪些GOIO控制器，哪个中断控制器控制着那个外设的中断。</li>
<li>传递硬件配置信息：将硬件设备的地址、中断号、时钟频率等配置信息传递给内核。</li>
<li>实现硬件描述与驱动代码的分离：使得驱动程序更见通用，时许更具设备树中的信息进行适当的调整，而无需修改驱动代码。</li>
</ul>
<h5 id="3、设备树的基本概念"><a href="#3、设备树的基本概念" class="headerlink" title="3、设备树的基本概念"></a>3、设备树的基本概念</h5><p>​	设备树有节点（node）和属性（property）组成，形成一个树状结构。</p>
<ul>
<li><p>节点（node）：代表硬件设备，例如内存、外设（USART、SPI、IIC等）。每个节点都有唯一的名称。并且包含多个属性。节点之间存在着父子关系。根节点可以用‘&#x2F;’表示。</p>
</li>
<li><p>属性（property）：描述节点的特性，例如设备的地址（reg）、中断号（interrupts）、时钟频率（clock-frequency）等。属性有键值成对组成，键是属性的名称，值属性的数据。</p>
</li>
<li><p>举一个简单的例子：</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/ &#123;  // 根节点</span><br><span class="line">    compatible = &quot;my-company,my-platform&quot;; // 兼容性字符串，用于驱动匹配</span><br><span class="line">    model = &quot;My Platform v1.0&quot;; // 平台型号</span><br><span class="line"></span><br><span class="line">    memory &#123; // 内存节点</span><br><span class="line">        reg = &lt;0x40000000 0x10000000&gt;; // 内存起始地址和大小</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    uart0: uart@1000 &#123; // UART 节点，命名为 uart0，地址为 0x1000</span><br><span class="line">        compatible = &quot;ns16550a&quot;; // UART 控制器类型</span><br><span class="line">        reg = &lt;0x1000 0x100&gt;; // UART 寄存器地址和大小</span><br><span class="line">        interrupts = ; // UART 中断号</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="4、设备树的表示形式：DTS、DTB、DTC"><a href="#4、设备树的表示形式：DTS、DTB、DTC" class="headerlink" title="4、设备树的表示形式：DTS、DTB、DTC"></a>4、设备树的表示形式：DTS、DTB、DTC</h5><ul>
<li><p>DTS（Device Tree Source）：设备树的源文件，以.dts为结尾的文件。在使用时可以像C语言中引用头文件一样使用<code>#include</code>指令。</p>
</li>
<li><p>DTC（Device Tree Compiler）：设备树的编译器。是一个工具程序，类似于gcc，可以将DTS文件编一个二进制的DTB文件。</p>
</li>
<li><p>DTB（Device Tree Blob）：编译后的二进制文件，内核在启动时会加载DTB文件，从其中获取硬件信息。</p>
<p> 使用dtc命令可以将DTS文件编译成DTB文件。</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">dtc -I dts -O dtb -o my_device.dtb my_device.dts</span><br></pre></td></tr></table></figure>

<ul>
<li>dtc：命令</li>
<li>-I dts：指定输入文件格式为DTS</li>
<li>-O dtb：指定输出文件格式为DTB</li>
<li>-o my_device.dtb：指定输出DTB的名称</li>
<li>my_device.dts：指定输入的DTS的名称</li>
</ul>
</li>
</ul>
<h2 id="2、设备树的框架"><a href="#2、设备树的框架" class="headerlink" title="2、设备树的框架"></a>2、设备树的框架</h2><h5 id="1、设备的组织结构：树形结构"><a href="#1、设备的组织结构：树形结构" class="headerlink" title="1、设备的组织结构：树形结构"></a>1、设备的组织结构：树形结构</h5><p>​	设备树采用树形结构来描述硬件平台的组成和连接关系。</p>
<ul>
<li><p>根节点（root node）：用<code>/</code>表示，是设备树的顶层节点，描述着整个硬件平台的基本信息。</p>
</li>
<li><p>子节点（child node）：根节点下的节点，描述平台生的各个设备，例如CPU、内存、总线、外设等。子节点也可以有自己的子节点。</p>
</li>
<li><p>父节点（parent node）和子节点（child node）：节点之间通过父子关系链接起来，表示设备之间的层次关系和链接关系。</p>
<p> 示例：</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/ &#123; // 根节点</span><br><span class="line">    compatible = &quot;my-company,my-platform&quot;;</span><br><span class="line">    model = &quot;My Platform v1.0&quot;;</span><br><span class="line"></span><br><span class="line">    cpus &#123; // CPU 节点</span><br><span class="line">        cpu@0 &#123; // 第一个 CPU 节点</span><br><span class="line">            compatible = &quot;arm,cortex-a9&quot;;</span><br><span class="line">            reg = &lt;0x0&gt;;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    memory &#123; // 内存节点</span><br><span class="line">        reg = &lt;0x40000000 0x10000000&gt;;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    uart0: uart@1000 &#123; // UART 节点</span><br><span class="line">        compatible = &quot;ns16550a&quot;;</span><br><span class="line">        reg = &lt;0x1000 0x100&gt;;</span><br><span class="line">        interrupts = ;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    i2c0: i2c@2000 &#123; // I2C 控制器节点</span><br><span class="line">        compatible = &quot;i2c-designware&quot;;</span><br><span class="line">        reg = &lt;0x2000 0x100&gt;;</span><br><span class="line"></span><br><span class="line">        eeprom@50 &#123; // 连接在 i2c0 上的 EEPROM 芯片节点</span><br><span class="line">            compatible = &quot;atmel,24c32&quot;;</span><br><span class="line">            reg = &lt;0x50&gt;;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="2、设备树的语法规则"><a href="#2、设备树的语法规则" class="headerlink" title="2、设备树的语法规则"></a>2、设备树的语法规则</h5><p>​	设备树使用Device Tree Source（DTS）的文本格式进行描述，类似于C语言的结构体。</p>
<ul>
<li><p>节点定义：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[label:] node-name@unit-address &#123;</span><br><span class="line">    properties;</span><br><span class="line">    child-nodes;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<ul>
<li>label：节点标签，用于在其他地方使用该节点时，可选。例如uart0、i2c0</li>
<li>node-name：节点名称，描述设备的类型。例如CPU、memory、uart。</li>
<li>unit-address：节点地址，用于区分同一类型的设备，可选。例如@0200.</li>
<li>properties：节点属性，描述设备的具体属性。</li>
<li>child-nodes：节点的子节点。</li>
</ul>
</li>
<li><p>属性定义：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">property-name = value;</span><br></pre></td></tr></table></figure>

<pre><code>  * property-name：属性名称，是一个字符串。例如compatible、reg、interrups。
  * value：属性的值。
</code></pre>
</li>
<li><p>包含头文件</p>
<ul>
<li>使用<code>#include</code>指令可以包含其他的DTS文件或者DTSI文件。</li>
</ul>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &quot;header.dtsi&quot;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="3、设备树的重要概念"><a href="#3、设备树的重要概念" class="headerlink" title="3、设备树的重要概念"></a>3、设备树的重要概念</h5><ul>
<li><p>compatible 属性：它时设备树中最重要的属性之一，它表示一个字符串列表，用于驱动程序和设备进行匹配。驱动程序会查找设备树中compatible属性包含其支持的字符串节点，如果匹配成功，则驱动程序就负责管理该设备。</p>
<p> 例如：</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">compatible = &quot;ns16550a&quot;, &quot;uart16550&quot;;</span><br></pre></td></tr></table></figure>

<p> 表示该设备兼容ns16550a和uart16550两种类型的驱动程序。</p>
</li>
<li><p>model属性：用于指定设备的制造商和型号。</p>
<p>  例如：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">model = &quot;EmbedFire LubanCat2 HDMI+MIPI&quot;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>status属性：用于指示设备的操作状态，通过status去禁止设备或者启用设备。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* External sound card */</span><br><span class="line">sound: sound &#123;</span><br><span class="line">    status = &quot;disabled&quot;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  | 状态值   | 描述                                  |</p>
</li>
</ul>
<p>| ——– | ————————————- |<br>    | okay     | 使能设备                              |<br>    | disabled | 禁用设备                              |<br>    | fail     | 表示设备不可运行，                    |<br>    | fail-sss | 表示设备不可运行，sss与具体的设备相关 |</p>
<ul>
<li><p>reg属性：用于描述地址空间资源，通常包含设备的起始地址和大小。</p>
<p>  例如：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">reg = &lt;0x1000 0x100&gt;;</span><br></pre></td></tr></table></figure>

<p>  表示的意思是设备的其实地址是0x1000，大小为0x100字节。</p>
</li>
<li><p>interrupt属性：用于描述设备的中断资源，通产包含中断号和中断触发方式。</p>
</li>
<li><p>追加&#x2F;修改节点内容</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&amp;cpu0 &#123;</span><br><span class="line">    cpu-supply = &lt;&amp;vdd_cpu&gt;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  这些代码并不包含在根节点下，他们不是一个新的节点，而是像原有的节点追加内容。在上面的例子中，&amp;cpu0表示像节点标签cpu0的节点追加数据，这个节点可能定义在泵文件也可以定义在迸溅所包含的设备树文件中。</p>
</li>
<li><p>特殊节点</p>
<ul>
<li><p>aliases子节点</p>
<p>  aliases子节点的作用就是为其他节点起一个别名，示例：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">    aliases &#123;</span><br><span class="line">            csi2dphy0 = &amp;csi2_dphy0;</span><br><span class="line">            csi2dphy1 = &amp;csi2_dphy1;</span><br><span class="line">            csi2dphy2 = &amp;csi2_dphy2;</span><br><span class="line">    /*----------- 省略------------*/</span><br><span class="line">            mmc0 = &amp;sdhci;</span><br><span class="line">            mmc1 = &amp;sdmmc0;</span><br><span class="line">            mmc2 = &amp;sdmmc1;</span><br><span class="line">            mmc3 = &amp;sdmmc2;</span><br><span class="line">            serial0 = &amp;uart0;</span><br><span class="line">            serial1 = &amp;uart1;</span><br><span class="line">            serial2 = &amp;uart2;</span><br><span class="line">    /*----------- 以下省略------------*/</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  以serual0 &#x3D; &amp;uart0为例，serial0是一个节点的名字，设置别名后我门可以使用serial0来指代uart0节点。</p>
</li>
<li><p>chsen子节点</p>
<p>  chosen子节点位于根节点下，示例：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">chosen &#123;</span><br><span class="line">          bootargs = &quot;earlycon=uart8250,mmio32,0xfe660000 console=ttyFIQ0 root=PARTUUID=614e0000-0000 rw rootwait&quot;;</span><br><span class="line">  &#125;;</span><br></pre></td></tr></table></figure>

<p>  chosen子节点不代表实际硬件，它主要用于内核传递参数。此外这个节点还可以用作uboot像linux内核传递配置参数的通道，我梦在uboot设置好参数，就是通过这个节点向内核中传递的。</p>
</li>
</ul>
</li>
</ul>
<h5 id="3、如何获取设备树节点信息"><a href="#3、如何获取设备树节点信息" class="headerlink" title="3、如何获取设备树节点信息"></a>3、如何获取设备树节点信息</h5><p>​	在设备树中‘’节点‘’对应着实际硬件中的设备，我梦在设备树中田间一个led节点，正常情况下我们可以从这个节点获取编写led驱动所用的所有信息。</p>
<p>​	在内核中提供了一组函数用于从设备节点中获取资源的函数，这些函数以of_开头，称为OF操作。</p>
<p>1、查找节点函数</p>
<ul>
<li><p>根据节点路径寻找节点函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct device_node *of_find_node_by_path(const char *path)</span><br></pre></td></tr></table></figure>

<p>  参数：</p>
<pre><code>  * path：指定节点在设备树中的路径
</code></pre>
<p>  返回值：</p>
<ul>
<li><p>device_node：结构体指针，失败返回为NULL；成功返回一个指针，该指针中保存着设备节点的信息。</p>
</li>
<li><p>device_node结构体如下，它定义在 <code>&lt;linux/of.h&gt;</code> 头文件中：</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct device_node &#123;</span><br><span class="line">    const char *name;           // 节点的名称，例如 &quot;uart@1000&quot;</span><br><span class="line">    phandle phandle;          // 节点的句柄，用于在设备树中唯一标识一个节点</span><br><span class="line">    const char *full_name;      // 节点的完整路径名，例如 &quot;/soc/uart@1000&quot;</span><br><span class="line">    struct fwnode_handle fwnode; // 用于统一访问不同类型的固件节点，如设备树、ACPI等</span><br><span class="line"></span><br><span class="line">    struct property *properties; // 指向节点属性链表的指针</span><br><span class="line">    struct property *deadprops;  // 指向已删除属性链表的指针（用于调试）</span><br><span class="line">    struct device_node *parent;  // 指向父节点的指针</span><br><span class="line">    struct device_node *child;   // 指向第一个子节点的指针</span><br><span class="line">    struct device_node *sibling; // 指向下一个兄弟节点的指针</span><br><span class="line"></span><br><span class="line">#if defined(CONFIG_OF_KOBJ)</span><br><span class="line">    struct kobject kobj;         // 用于将设备节点注册到 sysfs 文件系统中</span><br><span class="line">#endif</span><br><span class="line">    unsigned long _flags;        // 节点标志</span><br><span class="line">    void *data;                 // 用于存储驱动程序特定的数据</span><br><span class="line">#if defined(CONFIG_SPARC)</span><br><span class="line">    unsigned int unique_id;     // SPARC 平台特有的唯一 ID</span><br><span class="line">    struct of_irq_controller *irq_trans; // SPARC 平台特有的中断转换</span><br><span class="line">#endif</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>name：节点中属性为name的值</p>
</li>
<li><p>phandle：节点中为device-type的值</p>
</li>
<li><p>full-name：节点的名字，在device_node结构体后面放一个字符串，full-name指向它</p>
</li>
<li><p>properties：链表，连接该节点的所有属性</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct property &#123;</span><br><span class="line">    char *name;         // 属性的名称，例如 &quot;compatible&quot;、&quot;reg&quot;</span><br><span class="line">    int length;         // 属性值的长度（以字节为单位）</span><br><span class="line">    void *value;        // 指向属性值的指针</span><br><span class="line">    struct property *next; // 指向下一个属性的指针，构成链表</span><br><span class="line">    unsigned long _flags;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>parent：指向父节点</p>
</li>
<li><p>child：指向子节点</p>
</li>
<li><p>sibling：指向兄弟节点</p>
</li>
</ul>
</li>
</ul>
</li>
<li><p>根据系欸但名字寻找节点函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct device_node *of_find_node_by_name(struct device_node *from,const char *name);</span><br></pre></td></tr></table></figure>

<p>  参数：</p>
<ul>
<li>from：致电给从哪个节点开始查找，它本身不在查找行列中，之查找它之后的节点。</li>
<li>name：要寻找的节点名。</li>
</ul>
<p>  返回值：</p>
<pre><code>  * device—node：节点结构体指针，上面的一样。
</code></pre>
</li>
<li><p>根据节点类型寻找节点函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct device_node *of_find_node_by_type(struct device_node *from,const char *type)</span><br></pre></td></tr></table></figure>

<p>  参数：</p>
<ul>
<li>from：指定从哪个节点开始找。</li>
<li>type：要寻找的节点的类型。</li>
</ul>
<p>  返回值：</p>
<ul>
<li><strong>device_node：</strong> device_node类型的结构体指针。</li>
</ul>
</li>
<li><p>根据节点类型和compeatible属性寻找节点函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct device_node *of_find_compatible_node(struct device_node *from,const char *type, const char *compatible)</span><br></pre></td></tr></table></figure>

<p>  相对于of_find_node_by_name，多了一个compatible（属性）的参数。</p>
<p>  compatible：要寻找的节点的兼容性属性。</p>
</li>
<li><p>根据匹配表寻找节点函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static inline struct device_node *of_find_matching_node_and_match(struct device_node *from, const struct of_device_id *matches, const struct of_device_id **match)</span><br></pre></td></tr></table></figure>

<p>  相对于前面的几种，多了matches、match参数。</p>
<p>  matches：指向一个of_device_id数组的指针。这个数组包含了要匹配的设备ID列表。</p>
<p>  match：指向of_device_id指针的指针。如果找到匹配的节点，则将匹配的结构体的地址存储在这个指针指向的位置。</p>
<p>  struct of_device_id结构体：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/*</span><br><span class="line"> * Struct used for matching a device</span><br><span class="line"> */</span><br><span class="line">struct of_device_id &#123;</span><br><span class="line">	char	name[32];</span><br><span class="line">	char	type[32];</span><br><span class="line">	char	compatible[128];</span><br><span class="line">	const void *data;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<ul>
<li>name：设备的名称。</li>
<li>type：设备的类型。</li>
<li>compatible：设备的兼容性字符串。用于驱动和设备的匹配。</li>
<li>data：指向驱动私有数据的指针。</li>
</ul>
</li>
<li><p>寻找父节点函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct device_node *of_get_parent(const struct device_node *node)</span><br></pre></td></tr></table></figure>

<p>  参数：</p>
<ul>
<li>node：指定要查找父节点的节点。</li>
</ul>
<p>  返回值：</p>
<ul>
<li>node：是一个指向device_node类型的结构体指针。</li>
</ul>
</li>
<li><p>寻找子节点函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct device_node *of_get_next_child(const struct device_node *node, struct device_node *prev)</span><br></pre></td></tr></table></figure>

<ul>
<li>node：指定谁要查找它的子节点。</li>
<li>prev：前一个子节点。这是一个迭代的过程。就像找孙子，你要先把哪个儿子的节点找到，才能找到该儿子下的孙子节点。如果为NULL，则直接找儿子节点。</li>
</ul>
</li>
</ul>
<p>这里介绍的七个寻找节点函数，共同点是返回值类型都相同。只要找到节点就可以返回节点对应的device-node结构体，通过这个结构体就可以获取到设备节点中的属性信息、父子节点等。</p>
<p>第一个函数of_find_node_by_path与其他六个不同，它是通过节点路径来寻找节点的，节点路径是从设备树源文件（.dts）中找到的。中间四个函数根据节点属性在某一节点之后查找相符合的要求的设备节点。最后两个根据父子系节点进行查找。</p>
<p>2、提取属性值的of函数</p>
<p>上面我们看了七个查找节点的函数，他们都是找到一个设备节点然后返回相对应的结构体指针device-node。下面我们讲解从结构体中获取我们想要的设备节点属性信息。</p>
<ul>
<li><p>查找节点属性函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct property *of_find_property(const struct device_node *np,const char *name,int *lenp)</span><br></pre></td></tr></table></figure>

<p>  参数：</p>
<ul>
<li>np：指定要获取的设备节点的属性信息。</li>
<li>name：属性名</li>
<li>lenp：获取等到的属性值的大小。这个指针作为输出参数，这个参数带回的值是实际获取得到的属性大小</li>
</ul>
<p>  返回值：</p>
<ul>
<li><p>property：获取的属性。而struct property *，我们称为系欸但属性结构体。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct property &#123;</span><br><span class="line">	char	*name;</span><br><span class="line">	int	length;</span><br><span class="line">	void	*value;</span><br><span class="line">	struct property *next;</span><br><span class="line">#if defined(CONFIG_OF_DYNAMIC) || defined(CONFIG_SPARC)</span><br><span class="line">	unsigned long _flags;</span><br><span class="line">#endif</span><br><span class="line">#if defined(CONFIG_OF_PROMTREE)</span><br><span class="line">	unsigned int unique_id;</span><br><span class="line">#endif</span><br><span class="line">#if defined(CONFIG_OF_KOBJ)</span><br><span class="line">	struct bin_attribute attr;</span><br><span class="line">#endif</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<ul>
<li>name：属性名</li>
<li>length：属性长度</li>
<li>value：属性值</li>
<li>next：下个属性</li>
</ul>
</li>
</ul>
</li>
<li><p>读取整形属性函数</p>
<p>  读取属性函数是一组函数，分别为8、16、32、64位数据。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//8位整数读取函数</span><br><span class="line">int of_property_read_u8_array(const struct device_node *np, const char *propname, u8 *out_values, size_t sz)</span><br><span class="line"></span><br><span class="line">//16位整数读取函数</span><br><span class="line">int of_property_read_u16_array(const struct device_node *np, const char *propname, u16 *out_values, size_t sz)</span><br><span class="line"></span><br><span class="line">//32位整数读取函数</span><br><span class="line">int of_property_read_u32_array(const struct device_node *np, const char *propname, u32 *out_values, size_t sz)</span><br><span class="line"></span><br><span class="line">//64位整数读取函数</span><br><span class="line">int of_property_read_u64_array(const struct device_node *np, const char *propname, u64 *out_values, size_t sz)</span><br></pre></td></tr></table></figure>

<p>  参数：</p>
<ul>
<li>np：指定要读取那个设备节点结构体。</li>
<li>propname：指定要获取设备几点的那个属性</li>
<li>out-value：这是一个输出参数，是函数的返回值，保存读取等到的数据</li>
<li>sz：这是一个输入参数，用于设置读取的长度。</li>
</ul>
<p>  返回值：</p>
<ul>
<li>成功返回0，错误返回错误状态吗。-EINVAL（属性不存在）、-ENODATA（没有要读取的数据）、-EOVERFLOW（属性值列表太小）。</li>
</ul>
</li>
<li><p>读取字符串属性函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int of_property_read_string(const struct device_node *np,const char *propname,const char **out_string)</span><br></pre></td></tr></table></figure>

<p>  参数：</p>
<ul>
<li>np：要读取那个设备节点的属性</li>
<li>propname：属性名</li>
<li>out-string：获取等待字符串的指针。</li>
</ul>
<p>  返回值：</p>
<ul>
<li>成功返回0，失败返回错误状态吗</li>
</ul>
<p>  推荐使用下面的这个函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int of_property_read_string_index(const struct device_node *np,const char *propname, int index,const char **out_string)</span><br></pre></td></tr></table></figure>

<p>  相对于前面增加了index，用于指定读取属性值中的第几个字符串，index从零开始计数。</p>
</li>
<li><p>读取布尔型属性函数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static inline bool of_property_read_bool(const struct device_node *np, const char *propname);</span><br></pre></td></tr></table></figure>

<p>  参数：</p>
<ul>
<li>np：要读取的那个设备节点的属性信息</li>
<li>propname：属性名</li>
</ul>
<p>  返回值：</p>
<ul>
<li>这个函数不是读取某个布尔型属性的值，仅仅读取这个属性存在或者不存在，如果想要获取值，还是建议使用“全能”函数查找节点of_find_property。</li>
</ul>
</li>
</ul>
<p>3、内存映射相关的of函数</p>
<p>在设备树的节点中大多包含一些捏村想过的属性，比如常用的reg属性。通常情况下，等到寄存器地址之后我们还要通过ioremap函数将物理地址转化为虚拟地址。现在内核提供了一些of函数，自动完成物理地址到虚拟地址的转换。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void __iomem *of_iomap(struct device_node *np, int index)</span><br></pre></td></tr></table></figure>

<p>参数：</p>
<ul>
<li>np：要获取的那个设备节点的属性信息</li>
<li>index：通常情况下reg属性包含多段，index用于指定映射哪一段，从零开始</li>
</ul>
<p>返回值：</p>
<ul>
<li>成功返回转换的地址。失败返回NULL。</li>
</ul>
<p>内核也提供看常规获取地址的of函数，这些函数得到的值就是我们在设备树中设置的地址值，</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int of_address_to_resource(struct device_node *dev, int index, struct resource *r)</span><br></pre></td></tr></table></figure>

<p>参数：</p>
<ul>
<li>dev：指定要获取的那个设备节点的属性信息</li>
<li>index：通常情况下reg包含多端。</li>
<li>r：这是要给resource结构体，是输出桉树用于返回得到的地址信息。</li>
</ul>
<p>返回值：</p>
<ul>
<li>成功返回0，失败返回错误状态吗</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct resource &#123;</span><br><span class="line">    resource_size_t start;</span><br><span class="line">    resource_size_t end;</span><br><span class="line">    const char *name;</span><br><span class="line">    unsigned long flags;</span><br><span class="line">    unsigned long desc;</span><br><span class="line">    struct resource *parent, *sibling, *child;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<ul>
<li>start：起始地址</li>
<li>end：结束地址</li>
<li>name：属性的名称</li>
</ul>
<h5 id="4、使用设备树驱动led示例"><a href="#4、使用设备树驱动led示例" class="headerlink" title="4、使用设备树驱动led示例"></a>4、使用设备树驱动led示例</h5><p>1、像设备树中添加节点：</p>
<ul>
<li><p>在linux内核源码中找到设备树文件。（&#x2F;home&#x2F;h&#x2F;h&#x2F;linux-5.4.31&#x2F;arch&#x2F;arm&#x2F;boot&#x2F;dts）</p>
</li>
<li><p>打开设备树文件，在其中添加led节点。（stm32mp157a-fsmpla.dts）</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> led_test@0x54004000&#123;</span><br><span class="line">      compatible = &quot;h,led1&quot;;</span><br><span class="line">      reg = &lt;0x54004000 0x400&gt;;</span><br><span class="line">      led_name = &quot;led01&quot;;</span><br><span class="line">      led_minor = &lt;11&gt;;</span><br><span class="line">      moder_clear = &lt;0x3f&gt;;</span><br><span class="line">      moder_data = &lt;0x15&gt;;</span><br><span class="line">      odr = &lt;0x7&gt;;</span><br><span class="line">      shift = &lt;5&gt;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>参数：</p>
<pre><code>* led_test：设备节点名称。
* @0x54004000：设备的起始地址。当属性中有reg时，可以在这里添加0x54004000，当然有没有并没有什么影响。
* compatible：用于匹配驱动程序。
* reg：设备的寄存器空间地址。包括其实地址和空间大小。
* （下面就是自定义属性）
* led_name：设备名称。
* moder_clear：清理GPIO引脚的输入输出端口。
* moder_data：配置GPIO引脚。
* odr：输出数据寄存器的值。决定了led的初始状态。
* shift：寄存器的位移量。
</code></pre>
</li>
<li><p>添加好设备节点后，保存退出。</p>
</li>
<li><p>进入linux内核目录下进行编译，重新传入板子中。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">h@h-virtual-machine:~/h/linux-5.4.31$ source /opt/stm32_sdk/environment-setup-cortexa7t2hf-neon-vfpv4-ostl-linux-gnueabi </span><br><span class="line">h@h-virtual-machine:~/h/linux-5.4.31$ make -j4 ARCH=arm dtbs LOADADDR=0xC2000040</span><br><span class="line">cp arch/arm/boot/dts/stm32mp157a-fsmpla.dtb /tftpboot/</span><br></pre></td></tr></table></figure>
</li>
<li><p>重新启动单片机，linux内核就会重新加载我们新写好的设备树文件。</p>
</li>
</ul>
<p>2、编写驱动代码（led_pdrv.c）</p>
<ul>
<li><p>编写驱动框架</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static int __init led_pdev_init(void)</span><br><span class="line">&#123;</span><br><span class="line">    printk(&quot;--------- %s ---------\n&quot;, __FUNCTION__);</span><br><span class="line">    // 2、注册platform-driver对象</span><br><span class="line">    return platform_driver_register(&amp;led_pdrv);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">static void __exit led_pdrv_exit(void)</span><br><span class="line">&#123;</span><br><span class="line">    printk(&quot;--------- %s ---------\n&quot;, __FUNCTION__);</span><br><span class="line">    platform_driver_unregister(&amp;led_pdrv);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">module_init(led_pdev_init);</span><br><span class="line">module_exit(led_pdrv_exit);</span><br><span class="line">MODULE_LICENSE(&quot;GPL&quot;);</span><br></pre></td></tr></table></figure>
</li>
<li><p>创建platform-driver对象，并且通过led_match_table中的compatible与驱动进行匹配。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 与pdev中的name匹配</span><br><span class="line">const struct platform_device_id led_id_table[] = &#123;</span><br><span class="line">    &#123; &quot;mp157_led&quot;, 0xa &#125;,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">// 与设备树中的compatible匹配</span><br><span class="line">const struct of_device_id led_match_table[] = &#123;</span><br><span class="line">    &#123; .compatible = &quot;h,led1&quot; &#125;,</span><br><span class="line">    &#123; /* sentinel */ &#125;</span><br><span class="line">&#125;;</span><br><span class="line">struct platform_driver led_pdrv = &#123;</span><br><span class="line">    .probe  = led_pdrv_probe,</span><br><span class="line">    .remove = led_pdrv_remove,</span><br><span class="line">    .driver = &#123;</span><br><span class="line">        .name = &quot;fsmp1_led&quot;,</span><br><span class="line">        .of_match_table = led_match_table,</span><br><span class="line">    &#125;,</span><br><span class="line">    .id_table = led_id_table,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>编写led_pdrv_probe、led_pdrv_remove函数，如果驱动匹配成功后，会调用led_pdrv_probe函数执行，在卸载时调用led_pdrv_remove函数。</p>
<ul>
<li><p>int led_pdrv_probe(struct platform_device *<em>pdev</em>)</p>
<ul>
<li><p>分配全局设备对象空间</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 设计led的结构体</span><br><span class="line">struct mp157_led_t &#123;</span><br><span class="line">    struct miscdevice misc;</span><br><span class="line">    unsigned int *moder;</span><br><span class="line">    unsigned int *odr;</span><br><span class="line"></span><br><span class="line">    int m_clear;</span><br><span class="line">    int m_data;</span><br><span class="line">    int o_data;</span><br><span class="line">    int shift;</span><br><span class="line">&#125;;</span><br><span class="line">    // 1、分配全局设备对象空间</span><br><span class="line">    led_dev = kzalloc(sizeof(*led_dev), GFP_KERNEL);</span><br><span class="line">    if (!led_dev) &#123;</span><br><span class="line">        printk(&quot;kzalloc failed\n&quot;);</span><br><span class="line">        return -ENOMEM;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>获取设备节点和数据</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 获取设备树节点对象</span><br><span class="line">np = pdev-&gt;dev.of_node;	//这里的np是直接获取pdev的设备树节点，并不是寻找到设备树节点</span><br><span class="line">of_property_read_string(np, &quot;led_name&quot;, (const char **)&amp;name);</span><br><span class="line">of_property_read_u32(np, &quot;led_minor&quot;, &amp;minor);</span><br><span class="line">of_property_read_u32(np, &quot;led_clear&quot;, &amp;led_dev-&gt;m_clear);</span><br><span class="line">of_property_read_u32(np, &quot;moder_data&quot;, &amp;led_dev-&gt;o_data);</span><br><span class="line">of_property_read_u32(np, &quot;odr&quot;, &amp;led_dev-&gt;o_data);  // 修正：读取odr属性，赋值给o_data</span><br><span class="line">of_property_read_u32(np, &quot;shift&quot;, &amp;led_dev-&gt;shift);</span><br></pre></td></tr></table></figure>
</li>
<li><p>初始化杂项设备</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">led_dev-&gt;misc.name = name;</span><br><span class="line">led_dev-&gt;misc.minor = minor;</span><br><span class="line">led_dev-&gt;misc.fops = &amp;led_fops;</span><br></pre></td></tr></table></figure>
</li>
<li><p>注册杂项设备</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ret = misc_register(&amp;led_dev-&gt;misc);</span><br><span class="line">if (ret) &#123;</span><br><span class="line">    printk(&quot;misc register failed\n&quot;);</span><br><span class="line">    goto err_kfree;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>硬件初始化</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">led_dev-&gt;moder = ioremap(res1-&gt;start, res1-&gt;end - res1-&gt;start + 1);</span><br><span class="line">if (!led_dev-&gt;moder) &#123;</span><br><span class="line">    printk(&quot;ioremap failed\n&quot;);</span><br><span class="line">    ret = -EINVAL;</span><br><span class="line">    goto err_misc_deregister;</span><br><span class="line">&#125;</span><br><span class="line">led_dev-&gt;odr = led_dev-&gt;moder + 5;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>int led_pdrv_remove(struct platform_device *<em>pdev</em>)</p>
<ul>
<li><p>平台资源的释放和注销（先注册后释放）</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">printk(&quot;--------- %s ---------\n&quot;, __FUNCTION__);</span><br><span class="line">iounmap(led_dev-&gt;moder);</span><br><span class="line">misc_deregister(&amp;led_dev-&gt;misc);</span><br><span class="line">kfree(led_dev);</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
</li>
<li><p>编写文件操作函数open、write、release。</p>
<ul>
<li><p>首先定义结构体</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">const struct file_operations led_fops = &#123;</span><br><span class="line">    .owner = THIS_MODULE,</span><br><span class="line">    .open   = led_pdrv_open,</span><br><span class="line">    .write  = led_pdrv_write,</span><br><span class="line">    .release  = led_pdrv_close,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>int led_pdrv_open(struct inode *<em>inode</em>, struct file *<em>filp</em>)</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 将GPIOZ——5、6、7设置成输出模式</span><br><span class="line">*led_dev-&gt;moder &amp;= ~(led_dev-&gt;m_clear &lt;&lt; (led_dev-&gt;shift * 2));</span><br><span class="line">*led_dev-&gt;moder |= led_dev-&gt;m_data  &lt;&lt; (led_dev-&gt;shift * 2);</span><br></pre></td></tr></table></figure>
</li>
<li><p>ssize_t led_pdrv_write(struct file *<em>filp</em>, const char __user *<em>buf</em>, size_t <em>size</em>, <em>loff_t</em> *<em>flags</em>)</p>
<ul>
<li><p>将应用数据转换为内核数据</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ret = copy_from_user(&amp;value, buf, size);</span><br><span class="line">if (ret) &#123;</span><br><span class="line">    printk(&quot;copy from user error\n&quot;);</span><br><span class="line">    return -EAGAIN;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>根据传输的值进行开关灯</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if (value) &#123;   // 开灯</span><br><span class="line">    *led_dev-&gt;odr |= led_dev-&gt;o_data &lt;&lt; led_dev-&gt;shift;</span><br><span class="line">&#125; else &#123;  // 关灯</span><br><span class="line">    *led_dev-&gt;odr &amp;= ~(led_dev-&gt;o_data &lt;&lt; led_dev-&gt;shift);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>int led_pdrv_close(struct inode *<em>inode</em>, struct file *<em>filp</em>)</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">*led_dev-&gt;odr &amp;= ~(led_dev-&gt;o_data &lt;&lt; led_dev-&gt;shift);</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>Linux驱动</category>
      </categories>
      <tags>
        <tag>Linux学习</tag>
        <tag>平台总线</tag>
        <tag>设备树</tag>
        <tag>DTS</tag>
      </tags>
  </entry>
  <entry>
    <title>驱动和设备匹配过程</title>
    <url>/2025/01/14/%E9%A9%B1%E5%8A%A8%E5%92%8C%E8%AE%BE%E5%A4%87%E5%8C%B9%E9%85%8D%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="匹配过程及其问题"><a href="#匹配过程及其问题" class="headerlink" title="匹配过程及其问题"></a>匹配过程及其问题</h1><h4 id="1、匹配过程"><a href="#1、匹配过程" class="headerlink" title="1、匹配过程"></a>1、匹配过程</h4><p>​	在linux驱动中，目前学到了三种编写代码的方式。</p>
<ul>
<li><p>第一种：传统编写。驱动和硬件写在一起，让linux内核和硬件交织在一起，这是早期linux开发的编写方式，不推荐使用，可以在初学时看看，也可以不看，不影响。</p>
</li>
<li><p>第二种：使驱动和设备硬件分开编写。pdrv主要编写linux的内核代码，pdev主要关于硬件的资源。分开有利管理，但是当设备多了之后，又出现了问题，代码太多，太繁。</p>
</li>
<li><p>第三种：使用设备树。驱动的代码基本不变，修改设备硬件的代码，把使用的设备硬件汇总为一个设备树的结构，每个节点都是一个设备硬件，代码简单、方便、便于移植。</p>
<span id="more"></span></li>
</ul>
<p>第一种不说了。主要讲第二、三种的匹配过程，阅读一下内核源码，看一下整个流程。（目前刚刚开始学习不久，仅仅学到设备树，所以就目前掌握的开始写和搜的，如果以后学习到更深，在回来补充）</p>
<ul>
<li><p>平台总线的平台驱动和平台设备匹配过程</p>
<ul>
<li><p>1、首先先讲解两个函数。平台设备注册int platform_device_register(struct <em>platform_device</em> *)；平台驱动注册platform_driver_register(<em>drv</em>)。这是两个注册函数，将我们自定义的平台设备和平台驱动注册到平台总线中去。他们会在平台总线中相遇，至于怎么找到双方，那就要有一个信物，两者个持一个，匹配上了就会成功。这个信物就是name（名称），下面会讲解。</p>
<ul>
<li><p><em><em>平台设备注册：int platform_device_register(struct platform_device pdev</em>)</em>* </p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int platform_device_register(struct platform_device *pdev)</span><br><span class="line">&#123;</span><br><span class="line">	device_initialize(&amp;pdev-&gt;dev);</span><br><span class="line">	setup_pdev_dma_masks(pdev);</span><br><span class="line">	return platform_device_add(pdev);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>在平台设备注册函数中主要完成下面任务：</p>
<p>  1、初始化struct platform_device结构体中的struct device结构体。[它将平台设备纳入到 Linux 设备模型中，使其能够被设备模型管理和访问。](#####1、在平台设备注册函数中，为什么要初始化struct device结构体？为什么说将平台设备纳入到 Linux 设备模型中？)</p>
<p>  2、关于DMA的，这个函数用于设置设备的 DMA (Direct Memory Access) 掩码。DMA 允许设备直接访问内存，而无需 CPU 的干预，从而提高数据传输效率。DMA 掩码用于限制设备可以访问的内存地址范围。</p>
<p>  3、这个函数是平台设备的注册的真正执行着，将平台设备添加到平台总线的设备列表中。触发驱动程序等到匹配。</p>
</li>
<li><p><strong>在platform_device_add(pdev);函数中，找到源码解读。</strong><br>  1、参数检查和初始化</p>
<ul>
<li>首先检查pdev指针是否为空。</li>
<li>如果pdev的父设备为空，则将其设置成平台总线。（可见平台总线相当于根节点，下面挂着一串平台设备列表）</li>
<li>设备pdev的总线类型为平台总线类型（platform_bus_type）（<a href="#####2%E3%80%81%E5%B9%B3%E5%8F%B0%E6%80%BB%E7%BA%BF%E7%B1%BB%E5%9E%8B%E5%92%8C%E5%B9%B3%E5%8F%B0%E5%8C%B9%E9%85%8D%E7%9A%84%E5%85%B3%E7%B3%BB">这个平台总线类型是平台匹配的重要部分</a>）。</li>
</ul>
<p>  2、设置设备名称</p>
<ul>
<li>根据 <code>pdev-&gt;id</code>的值，使用<code>dev_set_name</code>函数设备<code>pdev</code>的设备名称。</li>
<li>如果<code>pdev-&gt;id</code>为默认值，则将名称设置为<code>pdev-&gt;name.pdev-&gt;id</code>。</li>
<li>如果<code>pdev-&gt;id</code>为<code>PLATFORM_DEVID_NONE</code>，则将名称设置为<code>pdev-&gt;name</code>。</li>
<li>如果<code>pdev-&gt;id</code>为<code>PLATFORM_DEVID_AUTO</code>，则将名称设置为<code>pdev-&gt;name.pdev-&gt;id.auto</code>。</li>
</ul>
<p>  3、注册设备资源</p>
<ul>
<li>遍历<code>pdev-&gt;resource</code> 数组，该数组中存放着设备使用的资源信息（例如内存地址、中断号等）</li>
<li>为每个资源设置名称</li>
<li>为每个资源设置父资源</li>
</ul>
<p>  4、添加设备</p>
<ul>
<li>打印注册信息</li>
<li>调用 <code>device_add</code> 函数将设备添加到linux设备模型中。</li>
</ul>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int platform_device_add(struct platform_device *pdev)</span><br><span class="line">&#123;</span><br><span class="line">	u32 i;</span><br><span class="line">	int ret;</span><br><span class="line"></span><br><span class="line">	if (!pdev)</span><br><span class="line">		return -EINVAL;</span><br><span class="line"></span><br><span class="line">	if (!pdev-&gt;dev.parent)</span><br><span class="line">		pdev-&gt;dev.parent = &amp;platform_bus;</span><br><span class="line"></span><br><span class="line">	pdev-&gt;dev.bus = &amp;platform_bus_type;</span><br><span class="line"></span><br><span class="line">	switch (pdev-&gt;id) &#123;</span><br><span class="line">	default:</span><br><span class="line">		dev_set_name(&amp;pdev-&gt;dev, &quot;%s.%d&quot;, pdev-&gt;name,  pdev-&gt;id);</span><br><span class="line">		break;</span><br><span class="line">	case PLATFORM_DEVID_NONE:</span><br><span class="line">		dev_set_name(&amp;pdev-&gt;dev, &quot;%s&quot;, pdev-&gt;name);</span><br><span class="line">		break;</span><br><span class="line">	case PLATFORM_DEVID_AUTO:</span><br><span class="line">		/*</span><br><span class="line">		 * Automatically allocated device ID. We mark it as such so</span><br><span class="line">		 * that we remember it must be freed, and we append a suffix</span><br><span class="line">		 * to avoid namespace collision with explicit IDs.</span><br><span class="line">		 */</span><br><span class="line">		ret = ida_simple_get(&amp;platform_devid_ida, 0, 0, GFP_KERNEL);</span><br><span class="line">		if (ret &lt; 0)</span><br><span class="line">			goto err_out;</span><br><span class="line">		pdev-&gt;id = ret;</span><br><span class="line">		pdev-&gt;id_auto = true;</span><br><span class="line">		dev_set_name(&amp;pdev-&gt;dev, &quot;%s.%d.auto&quot;, pdev-&gt;name, pdev-&gt;id);</span><br><span class="line">		break;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	for (i = 0; i &lt; pdev-&gt;num_resources; i++) &#123;</span><br><span class="line">		struct resource *p, *r = &amp;pdev-&gt;resource[i];</span><br><span class="line"></span><br><span class="line">		if (r-&gt;name == NULL)</span><br><span class="line">			r-&gt;name = dev_name(&amp;pdev-&gt;dev);</span><br><span class="line"></span><br><span class="line">		p = r-&gt;parent;</span><br><span class="line">		if (!p) &#123;</span><br><span class="line">			if (resource_type(r) == IORESOURCE_MEM)</span><br><span class="line">				p = &amp;iomem_resource;</span><br><span class="line">			else if (resource_type(r) == IORESOURCE_IO)</span><br><span class="line">				p = &amp;ioport_resource;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		if (p) &#123;</span><br><span class="line">			ret = insert_resource(p, r);</span><br><span class="line">			if (ret) &#123;</span><br><span class="line">				dev_err(&amp;pdev-&gt;dev, &quot;failed to claim resource %d: %pR\n&quot;, i, r);</span><br><span class="line">				goto failed;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	pr_debug(&quot;Registering platform device &#x27;%s&#x27;. Parent at %s\n&quot;,</span><br><span class="line">		 dev_name(&amp;pdev-&gt;dev), dev_name(pdev-&gt;dev.parent));</span><br><span class="line"></span><br><span class="line">	ret = device_add(&amp;pdev-&gt;dev);</span><br><span class="line">	if (ret == 0)</span><br><span class="line">		return ret;</span><br><span class="line"></span><br><span class="line"> failed:</span><br><span class="line">	if (pdev-&gt;id_auto) &#123;</span><br><span class="line">		ida_simple_remove(&amp;platform_devid_ida, pdev-&gt;id);</span><br><span class="line">		pdev-&gt;id = PLATFORM_DEVID_AUTO;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	while (i--) &#123;</span><br><span class="line">		struct resource *r = &amp;pdev-&gt;resource[i];</span><br><span class="line">		if (r-&gt;parent)</span><br><span class="line">			release_resource(r);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"> err_out:</span><br><span class="line">	return ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>注册平台设备需要一个struct platform_device *<em>pdev</em>结构体，具体内容如下：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct platform_device &#123;</span><br><span class="line">	const char	*name;</span><br><span class="line">	int		id;</span><br><span class="line">	bool		id_auto;</span><br><span class="line">	struct device	dev;</span><br><span class="line">	u64		platform_dma_mask;</span><br><span class="line">	u32		num_resources;</span><br><span class="line">	struct resource	*resource;</span><br><span class="line"></span><br><span class="line">	const struct platform_device_id	*id_entry;</span><br><span class="line">	char *driver_override; /* Driver name to force a match */</span><br><span class="line"></span><br><span class="line">	/* MFD cell pointer */</span><br><span class="line">	struct mfd_cell *mfd_cell;</span><br><span class="line"></span><br><span class="line">	/* arch specific additions */</span><br><span class="line">	struct pdev_archdata	archdata;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>平台驱动注册函数platform_driver_register(<em>drv</em>)</strong> </p>
<ul>
<li><p>在linux内核代码中定义了一个宏定义关于平台驱动注册函数。</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define platform_driver_register(drv) \</span><br><span class="line">	__platform_driver_register(drv, THIS_MODULE)</span><br><span class="line">extern int __platform_driver_register(struct platform_driver *,</span><br><span class="line">					struct module *);</span><br></pre></td></tr></table></figure>

<p>  在其中我们可以看到platform_driver_register是通过宏定义代表__platform_driver_register这个函数的。在函数中有两个参数分别是platform_driver *、struct module *。其中我们重要关心的是platform_driver 结构体，这里的和上面已经讲过的platform_device一样，是在linux内核定义好的设备框架基础上田间自定义的构建成的。</p>
</li>
<li><p>在__platform_driver_register结构体中的源码如下：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int __platform_driver_register(struct platform_driver *drv,</span><br><span class="line">				struct module *owner)</span><br><span class="line">&#123;</span><br><span class="line">	drv-&gt;driver.owner = owner;</span><br><span class="line">	drv-&gt;driver.bus = &amp;platform_bus_type;</span><br><span class="line">	drv-&gt;driver.probe = platform_drv_probe;</span><br><span class="line">	drv-&gt;driver.remove = platform_drv_remove;</span><br><span class="line">	drv-&gt;driver.shutdown = platform_drv_shutdown;</span><br><span class="line"></span><br><span class="line">	return driver_register(&amp;drv-&gt;driver);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  首先对driver进行一些初始化。在其中我们关心的是<code>drv-&gt;driver.bus = &amp;platform_bus_type;</code> 、<code>drv-&gt;driver.probe = platform_drv_probe;</code>、 <code>drv-&gt;driver.remove = platform_drv_remove;</code> 这三个行代码，其中driver.bus指定了总线类型，也是与设备匹配的关键。下面两个probe、remove两个是一个中间层函数，他们会获取platform_device的信息，然后调用我们在platform_device中自定义的probe函数。</p>
</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h4 id="2、匹配中的各种问题"><a href="#2、匹配中的各种问题" class="headerlink" title="2、匹配中的各种问题"></a>2、匹配中的各种问题</h4><h5 id="1、在平台设备注册函数中，为什么要初始化struct-device结构体？为什么说将平台设备纳入到-Linux-设备模型中？"><a href="#1、在平台设备注册函数中，为什么要初始化struct-device结构体？为什么说将平台设备纳入到-Linux-设备模型中？" class="headerlink" title="1、在平台设备注册函数中，为什么要初始化struct device结构体？为什么说将平台设备纳入到 Linux 设备模型中？"></a>1、在平台设备注册函数中，为什么要初始化struct device结构体？为什么说将平台设备纳入到 Linux 设备模型中？</h5><p>​	1、struct device是linux设备模型的基础，所有的设备都必须通过struct device纳入到设备模型中才能够被内核有效的管理。</p>
<p>​	2、linux设备模型是内核中用于管理所有设备的框架，它提供了一套统一的接口和机制。在linux中所有的设备，例如平台设备、PCI设备、USB设备，这些统一是在框架的基础上写入一些自定义的构成的。</p>
<h5 id="2、平台总线类型和平台匹配的关系。"><a href="#2、平台总线类型和平台匹配的关系。" class="headerlink" title="2、平台总线类型和平台匹配的关系。"></a>2、平台总线类型和平台匹配的关系。</h5><p>​	1、平台总线类型的源码如下，其中 <code>struct bus_type platform_bus_type</code> 定义了平台总线的类型。在liunx设备模型中，总线类型是链接设备和驱动程序的桥梁。平台总线是一中虚拟的总线，用于管理那些无法通过标准总线（如PCI、USB）枚举的设备。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct bus_type platform_bus_type = &#123;</span><br><span class="line">	.name		= &quot;platform&quot;,</span><br><span class="line">	.dev_groups	= platform_dev_groups,</span><br><span class="line">	.match		= platform_match,</span><br><span class="line">	.uevent		= platform_uevent,</span><br><span class="line">	.dma_configure	= platform_dma_configure,</span><br><span class="line">	.pm		= &amp;platform_dev_pm_ops,</span><br><span class="line">&#125;;</span><br><span class="line">struct bus_type &#123;</span><br><span class="line">	const char		*name;</span><br><span class="line">	const char		*dev_name;</span><br><span class="line">	struct device		*dev_root;</span><br><span class="line">	const struct attribute_group **bus_groups;</span><br><span class="line">	const struct attribute_group **dev_groups;</span><br><span class="line">	const struct attribute_group **drv_groups;</span><br><span class="line"></span><br><span class="line">	int (*match)(struct device *dev, struct device_driver *drv);</span><br><span class="line">	int (*uevent)(struct device *dev, struct kobj_uevent_env *env);</span><br><span class="line">	int (*probe)(struct device *dev);</span><br><span class="line">	int (*remove)(struct device *dev);</span><br><span class="line">	void (*shutdown)(struct device *dev);</span><br><span class="line"></span><br><span class="line">	int (*online)(struct device *dev);</span><br><span class="line">	int (*offline)(struct device *dev);</span><br><span class="line"></span><br><span class="line">	int (*suspend)(struct device *dev, pm_message_t state);</span><br><span class="line">	int (*resume)(struct device *dev);</span><br><span class="line"></span><br><span class="line">	int (*num_vf)(struct device *dev);</span><br><span class="line"></span><br><span class="line">	int (*dma_configure)(struct device *dev);</span><br><span class="line"></span><br><span class="line">	const struct dev_pm_ops *pm;</span><br><span class="line"></span><br><span class="line">	const struct iommu_ops *iommu_ops;</span><br><span class="line"></span><br><span class="line">	struct subsys_private *p;</span><br><span class="line">	struct lock_class_key lock_key;</span><br><span class="line"></span><br><span class="line">	bool need_parent_lock;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>2、在上面说的两个结构体中，有两个函数关乎与匹配相关。<code>.match		= platform_match,</code> 、<code>int (*match)(struct device *dev, struct device_driver *drv);</code> 。下面是 <code>platform_match</code> 的代码。</p>
<ul>
<li>在开始时传入设备和驱动的结构体。</li>
<li>检查 <code>driver_override</code> 参数是 否被设置。<code>driver_override</code> 允许用户强制指定设备要使用的驱动程序。</li>
<li>首先尝试使用设备树，在<code>of_driver_match_device</code> 函数中会根据设备树中的 <code>compatible</code> 属性里匹配设备和驱动程序。成功则直接返回。</li>
<li>然后在尝试ACPI（Advanced Configuration and Power Interface）进行匹配。在 <code>acpi_driver_match_device</code> 函数中会根据ACPI描述符进行匹配。成功则直接返回。</li>
<li>如果定义了<code>id_table</code> 尝试使用ID表进行匹配。</li>
<li>如果上面的匹配都失败了，则尝试使用设备的名称和驱动的名称进行匹配。</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static int platform_match(struct device *dev, struct device_driver *drv)</span><br><span class="line">&#123;</span><br><span class="line">	struct platform_device *pdev = to_platform_device(dev);</span><br><span class="line">	struct platform_driver *pdrv = to_platform_driver(drv);</span><br><span class="line"></span><br><span class="line">	/* When driver_override is set, only bind to the matching driver */</span><br><span class="line">	if (pdev-&gt;driver_override)</span><br><span class="line">		return !strcmp(pdev-&gt;driver_override, drv-&gt;name);</span><br><span class="line"></span><br><span class="line">	/* Attempt an OF style match first */</span><br><span class="line">	if (of_driver_match_device(dev, drv))</span><br><span class="line">		return 1;</span><br><span class="line"></span><br><span class="line">	/* Then try ACPI style match */</span><br><span class="line">	if (acpi_driver_match_device(dev, drv))</span><br><span class="line">		return 1;</span><br><span class="line"></span><br><span class="line">	/* Then try to match against the id table */</span><br><span class="line">	if (pdrv-&gt;id_table)</span><br><span class="line">		return platform_match_id(pdrv-&gt;id_table, pdev) != NULL;</span><br><span class="line"></span><br><span class="line">	/* fall-back to driver name match */</span><br><span class="line">	return (strcmp(pdev-&gt;name, drv-&gt;name) == 0);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Linux驱动</category>
      </categories>
      <tags>
        <tag>Linux学习</tag>
        <tag>平台驱动</tag>
        <tag>平台设备</tag>
        <tag>平台匹配</tag>
      </tags>
  </entry>
  <entry>
    <title>音频播放</title>
    <url>/2025/03/14/%E9%9F%B3%E9%A2%91%E6%92%AD%E6%94%BE/</url>
    <content><![CDATA[<h1 id="音频播放"><a href="#音频播放" class="headerlink" title="音频播放"></a>音频播放</h1><h3 id="前其准备"><a href="#前其准备" class="headerlink" title="前其准备"></a>前其准备</h3><p>我以思维导图的方式写了相关的原理和所需要的知识点。</p>
<p><img src="/2025/03/14/%E9%9F%B3%E9%A2%91%E6%92%AD%E6%94%BE/123.png"></p>
<p><a href="https://mubu.com/app/edit/home/6T6aswkdt2I#m">https://mubu.com/app/edit/home/6T6aswkdt2I#m</a></p>
<span id="more"></span>

<h3 id="代码解读"><a href="#代码解读" class="headerlink" title="代码解读"></a>代码解读</h3><h4 id="audioplay-c"><a href="#audioplay-c" class="headerlink" title="audioplay.c"></a>audioplay.c</h4><ul>
<li>全局变量</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool play_initialized = false;              // 播放是否初始化完成</span><br><span class="line">​static u8 *current_song = NULL;            // 当前播放的歌曲路径</span><br><span class="line">​static u32 last_fillnum = WAV_I2S_TX_DMA_BUFSIZE; // 上次填充的缓冲区大小</span><br><span class="line">​static DIR wavdir;                         // 音乐目录句柄</span><br><span class="line">​static u16 *wavindextbl;                   // WAV 文件索引表</span><br><span class="line">​static u16 totwavnum;                      // 路径下的文件总数</span><br><span class="line">​static u16 curindex;                       // 当前播放的歌曲索引</span><br><span class="line">​static char song_name[256] = &quot;Unknown&quot;;    // 当前歌曲名称</span><br></pre></td></tr></table></figure>

<ul>
<li><p>audio_start开始播放函数、audio_pause暂停播放函数、audio_resume从暂停状态到播放状态</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 开始音频播放</span><br><span class="line">void audio_start(void)</span><br><span class="line">&#123;</span><br><span class="line">    audiodev.status = 3 &lt;&lt; 0; // 开始播放+非暂停</span><br><span class="line">    I2S_Play_Start();</span><br><span class="line">&#125;</span><br><span class="line">// 暂停音频播放（仅停止硬件，不释放资源）</span><br><span class="line">void audio_pause(void)</span><br><span class="line">&#123;</span><br><span class="line">    audiodev.status &amp;= ~(1 &lt;&lt; 0); // 清除bit0，标记暂停</span><br><span class="line">    I2S_Play_Stop();              // 停止I2S硬件播放</span><br><span class="line">&#125;</span><br><span class="line">// 继续播放（从暂停状态恢复）</span><br><span class="line">void audio_resume(void)</span><br><span class="line">&#123;</span><br><span class="line">    audiodev.status |= (1 &lt;&lt; 0); // 设置bit0，继续播放</span><br><span class="line">    I2S_Play_Start();            // 恢复I2S硬件播放</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>结构体的内容如下：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef __packed struct</span><br><span class="line">&#123;  </span><br><span class="line">    u8 *i2sbuf1;</span><br><span class="line">    u8 *i2sbuf2;</span><br><span class="line">    u8 *tbuf;</span><br><span class="line">    FIL *file;</span><br><span class="line">    u8 status;</span><br><span class="line">&#125;__audiodev;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>audio_stop：停止播放并清理空间</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void audio_stop(void)</span><br><span class="line">&#123;</span><br><span class="line">    audiodev.status = 0;</span><br><span class="line">    I2S_Play_Stop();</span><br><span class="line">    play_initialized = false;</span><br><span class="line">    if (current_song)</span><br><span class="line">    &#123;</span><br><span class="line">        myfree(SRAMIN, current_song);</span><br><span class="line">        current_song = NULL;</span><br><span class="line">    &#125;</span><br><span class="line">    if (audiodev.file)</span><br><span class="line">    &#123;</span><br><span class="line">        f_close(audiodev.file);</span><br><span class="line">        myfree(SRAMIN, audiodev.file);</span><br><span class="line">        audiodev.file = NULL;</span><br><span class="line">    &#125;</span><br><span class="line">    if (wavindextbl)</span><br><span class="line">    &#123;</span><br><span class="line">        myfree(SRAMIN, wavindextbl);</span><br><span class="line">        wavindextbl = NULL;</span><br><span class="line">    &#125;</span><br><span class="line">	strcpy(song_name, &quot;Unknown&quot;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>清零status。关闭I2S。</li>
<li>重置音乐播放标志位为false。</li>
<li>释放申请的内存。</li>
<li>将音乐界面的标签重置为”Unknown”。</li>
</ul>
</li>
<li><p>audio_get_tnum：获取目录中的WAV文件总数</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">u16 audio_get_tnum(u8 *path)</span><br><span class="line">&#123;</span><br><span class="line">    u8 res;</span><br><span class="line">    u16 rval = 0;</span><br><span class="line">    DIR tdir;</span><br><span class="line">    FILINFO tfileinfo;</span><br><span class="line">    u8 *fn;</span><br><span class="line">    res = f_opendir(&amp;tdir, (const TCHAR*)path);</span><br><span class="line">    tfileinfo.lfsize = _MAX_LFN * 2 + 1;</span><br><span class="line">    tfileinfo.lfname = mymalloc(SRAMIN, tfileinfo.lfsize);</span><br><span class="line">    if (res == FR_OK &amp;&amp; tfileinfo.lfname != NULL)</span><br><span class="line">    &#123;</span><br><span class="line">        while (1)</span><br><span class="line">        &#123;</span><br><span class="line">            res = f_readdir(&amp;tdir, &amp;tfileinfo);</span><br><span class="line">            if (res != FR_OK || tfileinfo.fname[0] == 0) break;</span><br><span class="line">            fn = (u8*)(*tfileinfo.lfname ? tfileinfo.lfname : tfileinfo.fname);</span><br><span class="line">            res = f_typetell(fn);</span><br><span class="line">            if ((res &amp; 0XF0) == 0X40) rval++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    myfree(SRAMIN, tfileinfo.lfname);</span><br><span class="line">    return rval;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>使用f_opendir打开目录。<ul>
<li>f_opendir(DIR *dp, const TCHAR *path)<ul>
<li>第一参数dp：指向DIR结构体的指针，用于存储目录信息。、</li>
<li>返回值：FR—OK表示读取成功。</li>
</ul>
</li>
</ul>
</li>
<li>动态分配文件名缓冲区（lfname）。<ul>
<li>_MAX_LFN * 2 + 1<ul>
<li>_MAX_LFN ：表示最大的文件名长度。</li>
<li>乘2：再FATFS文件系统中LFN采用UTF-16，每个字符需要2个字节。</li>
<li>+1：表示在字符的结尾加上字符串结束符“\0”。</li>
</ul>
</li>
</ul>
</li>
<li>使用f_readdir依次读取目录中的文件。</li>
<li>使用f_typetell 判断文件类型，0x40表示WAV文件。<ul>
<li>f_typetell 用于判断文件类型。返回值res用于表示文件类型。</li>
<li>低8位：大类（音频、视频、文档等）</li>
<li>高8位：具体类型（MP3、WAV、TXT）</li>
</ul>
</li>
<li>返回WAV文件总数rval。</li>
</ul>
</li>
<li><p>audio_play_init：初始化播放器</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">u8 audio_play_init(void)</span><br><span class="line">&#123;</span><br><span class="line">    // 配置 WM8978 音频编解码器</span><br><span class="line">    WM8978_ADDA_Cfg(1, 0);  // 使能 DAC，关闭 ADC</span><br><span class="line">    WM8978_Input_Cfg(0, 0, 0);  // 关闭输入路径</span><br><span class="line">    WM8978_Output_Cfg(1, 0);    // 使能 DAC 输出</span><br><span class="line"></span><br><span class="line">    // 打开音乐目录</span><br><span class="line">    while (f_opendir(&amp;wavdir, &quot;0:/MUSIC&quot;)) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line">    // 获取 WAV 文件总数</span><br><span class="line">    totwavnum = audio_get_tnum(&quot;0:/MUSIC&quot;);</span><br><span class="line">    if (totwavnum == 0) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line">    // 分配内存</span><br><span class="line">    wavfileinfo.lfsize = _MAX_LFN * 2 + 1;</span><br><span class="line">    wavfileinfo.lfname = mymalloc(SRAMIN, wavfileinfo.lfsize);</span><br><span class="line">    current_song = mymalloc(SRAMIN, wavfileinfo.lfsize);</span><br><span class="line">    wavindextbl = mymalloc(SRAMIN, 2 * totwavnum);</span><br><span class="line"></span><br><span class="line">    // 构建 WAV 文件索引表</span><br><span class="line">    res = f_opendir(&amp;wavdir, &quot;0:/MUSIC&quot;);</span><br><span class="line">    if (res == FR_OK)</span><br><span class="line">    &#123;</span><br><span class="line">        curindex = 0;</span><br><span class="line">        while (1)</span><br><span class="line">        &#123;</span><br><span class="line">            u16 temp = wavdir.index;</span><br><span class="line">            res = f_readdir(&amp;wavdir, &amp;wavfileinfo);</span><br><span class="line">            if (res != FR_OK || wavfileinfo.fname[0] == 0) break;</span><br><span class="line">            fn = (u8*)(*wavfileinfo.lfname ? wavfileinfo.lfname : wavfileinfo.fname);</span><br><span class="line">            res = f_typetell(fn);</span><br><span class="line">            if ((res &amp; 0XF0) == 0X40)</span><br><span class="line">            &#123;</span><br><span class="line">                wavindextbl[curindex] = temp;</span><br><span class="line">                curindex++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    myfree(SRAMIN, wavfileinfo.lfname);</span><br><span class="line">    curindex = 0; // 从第一首开始</span><br><span class="line">    return audio_play_next();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>配置WM8978：启用DAC输出，关闭输入和ADC。</p>
<ul>
<li>使能DAC，即打开音频播放功能。</li>
<li>关闭ADC，不使用音频录制。</li>
<li>输入数据：关闭了ADC，音频数据不会从MIC或者LIN IN进入，而是通过I2S总线输入。</li>
<li>使能DAC输出，时音频信号能够经过耳机或者喇叭输出。</li>
</ul>
</li>
<li><p>打开0:&#x2F;MUSIC目录并统计WAV文件数。</p>
</li>
<li><p>分配内存：歌曲路径长文件名缓冲区和索引表</p>
</li>
<li><p>构建索引表：记录每个WAV文件的目录索引</p>
<ul>
<li><p>temp：存储这个当前遍历到的文件在目录中索引（从1开始计数）</p>
</li>
<li><p>例如：0:&#x2F;MUSIC目录下有四个文件，分别为one.wav、two.text、screen.wav、four.mp3。</p>
</li>
<li><table>
<thead>
<tr>
<th><code>wavdir.index (temp)</code></th>
<th>文件名</th>
<th><code>res = f_typetell(fn)</code></th>
<th><code>(res &amp; 0XF0) == 0X40</code></th>
<th><code>wavindextbl[curindex]</code></th>
<th><code>curindex</code></th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>one.wav</td>
<td>0x41</td>
<td>✅（是 WAV）</td>
<td><code>wavindextbl[0] = 1</code></td>
<td>1</td>
</tr>
<tr>
<td>2</td>
<td>two.txt</td>
<td>0x30</td>
<td>❌（不是 WAV）</td>
<td>不存入数组</td>
<td>1</td>
</tr>
<tr>
<td>3</td>
<td>screen.wav</td>
<td>0x41</td>
<td>✅（是 WAV）</td>
<td><code>wavindextbl[1] = 3</code></td>
<td>2</td>
</tr>
<tr>
<td>4</td>
<td>four.mp3</td>
<td>0x50</td>
<td>❌（不是 WAV）</td>
<td>不存入数组</td>
<td>2</td>
</tr>
</tbody></table>
</li>
</ul>
</li>
<li><p>调用audio_play_next函数，播放第一首歌。</p>
</li>
</ul>
</li>
<li><p>audio_play_next:播放下一首</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">u8 audio_play_next(void)</span><br><span class="line">&#123;</span><br><span class="line">    // 如果已有文件打开，先关闭</span><br><span class="line">    if (audiodev.file) &#123; f_close(audiodev.file); myfree(SRAMIN, audiodev.file); audiodev.file = NULL; &#125;</span><br><span class="line"></span><br><span class="line">    // 循环播放</span><br><span class="line">    if (curindex &gt;= totwavnum) curindex = 0;</span><br><span class="line"></span><br><span class="line">    // 获取下一首歌信息</span><br><span class="line">    res = f_opendir(&amp;wavdir, &quot;0:/MUSIC&quot;);</span><br><span class="line">    dir_sdi(&amp;wavdir, wavindextbl[curindex]);</span><br><span class="line">    res = f_readdir(&amp;wavdir, &amp;wavfileinfo);</span><br><span class="line">    </span><br><span class="line">    //构建完整的路径</span><br><span class="line">    strcpy((char*)current_song, &quot;0:/MUSIC/&quot;);</span><br><span class="line">    strcat((char*)current_song, (const char*)fn);</span><br><span class="line">    strncpy(song_name, (char*)fn, sizeof(song_name) - 1);</span><br><span class="line"></span><br><span class="line">    // 初始化 WAV 解码</span><br><span class="line">    res = wav_decode_init(current_song, &amp;wavctrl);</span><br><span class="line">    if (res == 0)</span><br><span class="line">    &#123;</span><br><span class="line">        // 配置 I2S 根据 WAV 参数</span><br><span class="line">        if (wavctrl.bps == 16) &#123; ... &#125;</span><br><span class="line">        else if (wavctrl.bps == 24) &#123; ... &#125;</span><br><span class="line">        I2S2_SampleRate_Set(wavctrl.samplerate);</span><br><span class="line">        I2S2_TX_DMA_Init(audiodev.i2sbuf1, audiodev.i2sbuf2, WAV_I2S_TX_DMA_BUFSIZE / 2);</span><br><span class="line">        i2s_tx_callback = wav_i2s_dma_tx_callback;</span><br><span class="line"></span><br><span class="line">        // 打开文件并填充缓冲区</span><br><span class="line">        audiodev.file = mymalloc(SRAMIN, sizeof(FIL));</span><br><span class="line">        res = f_open(audiodev.file, (TCHAR*)current_song, FA_READ);</span><br><span class="line">        f_lseek(audiodev.file, wavctrl.datastart);</span><br><span class="line">        last_fillnum = wav_buffill(audiodev.i2sbuf1, WAV_I2S_TX_DMA_BUFSIZE, wavctrl.bps);</span><br><span class="line">        last_fillnum = wav_buffill(audiodev.i2sbuf2, WAV_I2S_TX_DMA_BUFSIZE, wavctrl.bps);</span><br><span class="line">        play_initialized = true;</span><br><span class="line">        audio_start();</span><br><span class="line">        curindex++;</span><br><span class="line">    &#125;</span><br><span class="line">    return res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>关闭当前文件</li>
<li>检查索引，并实现循环播放。</li>
<li>打开目录，定位到下一首歌，构建完整的路劲，并提取歌曲名称。<ul>
<li>dir_sdi（）：用于设置目录读取索引，相当于告诉f_readdir（）从某个索引位置开始读取。这样就能<strong>直接跳到指定的 WAV 文件</strong>，而不是重新遍历整个目录。</li>
<li>构建完整的路径：<ul>
<li>strcpy((char*)current_song, “0:&#x2F;MUSIC&#x2F;“)：将字符串”0:&#x2F;MUSIC&#x2F;“复制到current_song。</li>
<li>strcat((char*)current_song, (const char*)fn);：将fn凭借到current_song的末尾。</li>
<li>strncpy(song_name, (char*)fn, sizeof(song_name) - 1);：将fn复制到song_name，去除\0。</li>
</ul>
</li>
</ul>
</li>
<li>调用wav_decode_init 解析WAV头文件，获取参数（采样率、位深、通道数等）<ul>
<li>i2s_tx_callback &#x3D; wav_i2s_dma_tx_callback;：当 I2S 通过 DMA 发送完一部分数据时，会触发 <code>wav_i2s_dma_tx_callback</code>。</li>
</ul>
</li>
<li>配置I2S和DMA，根据WAV文件的位深设置格式。<ul>
<li>f_lseek(audiodev.file, wavctrl.datastart);：<ul>
<li><code>f_lseek()</code> <strong>用于移动文件指针</strong>，</li>
<li><code>wavctrl.datastart</code> 代表<strong>WAV 音频数据起始地址</strong>，</li>
</ul>
</li>
<li>last_fillnum &#x3D; wav_buffill(audiodev.i2sbuf1, WAV_I2S_TX_DMA_BUFSIZE, wavctrl.bps);<br>  last_fillnum &#x3D; wav_buffill(audiodev.i2sbuf2, WAV_I2S_TX_DMA_BUFSIZE, wavctrl.bps);<ul>
<li><strong><code>wav_buffill()</code> 用于填充 I2S DMA 缓冲区</strong>，确保音频数据连续传输。</li>
</ul>
</li>
</ul>
</li>
<li>更行curindex索引，为下一首歌做准备。</li>
</ul>
</li>
<li><p>audio_play_prev：播放上一首歌</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">u8 audio_seek(u32 position)</span><br><span class="line">&#123;</span><br><span class="line">    if (!play_initialized || !audiodev.file) return 1;</span><br><span class="line"></span><br><span class="line">    // 计算目标文件位置</span><br><span class="line">    u32 file_pos = wavctrl.datastart + (position * (wavctrl.datasize / 100));</span><br><span class="line">    if (file_pos &gt; (wavctrl.datastart + wavctrl.datasize))</span><br><span class="line">        file_pos = wavctrl.datastart + wavctrl.datasize;</span><br><span class="line"></span><br><span class="line">    // 暂停播放</span><br><span class="line">    audio_pause();</span><br><span class="line"></span><br><span class="line">    // 调整文件指针</span><br><span class="line">    f_lseek(audiodev.file, file_pos);</span><br><span class="line">    wavctrl.cursec = (position * wavctrl.totsec) / 100;</span><br><span class="line"></span><br><span class="line">    // 重新填充缓冲区</span><br><span class="line">    last_fillnum = wav_buffill(audiodev.i2sbuf1, WAV_I2S_TX_DMA_BUFSIZE, wavctrl.bps);</span><br><span class="line">    last_fillnum = wav_buffill(audiodev.i2sbuf2, WAV_I2S_TX_DMA_BUFSIZE, wavctrl.bps);</span><br><span class="line"></span><br><span class="line">    // 恢复播放</span><br><span class="line">    audio_resume();</span><br><span class="line"></span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 非阻塞播放任务</span><br><span class="line">void audio_play_task(void)</span><br><span class="line">&#123;</span><br><span class="line">    if (play_initialized &amp;&amp; (audiodev.status &amp; (1 &lt;&lt; 1))) // 检查是否在播放状态</span><br><span class="line">    &#123;</span><br><span class="line">        if (wavtransferend)</span><br><span class="line">        &#123;</span><br><span class="line">            wavtransferend = 0;</span><br><span class="line">            if (last_fillnum != WAV_I2S_TX_DMA_BUFSIZE)</span><br><span class="line">            &#123;</span><br><span class="line">                audio_pause(); // 先暂停当前播放</span><br><span class="line">                if (audio_play_next() != 0) // 切换到下一首</span><br><span class="line">                &#123;</span><br><span class="line">                    audio_stop();</span><br><span class="line">                    printf(&quot;Music list finished\n&quot;);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            else</span><br><span class="line">            &#123;</span><br><span class="line">                if (wavwitchbuf)</span><br><span class="line">                    last_fillnum = wav_buffill(audiodev.i2sbuf2, WAV_I2S_TX_DMA_BUFSIZE, wavctrl.bps);</span><br><span class="line">                else</span><br><span class="line">                    last_fillnum = wav_buffill(audiodev.i2sbuf1, WAV_I2S_TX_DMA_BUFSIZE, wavctrl.bps);</span><br><span class="line">                wav_get_curtime(audiodev.file, &amp;wavctrl);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>与 audio_play_next() 类似，但将 curindex 减 1，并在边界时循环到最后一首。</li>
</ul>
</li>
<li><p>audio_play_task：非阻塞播放任务</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void audio_play_task(void)</span><br><span class="line">&#123;</span><br><span class="line">    if (play_initialized &amp;&amp; (audiodev.status &amp; (1 &lt;&lt; 1)))</span><br><span class="line">    &#123;</span><br><span class="line">        if (wavtransferend)</span><br><span class="line">        &#123;</span><br><span class="line">            wavtransferend = 0;</span><br><span class="line">            if (last_fillnum != WAV_I2S_TX_DMA_BUFSIZE)</span><br><span class="line">            &#123;</span><br><span class="line">                audio_pause();</span><br><span class="line">                if (audio_play_next() != 0) audio_stop();</span><br><span class="line">            &#125;</span><br><span class="line">            else</span><br><span class="line">            &#123;</span><br><span class="line">                if (wavwitchbuf)</span><br><span class="line">                    last_fillnum = wav_buffill(audiodev.i2sbuf2, WAV_I2S_TX_DMA_BUFSIZE, wavctrl.bps);</span><br><span class="line">                else</span><br><span class="line">                    last_fillnum = wav_buffill(audiodev.i2sbuf1, WAV_I2S_TX_DMA_BUFSIZE, wavctrl.bps);</span><br><span class="line">                wav_get_curtime(audiodev.file, &amp;wavctrl);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>检查播放状态和DMA传输完成标志。<ul>
<li>只有 <strong>播放初始化完成</strong> 且 <strong>当前处于播放状态</strong>，任务才会继续执行。</li>
</ul>
</li>
<li>如果缓冲区未满，切换下一首。<ul>
<li>last_fillnum !&#x3D; WAV_I2S_TX_DMA_BUFSIZE<ul>
<li>如果上一次的缓冲区未满，则当前的肯定为0，则表示读取结束，开始下一首。</li>
</ul>
</li>
</ul>
</li>
<li>否则，根据当前缓冲区填充数据，并更新播放时间。</li>
</ul>
</li>
</ul>
<h3 id="wavplay-c"><a href="#wavplay-c" class="headerlink" title="wavplay.c"></a>wavplay.c</h3><ul>
<li><p>全局变量</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__wavctrl wavctrl;       // WAV 文件控制结构体</span><br><span class="line">vu8 wavtransferend = 0;  // I2S DMA 传输完成标志</span><br><span class="line">vu8 wavwitchbuf = 0;     // 当前处理的缓冲区指示（0 = i2sbuf1，1 = i2sbuf2）</span><br></pre></td></tr></table></figure>
</li>
<li><p>wav_decode_init：初始化WAV文件解析</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">u8 wav_decode_init(u8* fname, __wavctrl* wavx)</span><br><span class="line">&#123;</span><br><span class="line">    FIL* ftemp;</span><br><span class="line">    u8 *buf;</span><br><span class="line">    u32 br = 0;</span><br><span class="line">    u8 res = 0;</span><br><span class="line"></span><br><span class="line">    ChunkRIFF *riff;</span><br><span class="line">    ChunkFMT *fmt;</span><br><span class="line">    ChunkFACT *fact;</span><br><span class="line">    ChunkDATA *data;</span><br><span class="line"></span><br><span class="line">    ftemp = (FIL*)mymalloc(SRAMIN, sizeof(FIL));</span><br><span class="line">    buf = mymalloc(SRAMIN, 512);</span><br><span class="line">    if (ftemp &amp;&amp; buf)  // 内存分配成功</span><br><span class="line">    &#123;</span><br><span class="line">        res = f_open(ftemp, (TCHAR*)fname, FA_READ);  // 打开文件</span><br><span class="line">        if (res == FR_OK)</span><br><span class="line">        &#123;</span><br><span class="line">            f_read(ftemp, buf, 512, &amp;br);  // 读取前 512 字节</span><br><span class="line">            riff = (ChunkRIFF *)buf;       // RIFF 块</span><br><span class="line">            if (riff-&gt;Format == 0X45564157)  // &quot;WAVE&quot; (WAV 文件标识)</span><br><span class="line">            &#123;</span><br><span class="line">                fmt = (ChunkFMT *)(buf + 12);  // FMT 块</span><br><span class="line">                fact = (ChunkFACT *)(buf + 12 + 8 + fmt-&gt;ChunkSize);  // FACT 块</span><br><span class="line">                if (fact-&gt;ChunkID == 0X74636166 || fact-&gt;ChunkID == 0X5453494C)  // &quot;fact&quot; 或 &quot;LIST&quot;</span><br><span class="line">                    wavx-&gt;datastart = 12 + 8 + fmt-&gt;ChunkSize + 8 + fact-&gt;ChunkSize;</span><br><span class="line">                else</span><br><span class="line">                    wavx-&gt;datastart = 12 + 8 + fmt-&gt;ChunkSize;  // 无 FACT/LIST 块</span><br><span class="line">                data = (ChunkDATA *)(buf + wavx-&gt;datastart);  // DATA 块</span><br><span class="line">                if (data-&gt;ChunkID == 0X61746164)  // &quot;data&quot;</span><br><span class="line">                &#123;</span><br><span class="line">                    // 提取 WAV 文件信息</span><br><span class="line">                    wavx-&gt;audioformat = fmt-&gt;AudioFormat;  // 音频格式 (1 = PCM)</span><br><span class="line">                    wavx-&gt;nchannels = fmt-&gt;NumOfChannels;  // 通道数</span><br><span class="line">                    wavx-&gt;samplerate = fmt-&gt;SampleRate;    // 采样率</span><br><span class="line">                    wavx-&gt;bitrate = fmt-&gt;ByteRate * 8;     // 位速 (bps)</span><br><span class="line">                    wavx-&gt;blockalign = fmt-&gt;BlockAlign;    // 块对齐</span><br><span class="line">                    wavx-&gt;bps = fmt-&gt;BitsPerSample;        // 位深 (16/24/32)</span><br><span class="line">                    wavx-&gt;datasize = data-&gt;ChunkSize;      // 数据大小</span><br><span class="line">                    wavx-&gt;datastart = wavx-&gt;datastart + 8; // 数据起始位置</span><br><span class="line"></span><br><span class="line">                    // 调试输出</span><br><span class="line">                    printf(&quot;wavx-&gt;audioformat:%d\r\n&quot;, wavx-&gt;audioformat);</span><br><span class="line">                    printf(&quot;wavx-&gt;nchannels:%d\r\n&quot;, wavx-&gt;nchannels);</span><br><span class="line">                    printf(&quot;wavx-&gt;samplerate:%d\r\n&quot;, wavx-&gt;samplerate);</span><br><span class="line">                    printf(&quot;wavx-&gt;bitrate:%d\r\n&quot;, wavx-&gt;bitrate);</span><br><span class="line">                    printf(&quot;wavx-&gt;blockalign:%d\r\n&quot;, wavx-&gt;blockalign);</span><br><span class="line">                    printf(&quot;wavx-&gt;bps:%d\r\n&quot;, wavx-&gt;bps);</span><br><span class="line">                    printf(&quot;wavx-&gt;datasize:%d\r\n&quot;, wavx-&gt;datasize);</span><br><span class="line">                    printf(&quot;wavx-&gt;datastart:%d\r\n&quot;, wavx-&gt;datastart);</span><br><span class="line">                &#125; else res = 3;  // 未找到 DATA 块</span><br><span class="line">            &#125; else res = 2;  // 非 WAV 文件</span><br><span class="line">        &#125; else res = 1;  // 打开文件失败</span><br><span class="line">    &#125;</span><br><span class="line">    f_close(ftemp);</span><br><span class="line">    myfree(SRAMIN, ftemp);</span><br><span class="line">    myfree(SRAMIN, buf);</span><br><span class="line">    return res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>分配内存：文件对象ftmp和缓冲区buf。</li>
<li>打开文件并读取前512字节。</li>
<li>检查RIFF块，确认文件格式为WAVE。</li>
<li>解析FMT块，提取音乐格式、通道数、采样率等信息。</li>
<li>检查是否FACT或者LIST块，并计算数据的起始位置datastart。</li>
<li>解析DATA块，获取数据大小，并更新datastart。</li>
</ul>
</li>
<li><p>wav_buffill：填充音频缓冲区</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">u32 wav_buffill(u8 *buf, u16 size, u8 bits)</span><br><span class="line">&#123;</span><br><span class="line">    u16 readlen = 0;</span><br><span class="line">    u32 bread;</span><br><span class="line">    u16 i;</span><br><span class="line">    u8 *p;</span><br><span class="line">    if (bits == 24)  // 24 位音频</span><br><span class="line">    &#123;</span><br><span class="line">        readlen = (size / 4) * 3;  // 计算读取字节数</span><br><span class="line">        f_read(audiodev.file, audiodev.tbuf, readlen, (UINT*)&amp;bread);</span><br><span class="line">        p = audiodev.tbuf;</span><br><span class="line">        for (i = 0; i &lt; size;)</span><br><span class="line">        &#123;</span><br><span class="line">            buf[i++] = p[1];  // 中间字节</span><br><span class="line">            buf[i] = p[2];    // 高字节</span><br><span class="line">            i += 2;</span><br><span class="line">            buf[i++] = p[0];  // 低字节</span><br><span class="line">            p += 3;</span><br><span class="line">        &#125;</span><br><span class="line">        bread = (bread * 4) / 3;  // 调整填充后的大小</span><br><span class="line">    &#125; else &#123;  // 16 位音频</span><br><span class="line">        f_read(audiodev.file, buf, size, (UINT*)&amp;bread);</span><br><span class="line">        if (bread &lt; size)  // 数据不足，补 0</span><br><span class="line">            for (i = bread; i &lt; size - bread; i++) buf[i] = 0;</span><br><span class="line">    &#125;</span><br><span class="line">    return bread;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>参数<ul>
<li>buf：目标缓冲区</li>
<li>size：要填充的字节数</li>
<li>bits：音频位深</li>
</ul>
</li>
<li>24位：没三个字节数据拓展为4字节，调整字节顺序<ul>
<li>在I2S传输时要么传输16位，要么传输32位数据。24位数据要补充一个字节的数据。</li>
<li>不能补充在最低位，也不能补充在最高位。最低为决定了信号的精度、最高决定以了信号的振幅，也就是音量。所以补充在中间。</li>
</ul>
</li>
<li>16位：直接读取数据，不足为0.</li>
</ul>
</li>
<li><p>wav_i2s_dma_tx_callback：DMA回调和时间计算</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void wav_i2s_dma_tx_callback(void)</span><br><span class="line">&#123;</span><br><span class="line">    u16 i;</span><br><span class="line">    if (DMA1_Stream4-&gt;CR &amp; (1 &lt;&lt; 19))  // DMA 传输 i2sbuf1</span><br><span class="line">    &#123;</span><br><span class="line">        wavwitchbuf = 0;</span><br><span class="line">        if ((audiodev.status &amp; 0X01) == 0)  // 暂停状态</span><br><span class="line">            for (i = 0; i &lt; WAV_I2S_TX_DMA_BUFSIZE; i++) audiodev.i2sbuf1[i] = 0;  // 填充 0</span><br><span class="line">    &#125; else &#123;  // DMA 传输 i2sbuf2</span><br><span class="line">        wavwitchbuf = 1;</span><br><span class="line">        if ((audiodev.status &amp; 0X01) == 0)  // 暂停状态</span><br><span class="line">            for (i = 0; i &lt; WAV_I2S_TX_DMA_BUFSIZE; i++) audiodev.i2sbuf2[i] = 0;  // 填充 0</span><br><span class="line">    &#125;</span><br><span class="line">    wavtransferend = 1;  // 标记传输完成</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>功能：I2S DMA 传输完成时的回调函数。</li>
<li>检查DMA流状态，判断当前传输的是buf1还是buf2.</li>
<li>更新wavwitchbuf 指示标志。</li>
<li>如果处于暂停状态，填充缓冲区为0，静音。</li>
<li>设置wavwitchbuf &#x3D; 1，通知传输完成。</li>
</ul>
</li>
<li><p>wav_get_curtime：计算当前播放时间</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void wav_get_curtime(FIL* fx, __wavctrl *wavx)</span><br><span class="line">&#123;</span><br><span class="line">    long long fpos;</span><br><span class="line">    wavx-&gt;totsec = wavx-&gt;datasize / (wavx-&gt;bitrate / 8);  // 总时长 (秒)</span><br><span class="line">    fpos = fx-&gt;fptr - wavx-&gt;datastart;                    // 当前位置</span><br><span class="line">    wavx-&gt;cursec = fpos * wavx-&gt;totsec / wavx-&gt;datasize;  // 当前播放时间 (秒)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>计算：总时常 &#x3D; 数据大小 &#x2F; 字节速率。<ul>
<li>字节速率bitrate &#x3D;  采样率 * 通道数 * 位深。单位（每秒初始多少位）。</li>
<li>除8：转化为每秒传输多少字节。</li>
</ul>
</li>
<li>当前时间 &#x3D; （当前偏移量 &#x2F; 数据大小） * 总时长。</li>
</ul>
</li>
</ul>
<h3 id="MP3解码"><a href="#MP3解码" class="headerlink" title="MP3解码"></a>MP3解码</h3><p>MP3格式音乐文件普遍存在我们的生活中，实际上MP3本身就是一种音频编码格式，全称为Moving Picture Experts Group Audio Layer III(MPEG Audio Layer 3)。</p>
<p>MP3的主要目标是通过压缩音频数据，在尽量保持音质的情况下大幅减少文件大小，从而方便存储和传输。MPEG音频文件时MPEG标准中声音部分，根据压缩质量和编码复杂程度划分为三层，即Layer-1、Layer2、Layer3，且分别对应MP1、MP2、MP3这三种声音文件，其中MP3压缩率为10：1至12：1，可以大大减少文件占用的存储空间大小。MPEG音频编码的层次越高，编码器越复杂，压缩率越高。</p>
<p>Mp3时利用人耳对高频声音信号的不敏感的特性，将时域波形信号转化为频率信号，并划分为多个频段，对不同频段使用不同的压缩率，加大高频的压缩比，对低频使用小压缩比，保证信号不失真。这样一来相当于抛弃人儿基本听不到的高频声音，只保留能听到的低频部分，这样可得到很高的压缩率。</p>
<p>简单来说，MP3就像一个聪明的“音频裁缝”，它知道哪些部分可以“剪掉”而不影响你听歌的体验。</p>
<h4 id="MP3文件结构"><a href="#MP3文件结构" class="headerlink" title="MP3文件结构"></a>MP3文件结构</h4><p>MP3文件大致分为3个部分：TAG-V2（ID3V2），音频数据，TAG-v1（ID3V1）.</p>
<p>ID3时MP3文件中附加的关于MP3的歌手信息，标题、专辑名称、年代、风格等信息，有两个版本，ID3V2和ID3V1.</p>
<p>ID3V1固定存放在MP3的尾部，固定字节长度为128字节，以TAG三个字符开头，后面跟上歌曲的信息。因为ID3V1长度有限，所以多添加了一个ID3V2，如果存在则在MP3的开头部分。它实际就是ID3V1的补充。</p>
<h5 id="ID3V2"><a href="#ID3V2" class="headerlink" title="ID3V2"></a>ID3V2</h5><p>ID3V2以灵活管理的方法MP3文件附件信息，比ID3V1可以存储很多的信息，同时也比ID3V1在结构上更复杂。</p>
<p>常用是ID3V2.3版本， 一个MP3文件最多就一个ID3V2.3标签。ID3V2.3标签由一个标签头和若干个标签帧或一个扩展标签头组成。关于曲目的信息如标题、 作者等都存放在不同的标签帧中，扩展标签头和标签帧并不是必要的，但每个标签至少要有一个标签帧。标签头和标签帧一起顺序存放在MP3文件的首部。</p>
<p><img src="/2025/03/14/%E9%9F%B3%E9%A2%91%E6%92%AD%E6%94%BE/I2S06.png" alt="标签头"></p>
<p>其标签大小计算如下：</p>
<p>Total_size&#x3D;(Size[0]&amp;0x7F)*0x200000+(Size[1]&amp;0x7F)*0x400+(Size[2]&amp;0x7F)*0x80+(Size[3]&amp;0x7F)</p>
<p>每个标签帧都有一个10个字节的帧头和至少一个字节的不定长度内容，在文件中是连续存放的。标签帧头由三个部分组成，即Frame[4]、Size[4]和Flags[2]。 Frame是用四个字符表示帧内容含义，比如TIT2为标题、TPE1为作者、TALB为专辑、TRCK为音轨、TYER为年代等等信息。 Size用四个字节组成32bit数表示帧大小。Flags是标签帧的标志位，一般为0即可。</p>
<ul>
<li>位置：文件开头</li>
<li>大小：可变化</li>
<li>结构：<ul>
<li>10字节头部：<ul>
<li>3字节：ID3</li>
<li>2字节：版本</li>
<li>1字节：标志</li>
<li>4字节：大小</li>
</ul>
</li>
<li>多个帧：如标题、艺术家、封面图片。</li>
</ul>
</li>
</ul>
<h5 id="ID3V1"><a href="#ID3V1" class="headerlink" title="ID3V1"></a>ID3V1</h5><p>ID3V1是早期的版本，可以存放的信息有限，但是编程比ID3V2简单。ID3V1是固定在MP3文件末尾的128字节。</p>
<p><img src="/2025/03/14/%E9%9F%B3%E9%A2%91%E6%92%AD%E6%94%BE/I2S07.png" alt="ID3V1结构"></p>
<p>其中，歌名是固定分配30个字节，如果歌名太短则以0填充完整，太长则被截断，其他信息存储类似。MP3的音乐类别总共147种，每一种对应着一个数组，比如0对应“Blues”、1对应“Classic Rock”、2对应“Country”等等。</p>
<ul>
<li>位置：文件末尾</li>
<li>大小：固定128字节</li>
<li>结构：<ul>
<li>3字节标识：TAG</li>
<li>30字节：标题</li>
<li>30字节：作家</li>
<li>30字节：专辑</li>
<li>4字节：年份</li>
<li>30字节：注释</li>
<li>1字节：流派</li>
</ul>
</li>
</ul>
<h5 id="MP3数据帧"><a href="#MP3数据帧" class="headerlink" title="MP3数据帧"></a>MP3数据帧</h5><p>音频数据是MP3文件的主体部分，它由一系列数据帧组成，每个数据帧包含一段音频的压缩数据，通过解码库解码即可得到对应的PCM音频数据，就可以通过I2S发送给WM8978芯片播放音乐，按照顺序解码所有的数据帧就可得到整个MP3文件的音轨。</p>
<p>每个数据帧由两个部分组成，帧头和数据实体，数据帧的长度可能不同，由位率决定。</p>
<ul>
<li><p>帧头记录着MP3数据帧的位率、采样率、版本等等信息。总共四个字节。</p>
</li>
<li><p>【可选】CRC：2字节，用于错误检测。</p>
</li>
<li><p>边信息：存储解码所需要的辅助信息。</p>
</li>
<li><p>主数据：压缩后的音频数据。</p>
</li>
</ul>
<p><img src="/2025/03/14/%E9%9F%B3%E9%A2%91%E6%92%AD%E6%94%BE/I2S08.png" alt="数据帧头结构"></p>
<p>位率在不同的版本和层都有不同的定义，具体的参考表中的数据，单位kbps。其中V1对应MPEG-1，V2对应MPEG-2和MPEG-2.5；L1对应Layer1， L2对应Layer2，L3对应Layer3，free表示位率可变，bad表示该定义不合法。</p>
<p><img src="/2025/03/14/%E9%9F%B3%E9%A2%91%E6%92%AD%E6%94%BE/I2S09.png" alt="位率选择"></p>
<p>例如，当Version&#x3D;11，Layer&#x3D;01，Bitrate_incdex&#x3D;0101时，描述为MPEG-1 Layer3(MP3)位率为64kbps。</p>
<p>数据帧长度取决于位率(Bitrate)和采样频率(Sampling_freq)，</p>
<p>公式为：帧大小（字节） &#x3D; (样本数 × 比特率 ÷ 采样率 ÷ 8) + 填充字节</p>
<p>具体计算如下：</p>
<blockquote>
<p>对于MPEG-1标准，Layer1时：</p>
<blockquote>
<p>Size&#x3D;(48000*Bitrate)&#x2F;Sampling_freq + Padding；</p>
</blockquote>
<p>Layer2或Layer3时：</p>
<blockquote>
<p>Size&#x3D;(144000*Bitrate)&#x2F;Sampling_freq + Padding；</p>
</blockquote>
<p>对于MPEG-2或MPEG-2.5标准，Layer1时：</p>
<blockquote>
<p>Size&#x3D;(24000*Bitrate)&#x2F;Sampling_freq + Padding；</p>
</blockquote>
<p>Layer2或Layer3时：</p>
<blockquote>
<p>Size&#x3D;(72000*Bitrate)&#x2F;Sampling_freq + Padding；</p>
</blockquote>
</blockquote>
<p>如果有CRC校验，则存放在在帧头后两个字节。接下来就是帧主数据(MAIN_DATA)。一般来说一个MP3文件每个帧的Bitrate是固定不变的， 所以每个帧都有相同的长度，称为CBR，还有小部分MP3文件的Bitrate是可变，称之为VBR，它是XING公司推出的算法。</p>
<p>MAIN_DATA保存的是经过压缩算法压缩后得到的数据，为得到大的压缩率会损失部分源声音，属于失真压缩。</p>
<h4 id="MP3解码库"><a href="#MP3解码库" class="headerlink" title="MP3解码库"></a>MP3解码库</h4><p>MP3文件是经过压缩算法压缩而存在的，为得到PCM信号，需要对MP3文件进行解码，解码过程大致为：比特流分析、霍夫曼编码、逆量化处理、立体声处理、 频谱重排列、抗锯齿处理、IMDCT变换、子带合成、PCM输出。整个过程涉及很多算法计算，要自己编程实现不是一件现实的事情，还好有很多公司经过长期努力实现了解码库编程。</p>
<p>现在合适在小型嵌入式控制器移植运行的有两个版本的开源MP3解码库，分别为Libmad解码库和Helix解码库，Libmad是一个高精度MPEG音频解码库， 支持MPEG-1、MPEG-2以及MPEG-2.5标准，它可以提供24bitPCM输出，完全是定点计算，更多信息可参考网站：<a href="http://www.underbit.com/%E3%80%82">http://www.underbit.com/。</a></p>
<p>Helix解码库支持浮点和定点计算实现，将该算法移植到STM32控制器运行使用定点计算实现，它支持MPEG-1、MPEG-2以及MPEG-2.5标准的Layer3解码。 Helix解码库支持可变位速率、恒定位速率，以及立体声和单声道音频格式。更多信息可参考网站：<a href="https://datatype.helixcommunity.org/Mp3dec%E3%80%82">https://datatype.helixcommunity.org/Mp3dec。</a></p>
<p>因为Helix解码库需要占用的资源比Libmad解码库更少，特别是RAM空间的使用，这对STM32控制器来说是比较重要的， 所以在实验工程中我们选择Helix解码库实现MP3文件解码。这两个解码库都是一帧为解码单位的，一次解码一帧，这在应用解码库时是需要着重注意的。</p>
<p>Helix解码库涉及算法计算，整个界面过程复杂，有兴趣可以深入探究，这里我们着重讲解Helix移植和使用方法。</p>
<p>Helix网站有提供解码库代码，经过整理，移植Helix解码库需要用到的的文件如图 <a href="https://doc.embedfire.com/mcu/stm32/f407batianhu/std/zh/latest/book/I2S.html#helix">Helix解码库文件结构</a> 。有优化解码速度，部分解码过程使用汇编实现。</p>
<p><img src="/2025/03/14/%E9%9F%B3%E9%A2%91%E6%92%AD%E6%94%BE/I2S014.png" alt="Helix解码库文件结构"></p>
<h5 id="Helix解码库移植"><a href="#Helix解码库移植" class="headerlink" title="Helix解码库移植"></a>Helix解码库移植</h5><p>现在我们可以移植Helix解码库工程中，实现MP3文件解码，将解码输出的PCM数据通过I2S接口发送到WM8978芯片实现音乐播放。</p>
<p>首先将需要用到的文件添加到工程中， 如图 <a href="https://doc.embedfire.com/mcu/stm32/f407batianhu/std/zh/latest/book/I2S.html#id44">添加Helix解码库文件到工程</a> 。MP3文件夹下文件是Helix解码库源码，工程移植中是不需要修改文件夹下代码的， 我们只需直接调用相关解码函数即可。建议自己移植时直接使用例程中mp3文件夹内文件。 我们是在mp3Player.c文件中调用Helix解码库相关函数实现MP3文件解码的，该文件是我们自己创建的。</p>
<p><img src="/2025/03/14/%E9%9F%B3%E9%A2%91%E6%92%AD%E6%94%BE/I2S015.png" alt="添加Helix解码库文件到工程"></p>
<p>功能实际上就是从SD卡内读取WAV格式文件数据，然后提取里边音频数据通过I2S传输到WM8978芯片内实现声音播放。 MP3播放器的功能也是类似的，只不过现在音频数据提取方法不同，MP3需要先经过解码库解码后才可得到“可直接”播放的音频数据。由此可以看到， MP3播放器只是添加了MP3解码库实现代码，在硬件设计上并没有任何改变，即这里直接使用“录音与回放实验”中硬件设计即可。</p>
<p>解码过程可能用到的Helix解码库函数有：</p>
<ul>
<li>MP3InitDecoder：初始化解码器函数</li>
<li>MP3FreeDecoder：关闭解码器函数</li>
<li>MP3FindSyncWord：寻找帧同步函数</li>
<li>MP3Decode：解码MP3帧函数</li>
<li>MP3GetLastFrameInfo：获取帧信息函数</li>
</ul>
<p>MP3InitDecoder函数初始化解码器，它会申请分配一个存储空间用于存放解码器状态的一个数据结构并将其初始化，该数据结构由MP3DecInfo结构体定义， 它封装了解码器内部运算数据信息。MP3InitDecoder函数会返回指向该数据结构的指针。</p>
<p>MP3FreeDecoder函数用于关闭解码器，释放由MP3InitDecoder函数申请的存储空间，所以一个MP3InitDecoder函数都需要有一个MP3FreeDecoder函数与之对应。 它有一个形参，一般由MP3InitDecoder函数的返回指针赋值。</p>
<p>MP3FindSyncWord函数用于寻址数据帧同步信息，实际上就是寻址数据帧开始的11bit都为“1”的同步信息。它有两个形参，第一个为源数据缓冲区指针，第二个为缓冲区大小， 它会返回一个int类型变量，用于指示同步字较缓冲区起始地址的偏移量，如果在缓冲区中找不到同步字，则直接返回-1。</p>
<p>MP3Decode函数用于解码数据帧，它有五个形参，第一个为解码器数据结构指针，一般由MP3InitDecoder函数返回值赋值；第二个参数为指向解码源数据缓冲区开始地址的一个指针， 注意这里是地址的指针，即是指针的指针；第三个参数是一个指向存放解码源数据缓冲区有效数据量的变量指针；第四个参数是解码后输出PCM数据的指针， 一般由我们定义的缓冲区地址赋值，对于双声道输出数据缓冲区以LRLRLR…顺序排列；第五个参数是数据格式选择，一般设置为0表示标准的MPEG格式。函数还有一个返回值， 用于返回解码错误，返回ERR_MP3_NONE说明解码正常。</p>
<p>MP3GetLastFrameInfo函数用于获取数据帧信息，它有两个形参，第一个为解码器数据结构指针，一般由MP3InitDecoder函数返回值赋值； 第二个参数为数据帧信息结构体指针，该结构体定义见 <a href="https://doc.embedfire.com/mcu/stm32/f407batianhu/std/zh/latest/book/I2S.html#i2s-25">代码清单:I2S-25</a> 。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef struct _MP3FrameInfo &#123;</span><br><span class="line">    int bitrate;        //位率</span><br><span class="line">    int nChans;         //声道数</span><br><span class="line">    int samprate;       //采样率</span><br><span class="line">    int bitsPerSample;  //采样位数</span><br><span class="line">    int outputSamps;    //PCM数据数</span><br><span class="line">    int layer;          //层级</span><br><span class="line">    int version;        //版本</span><br><span class="line">&#125; MP3FrameInfo;</span><br></pre></td></tr></table></figure>

<p>该结构体成员包括了该数据帧的位率、声道、采样频率等等信息，它实际上是从数据帧的帧头信息中提取的。</p>
<h5 id="MP3示例"><a href="#MP3示例" class="headerlink" title="MP3示例"></a>MP3示例</h5><p>用Yesterday Once More这首歌为示例，来重新学习一下MP3文件结构。</p>
<ul>
<li><p>MP3文件主要由下面几个主要的组成部分组成</p>
<ul>
<li>ID3V2标签：存储歌曲的元数据</li>
<li>音频帧序列：压缩后的音频数据、核心内容</li>
<li>其他元数据：如 Xing 头（用于 VBR 文件）。</li>
</ul>
</li>
<li><p>对于Yesterday Once More这首歌的MP3文件，文件结构大致如下</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[ID3v2 标签] + [音频帧序列] + [ID3v1 标签]</span><br></pre></td></tr></table></figure></li>
</ul>
<p><strong>1、ID3V2标签（文件开头）</strong></p>
<p>ID3V2标签通常位于文件开头，存储着歌曲的元数据，假设文件内容如下：</p>
<ul>
<li>标题：Yesterday Once More</li>
<li>艺术家：The Carpenters</li>
<li>专辑：Now &amp; Then</li>
<li>年份：1973</li>
</ul>
<p>ID3V2结构由10个字节的帧头，和多个帧组成。</p>
<ul>
<li><p>10个字节的头部</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">49 44 33 03 00 00 00 00 07 D0</span><br></pre></td></tr></table></figure>

<ul>
<li>49 44 33：标识ID3</li>
<li>03 00：版本号</li>
<li>00：标志位</li>
<li>00 00 07 D0 ：标签大小（7位编码，实际大小2000字节）</li>
</ul>
</li>
<li><p>帧</p>
<ul>
<li><p>TIT2（标题）</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">54 49 54 32 00 00 00 15 00 00 01 59 65 73 74 65 72 64 61 79 20 4F 6E 63 65 20 4D 6F 72 65</span><br></pre></td></tr></table></figure>

<ul>
<li>54 49 54 32：帧ID（TIT2 &#x3D; 标题）</li>
<li>00 00 00 15：大小（21字节）</li>
<li>00 00 标志</li>
<li>01 ：编码（UTF-16）</li>
<li>剩下的：内容Yesterday Once More</li>
</ul>
</li>
<li><p>TPE1（艺术家）</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">54 50 45 31 00 00 00 0F 00 00 00 54 68 65 20 43 61 72 70 65 6E 74 65 72 73</span><br></pre></td></tr></table></figure>

<ul>
<li>54 50 45 31：帧ID（TPE1 &#x3D; 艺术家）</li>
<li>00 00 00 0F：大小15字节</li>
<li>00 00 ：标志</li>
<li>00 ： 编码（ISO-8859-1）</li>
<li>54 68 65 …：内容The Carpenters</li>
</ul>
</li>
<li><p>类似的还有 TALB（专辑）、TYER（年份）等帧。假设 ID3v2 总大小约为 2000 字节。</p>
</li>
</ul>
</li>
</ul>
<p><strong>音频帧序列（文件主体）</strong></p>
<p>音频帧序列是 MP3 文件的核心，包含压缩后的音频数据。以 128 kbps、44.1 kHz、立体声为例，每帧的特性如下：</p>
<ul>
<li>帧时长：1152个样本 &#x2F; 44.1khz &#x3D; 26.12毫秒。</li>
<li>帧大小：（1152 * 128000 &#x2F; 44100 &#x2F;8） &#x3D; 417字节</li>
<li>总帧数：240 &#x2F; 0.02612秒&#x2F;帧 &#x3D; 9195帧</li>
<li>音频数据大小：417字节 * 9195 &#x3D; 3。83MB</li>
</ul>
<p>单帧结构由以下部分组成：</p>
<ul>
<li><p>帧头：4字节</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">FF FB 90 00</span><br></pre></td></tr></table></figure>

<ul>
<li>FF FB<ul>
<li>11111111 1111（11个1）：同步字，用来辨别的</li>
<li>10：MPEG-1.</li>
<li>11：Layer-3</li>
<li>1：无CRC</li>
</ul>
</li>
<li>90<ul>
<li>1001：比特率（128kbps）</li>
<li>00 ：采样率索引（44.1khz）</li>
<li>0：无填充</li>
<li>0：私有位</li>
</ul>
</li>
<li>00<ul>
<li>00：立体声</li>
<li>00：模式拓展</li>
<li>0：无版权</li>
<li>0：非原始</li>
<li>00：误强调</li>
</ul>
</li>
</ul>
</li>
<li><p>CRC【可选】：2字节</p>
</li>
<li><p>边信息：32字节</p>
<ul>
<li>边信息存储解码参数，比如：<ul>
<li>比特池指针：知识主数据位置</li>
<li>量化表选择：用于反量化</li>
<li>子带分配：比特分配给32个子带</li>
</ul>
</li>
</ul>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">01 00 02 ... (32 字节)</span><br></pre></td></tr></table></figure>

<ul>
<li>具体的由编码器生成，由解码时解析</li>
</ul>
</li>
<li><p>主数据：压缩音频数据。（381字节）</p>
<ul>
<li><p>主数据时经过霍夫曼编码的频率系数，长度未：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">417 - 4（帧头） - 32（边信息） = 381 字节</span><br></pre></td></tr></table></figure>
</li>
<li><p>内容可能是</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">4A 3B 12 ... (381 字节)</span><br></pre></td></tr></table></figure>

<p>  这些数据表示Yesterday Once More开头部分的压缩音频。</p>
</li>
</ul>
</li>
</ul>
<p><strong>ID3V1标签（文件末尾）</strong></p>
<p>ID3V1是固定的128字节，假设内容如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">54 41 47 59 65 73 74 65 72 64 61 79 20 4F 6E 63 65 20 4D 6F 72 65 00 ... [30 字节标题]</span><br><span class="line">54 68 65 20 43 61 72 70 65 6E 74 65 72 73 00 ... [30 字节艺术家]</span><br><span class="line">4E 6F 77 20 26 20 54 68 65 6E 00 ... [30 字节专辑]</span><br><span class="line">31 39 37 33 [4 字节年份]</span><br><span class="line">00 ... [30 字节注释]</span><br><span class="line">03 [1 字节流派，03 = Pop]</span><br></pre></td></tr></table></figure>

<ul>
<li><p>54 41 47（TAG）：标识 ID3v1。</p>
</li>
<li><p>字段长度固定，填充 00（空字符）。</p>
</li>
</ul>
<p><strong>文件整体布局</strong></p>
<ul>
<li><p>ID3v2：2000 字节（开头）。</p>
</li>
<li><p>音频帧：3,834,315 字节（9195 × 417）。</p>
</li>
<li><p>ID3v1：128 字节（结尾）。</p>
</li>
<li><p>总大小：2000 + 3,834,315 + 128 &#x3D; 3,836,443 字节（接近 3.84 MB，误差可能是文件系统对齐）。</p>
</li>
</ul>
<p><strong>解码视角</strong></p>
<p>播放器（比如用 Helix 解码库）读取文件时：</p>
<ol>
<li>跳过 ID3v2：识别 49 44 33，跳过 2000 字节。</li>
<li>解析帧：<ul>
<li>找到 FF FB，读取帧头。</li>
<li>解析边信息，解码主数据为 PCM（1152 个样本，约 26 毫秒）。</li>
<li>重复 9195 次，生成 240 秒音频。</li>
</ul>
</li>
<li>读取 ID3v1：显示歌曲信息。</li>
</ol>
]]></content>
      <categories>
        <category>stm32F407</category>
      </categories>
      <tags>
        <tag>stm32F407</tag>
        <tag>多媒体项目</tag>
      </tags>
  </entry>
</search>
